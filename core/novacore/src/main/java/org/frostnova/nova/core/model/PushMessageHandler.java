package org.frostnova.nova.core.model;

/**
 * 推送消息的处理器句柄
 * <p>
 * 数据源把推送配置读进来之后，每条推送消息都要挂上一个「到时候由谁来发」的实例。
 * <b>发是推送侧的事，数据源只负责把这个实例挂上去</b>——所以这里只是一个空接口，
 * 它在模型这一侧唯一的作用是给 {@link PushMessage#getHandlerInstance()} 一个类型，
 * 具体能做什么由推送侧的实现自己定义（见推送侧的事件处理器接口）。
 * <p>
 * 为什么不直接把处理器接口写在这里：那个接口带着「渲染成什么样」「在界面上叫什么名字」
 * 「有哪些可配参数」这一整套推送与界面的语义，把它放进模型，等于让读配置这件事
 * 连带背上整条推送链路。<b>而读配置的那一侧，一次也不会调用它。</b>
 */
public interface PushMessageHandler {
}
