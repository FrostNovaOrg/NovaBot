# 安全说明

## 报告漏洞

请不要通过公开 issue 报告安全问题。请使用本仓库的 **Security → Report a vulnerability**（私密漏洞报告）
描述问题与复现步骤，收到后会尽快处理。

---

## 使用立场

本项目面向**中小型公会、个人势主播及其运营人员**，用于**自有或已获授权**的直播间数据采集、
通知与运营分析，典型规模是**一到十几个直播间**。

我们**强烈不建议**将它用于非法或未经授权的大规模数据监控与爬取，包括但不限于：
数十上百个直播间的批量监控、商业数据服务、竞品情报采集、未获主播授权的第三方监控。

**诚实地说**：项目采用 AGPL-3.0 开源，我们**在技术上无法阻止**此类使用。
声明立场的意义在于确立设计目标与支持范围，而非假装能够强制执行——
此类使用不属于我们的设计目标，我们不为其提供支持，也不会为其调整架构。

与之配套的一条设计选择：**本项目不做风控对抗**，不伪造浏览器指纹、
不伪造观看行为数据、不实现针对平台反自动化机制的对抗手段。
因此在大规模采集场景下，**不保证账号安全，也不保证数据完整性**，
触发平台限流或风控的后果由使用者承担。

这不是能力不足，是刻意为之：连接行为按「礼貌客户端」设计——指数退避、全局连接节流、
不高频轮询、遵守平台的限流反馈。**这是产品的一部分，不是待优化项**，
任何「提高采集速度或密度」的需求都要先过这条线。

---

## 部署建议

### 必做

**不要把服务端口暴露到公网。** 默认配置只监听 `127.0.0.1`：

```yaml
server:
  address: 127.0.0.1
```

若确需从其他机器调用推送接口，请同时做到三点：配置强随机的 `api-token`、把来源地址加入
`allow-ips` 白名单、在防火墙上限制来源。

```yaml
starbot:
  adapter:
    onebot:
      security:
        allow-ips:
          - 127.0.0.1/32
          - 10.0.0.0/8
        fail-on-weak-config: true   # 外网可达的部署建议开启
      senders:
        - name: qq-onebot
          api-token: <openssl rand -base64 32 生成的随机串>
```

**同样要管住 OneBot 实现（NapCat 等）那一侧的端口。**
本项目的安装脚本**不会、也无法**替你配置它——它是独立进程，由你自行安装。
本文档此前对它只讲过封号风险，**没有讲过暴露面，这是个疏漏**。

NapCat 一般会开三个监听，**三个都要绑回环并各自设令牌**：

| 端口（默认） | 是什么 | 风险 |
|---|---|---|
| 3000 | OneBot HTTP | 能借机器人发消息 |
| 3001 | OneBot WebSocket | 同上 |
| **6099** | **WebUI** | 🔴 **拿到它就等于拿到这个 QQ 号**——能换登录、能往任意群发消息、能读历史 |

```jsonc
// onebot11_<QQ号>.json
"httpServers":      [{ "host": "127.0.0.1", "port": 3000, "token": "<强随机串>" }],
"websocketServers": [{ "host": "127.0.0.1", "port": 3001, "token": "<强随机串>" }]
// webui.json
{ "host": "127.0.0.1", "port": 6099, "token": "<强随机串>" }
```

⚠️ **别假设它默认就是安全的——装完自己核一遍**：

```bash
ss -tlnp | grep -E ':(3000|3001|6099)\b'   # 每一行都应是 127.0.0.1，不是 0.0.0.0
```

需要远程用 WebUI 扫码时，建端口转发过去，不要把 6099 放开：

```bash
ssh -L 6099:127.0.0.1:6099 用户名@服务器地址
```

**使用小号登录哔哩哔哩。** 程序默认开启 `auto-follow`，会主动用登录账号执行关注操作。
凭据一旦泄露，攻击者即取得该账号的完整控制权。

**配置界面若要开到公网，必须启用口令登录。** 放开 `allow-ips` 而不设口令，
等于把「改推送目标、看运行数据、读群成员绑定关系」的权限对全网敞开——
启动日志会为这种组合打出 ERROR，但配置本身不会被拒绝。

```yaml
starbot:
  core:
    config-ui:
      allow-ips:
        - 0.0.0.0/0               # 交给反向代理与 https 去挡
      auth:
        password: <直接填明文即可，启动时会自动换成哈希写回>
        # totp 默认为 true，登录后按界面提示扫码绑定即可，密钥会自动写回 totp-secret
```

开启后主要凭据改为登录换会话。几点须知：

- **二次验证默认要求**。设了口令却没绑验证器时界面会持续提示，可以「暂不绑定」跳过本次登录。
  真的不要就把 `auth.totp` 改成 `false`——那是个应当写下来的决定，
  因为此后面板的安全性就完全押在一个口令上了
- 口令会自动以哈希形式保存，但**二次验证密钥必须明文存放**（校验时要用它），
  因此仍要把 `application.yml` 的权限收紧到 `600`
- 必须走 https。会话 Cookie 的 `Secure` 标记跟随当前连接判断，
  部署在反向代理之后时需要设置 `server.forward-headers-strategy`，
  否则程序看到的永远是明文连接，Cookie 不会带上 `Secure`
- 怀疑 Cookie 泄漏时用界面上的「退出全部设备」，不必重启进程

**启动令牌通道默认关闭。** 打开它（`auth.operator-token: true`）之后，
启动日志里那个带 `?token=` 的地址能绕过口令与二次验证直接进入面板——
**这个令牌等同于口令加验证器**，而它走地址栏，会进反向代理的访问日志。
每次经此通道进入都会记一条 WARN 日志。

忘记口令、验证器丢了或手机换了时：把 `auth.operator-token` 改成 `true` 重启，
启动日志会打印那个地址，进去改完口令再改回 `false`。
真要长期开着的话，令牌的强度取决于 `config-ui.token`：留空时每次启动重新生成 43 字符随机串，
显式配置时务必也用随机串。

设置页里的口令、令牌与密钥一律显示为占位值，需要时点「显示」也只会看到占位值本身；
但「高级：直接编辑配置文件」显示的是文件原文，其中都是明文。**直播时不要展开那一段。**

### 建议

- 以独立的低权限用户运行，或放入容器
- 定期检查 `cookies.json` 与 `cookies.key` 的权限仍为 `600`
- 打包日志或工作目录发给他人前，先移除 `cookies.json`、`cookies.key` 与 `datasource.json`；
  4.1.0 起还要移除 **`state.json`**，它含群成员的 QQ 号与其绑定的哔哩哔哩 uid
- 关闭不需要的调试日志：`live-room-raw-message-log` 与 `dynamic-raw-message-log` 会把原始消息完整写入日志

### 关于观众数据

4.1.0 起会按用户记录直播间内的互动量（弹幕条数、礼物价值等），用于排行榜与个人数据查询，
并缓存观众头像地址。这些都是**直播间里本就公开可见**的信息，不涉及私聊内容或好友关系。

两点仍值得注意：

- **账号绑定已停用**。没有接口能证明某个 QQ 号与某个哔哩哔哩账号属于同一人，绑定只核对昵称防打错，
  而个人数据查询正建立在这条对应关系上。5.1.0 起绑定与个人数据查询的命令一并撤掉，
  控制台也不再显示这一块。**已有的绑定记录保留在 `state.json` 里但不再生效**
- **`state.json` 里的历史绑定关系仍是可以反推出人的**。换机器、发日志、交接部署时按上面那条处理
- **金额是另一回事**。直播收益、每位观众送了多少钱，属于主播的经营信息与观众的消费记录，
  与「弹幕多热闹」不是同一类。4.2.0 起「谁能看到金额」是会话自己的属性，
  **默认私聊显示、群聊隐藏**，且只能在控制台改——若允许群里的人自己把它打开，这道设置就形同虚设

### 关于反向代理

若在 Nginx 之后部署，需显式开启 `trust-proxy`，否则来源 IP 始终是代理的地址：

```yaml
starbot:
  adapter:
    onebot:
      security:
        trust-proxy: true
```

**只有在请求确实全部经过可信代理时才可开启。** 直接暴露的服务开启此项等同于让 IP 白名单失效——
任何人都能伪造 `X-Forwarded-For`。

**开启后取的是 `X-Forwarded-For` 里最右侧的那一段，不是最左侧。** 这个头是追加式的：
客户端自己带一个 `X-Forwarded-For: 1.2.3.4` 过来，反代（例如 nginx 的
`$proxy_add_x_forwarded_for`）把真实对端接在后面，上游收到的是 `1.2.3.4, <真实对端>`。
最左侧那一段完全由客户端说了算——拿它做白名单，等于白名单可以被一个请求头绕过。

因此这里假定**恰好一层可信代理**。若前面还有一层（CDN → 自建反代 → 本程序），
最右侧会是内层代理的地址，白名单会把所有人挡在门外——**失败方向是拒绝而不是放行**，
这也是它能当默认取法的理由。这种部署请在最外层用
`proxy_set_header X-Forwarded-For $remote_addr;` 覆写该头。

**⚠️ 还有一处取的是最左侧，它不在本程序里，在 Spring 中。**
开启 `server.forward-headers-strategy` 后，Spring 的 `ForwardedHeaderFilter` 会用
`X-Forwarded-For` **改写 `getRemoteAddr()`**，而它取的是**最左侧**。
配置控制台的登录锁定与只读口令代签发的来源 IP 都来自这个方法。

于是在「**开着 forwarded 策略 + 反代是追加式**」这个组合下，
**按来源 IP 的登录锁定不可依赖**：攻击者换着伪造最左侧那一段，一次锁定也触发不了。

这个组合下的兜底是**口令校验的全局速率上限**（每分钟 20 次，见 `LoginThrottle`）——
它按 IP 计数管不着的那条路，正是全局桶存在的理由。

**本仓库提供的反代模板一律覆写该头**（`proxy_set_header X-Forwarded-For $remote_addr;`），
不用 `$proxy_add_x_forwarded_for`。覆写之后头里只有一段，最左＝最右＝真实对端，
上面那个组合就不成立——**模板这么写不是随手写的，改它之前请先读完这一段**。

本程序没有自己解析并覆盖 `getRemoteAddr()`：那样做的失败方向是把正常用户挡在门外，
代价大于收益。若你的部署确实是追加式反代，请改成覆写，或不要开 `forward-headers-strategy`。

### 关于安全响应头

本程序对所有响应加 `X-Content-Type-Options: nosniff` 与 `X-Frame-Options: SAMEORIGIN`，
并对 `/config` 与 `/nova` 两个命名空间加 `Cache-Control: no-store`
（前者是控制台，`/config/api/values` 这类端点返回的是配置项的当前值；后者是代签发只读口令，
响应体里就是那把口令）。

`X-Frame-Options` 取 `SAMEORIGIN` 而非 `DENY` 是**有意的**：点击劫持的威胁模型是跨站嵌套，
`SAMEORIGIN` 已经挡住；`DENY` 多防的只有同源嵌套，而同源嵌套是本程序自己在用的形态。

内容安全策略（CSP）尚未提供：写窄了会打死 WebUI 代理与内联资源，收益与风险要单独评估。

---

## 已知风险（非本项目可修复）

- **NapCat 等 OneBot 实现属于非官方 QQ 协议实现**，使用存在账号被封禁的风险
- **QQ 登录态可能在无预兆的情况下失效，需要重新扫码**。NapCat 的快速登录依赖 QQ
  保存的登录态，实测并不总是可靠：服务端或 NapCat 重启后，有一定概率提示登录态已失效。
  根因是腾讯对非官方协议实现的风控，NapCat 侧无法根治
  （上游 [#1632](https://github.com/NapNeko/NapCatQQ/issues/1632)、
  [#1728](https://github.com/NapNeko/NapCatQQ/issues/1728) 均已关闭，
  给出的是更新 GUID、降级版本一类的绕行建议而非修复），本项目侧同样无法代为解决。
  **本项目能做的是不让它静默发生**：OneBot 连接中断会触发健康告警，
  配置 Webhook 告警通道后可推送到手机——注意 QQ 告警此时已随之失效，
  必须配置不经过机器人链路的通道，详见配置模板中 `alert` 段的说明
- **哔哩哔哩的接口风控**可能导致直播间长连接被限制，此时弹幕、礼物等事件无法接收；
  开播下播推送可由备用直播推送兜底
