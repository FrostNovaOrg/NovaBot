# 更新日志

本文件记录本仓库相对上游 StarBot 的改动，依据 AGPL-3.0 第 5 条要求提供。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [未发布]

### 新增

- 首页健康栏能看出 B 站长连协议出现未知消息类型／操作码。
- 风控判定的观测里说得出口「这几分钟是消息收到了但解析失败」；这类时段在下播报告的采集缺口里单列成「解析降级」，与「断流」分开。
- 下播报告、动态图和数据查询图改由独立的报告插件绘制；装上哔哩哔哩插件时才会一并装上。
- 首页健康栏能看出弹幕解析失败、关键字段缺失与接口应答缺数据。
- 插件现在可以在初始设置向导里申报自己的一步。
- 插件现在可以在控制台首页申报自己的卡片。
- 插件现在可以向核心申报配置键的旧名字，以及保存之后立刻生效的配置项。
- 插件页可以登记多个脚本；顶级页刷新时会带上地址栏里的子路径；向导里的插件步骤可以声明允许跳过。
- 向导里的插件步骤现在可以自己判断能不能进下一步、自己处理跳过按钮，并在向导走完时给出一个「去看看」的去处。
- 主播页改由控制台插件提供：装上才有「主播」入口，卸掉入口跟着消失。
- 推送页改由控制台插件提供：装上才有「推送」入口，卸掉入口跟着消失。设置页保存不再夹带推送配置。

### 变更

- 整理了许可与来源说明的位置，控制台设置页底部加上许可与源码地址。
- 重写了 NapCat 扩展与 OneBot 消息转换这两处的实现，行为未变：接口地址、配置项、日志用词和发出去的消息内容都和原先一样。
- 设置页「登录与安全」四项改成与其他设置相同的横行；改密码在页内弹窗里进行；登录相关文案把「控制台口令」改称「密码」；登记通行密钥时在控制台内给设备起名，列表过宽时可以横向滚动。
- 动态通知与下播报告两个推送处理器随报告插件挪到了 `com.starlwr.bot.report.handler`（原先在 `com.starlwr.bot.bilibili.handler`）。**旧名字仍然认得**：`datasource.json` 里照旧写的推送照常发，按旧名存下来的自定义模板照旧读得到，启动后每个旧名会打一条提醒。建议照 `datasource.example.json` 改过来；改不改、什么时候改，由你决定。
- 控制台主导航「QQ 推送」改为「推送」。
- 初始设置向导里的「第一位主播，推到哪」和首页的「今日」卡改由控制台插件提供：装上才有，卸掉跟着消失；没装时向导是四步。推送总开关跟着挪到「今日」卡上，首页的「立即自检」改挂在探针卡里。
- 初始设置向导和首页里「连上 QQ 机器人」这类句子随插件申报的词表显示；没申报时改说「连上机器人」。
- 发送测试失败时的提示、配置里缺号码的说明、以及推送堆积时的健康检查，不再写出具体聊天平台名。
- 设置页告警、推送目标、机器人连接、口令说明和登录页里不再写死平台名；没申报词表时改说「机器人」「用户号」这类中性词。
- 更多配置项的说明改说「平台」「机器人」，不再写出具体聊天平台名。
- 机器人告警的目标三项从 `starbot.core.alert.qq-platform`／`qq-type`／`qq-num` 挪到了 `starbot.adapter.onebot.alert`（`platform`／`type`／`num`）。**旧键仍然认得**，只写旧位置的照常生效，启动时会打一条提醒；两处都写时以新位置为准，新位置没写到的项才取旧位置的值。改不改、什么时候改，由你决定。
- NapCat 代登录凭据四项从 `starbot.core.config-ui.napcat` 挪到了 `starbot.adapter.onebot.napcat`（`token`／`token-hash`／`totp-secret`／`address`）。**旧键仍然认得**，只写旧位置的照常生效，启动时会打一条提醒；两处都写时以新位置为准。若旧位置还留着 token 明文，启动时会换算成哈希写到新位置并把旧位置清空。
- 重写了核心的图像、字体、二维码、邮件等工具件，行为未变。

### 修复

- 下播时明细留档出错不再影响下播推送与报告。
- 控制台在插件未提供机器人实现名时，连接页标题和日志入口改说「机器人」，不再把兜底词写进括号。
- 地址栏已是 /config#/… 时登录成功不再停在登录页：落点改为先算再走，同址带片段时整页重载，无片段时照常跳转。
- 登录页「用口令登录」折叠头改为次级按钮样式，展开后收成居中小字标题，不再是一行裸文字。
- 推送页「通道一览」表格列多时不再撑破卡片，改为在卡片内横向滚动。
- 控制台侧栏「推送」「主播」两条入口补回了图标，与其余四条一致：这两页改由插件提供之后，入口一直只有文字。插件现在可以为自己的顶级页申报图标（只给形状，外框由控制台统一套），没申报的画一个中性图标。
- 推送页读得懂 `datasource.json` 里按处理器旧名字写的推送了。此前那样的一条：开关显示成「关」而消息照常发出、取消勾选也关不掉、按旧名存的自定义模板读不到而页面写着「默认模板」——三样都不报错。现在开关、模板、报告版式与默认模板页的「谁在用」一律先归到现名字再比。在界面上动过这一项之后，配置里那一条会跟着改成现名字（同一类通知不会变成两条）；只是打开页面看看不会动你的配置文件。

## [5.2.0] - 2026-09-07

### 新增

- 每场直播的礼物、盲盒、上舰、关注现在逐条记录在明细目录里，方便事后逐笔核对。
- 现在提供数据目录异地备份脚本与每日定时器模板，一条命令即可把直播数据同步到另一台机器。
- 下播报告的互动曲线现在多了一条「在线人数」折线（登录观众，哔哩哔哩高能榜口径）；消息里没有这个字段时，用高能榜人数代替。

### 变更

- 下播报告图不再另存副本，控制台回看时按当场数据现画；日志会记一行重画耗时。

### 修复

#### 控制台与向导

- 添加主播时查不到或主播已在列表里，「查询」按钮不再卡死，可直接改 uid 再查。
- 连接页里改了机器人地址、端口或 Token 后，「保存」会重新锁上，须再测试连接；测试出错时也不再留着上次的「可保存」状态。
- 初始化向导里改了机器人地址、端口或 Token 后，须重新「测试连接」才能下一步，不再带着旧的测试结论往下走；离开向导再回来时，没保存的机器人参数（含 Token）还在且须重新测通，没有未保存改动时 Token 格清空、已测通按这台机器上已经配好的来放行。
- 开关二次验证时若请求失败，开关会拨回原来的状态并说明原因，不再停在错误的一档；开关成功后，签发只读口令的表单立刻按新状态显示验证码栏。
- 初始化向导里改了主播 uid 或平台后，先前找到的主播会作废，须重新「找一下」才能下一步，不再把旧主播配进去。
- 配置项在文件里找不到上级块时，界面会明说没保存并指出是哪一项，不再显示保存成功。
- 配置项先写错再改对后，页面不再一直提示需要重启。
- 初始设置里设好控制台口令后，设置页「登录与安全」和顶栏会马上按已上锁来显示，不用整页刷新。
- 初始设置页加主播空着按下会先提示，不再直接提交。
- 哔哩哔哩页退出登录的确认改用控制台自己的确认框，不再弹浏览器原生对话框。
- 首页今日格明细里的平台现在显示人话名，多平台时第 6 个群起也能分清。
- 只读口令的三个签发/吊销接口失败时统一带机器可读的 reason 和人话 message：签发写不进磁盘时，页面显示原因，不再只显示 HTTP 500；吊销「没找到」后清单会自动重取。
- 设置页底部的配置文件路径还没确定时，点那一行不会把「未能确定」复制进剪贴板，会提示路径还没确定。
- 插件现在可以在控制台申报自己的整页入口。

#### 安全模式

- 安全模式里端口配成 0 或超过 65535 时改用默认端口；写成带引号的数字时也会接管同一个端口，不再退回默认或照原样去监听。
- 安全模式里备份保留份数写成带引号的数字或驼峰键（`configUi.backupKeep`）时也照配置走，不再退回默认份数；写超 1–100 时按边界生效并在日志提示，设置页显示的也是生效值；保存时按这份数清旧备份，不再固定只留 10 份，且与主程序用同一套带时间戳的备份，不再自写一份。

#### 下播报告与本场数据

- 下播报告里粉丝、粉丝团、大航海本场没有变化时写「持平」，不再写「+0」。
- 本场数据和下播报告里盲盒盈亏为零时显示「持平」，不再写「盈利 ¥0」；排行里为零的观众显示「¥0」，不再带加号。
- 本场数据图不再出现空的「直播时长」脚注。
- 周月统计不再列在线人数与高能用户。
- 新版礼物报文里的盲盒现在也认得：盒子和开出的礼物分开记，实扣按盒子算，不再一律当普通礼物。

#### 群内命令

- 查榜命令里盲盒盈亏为零的人现在显示「¥0」，不再带加号，与下播报告同一口径。
- Token 配错时，发消息的返回码与日志会直接说「Token 配置不正确」，不再报「未知异常」。

#### 推送与告警

- 升级时名单非空但没有能补发提示的会话（全部停用，或每条目标列表为空），日志现在写「无可补记」；「名单为空」只用于名单里一个人都没有的情况。
- 告警页「发一条测试」的结果里会连带保留「有改动还没保存、测的是已保存配置」的提醒，不再被结果盖掉。
- 推送平台名没对上时，返回码与日志会直接说「平台未注册」并列出已注册的名字，不再报「未知异常」。
- 升级时停用着的推送会话，启用后仍会收到一次用法提示；配置暂时读不到时，不再把「已经提示过」记下来而漏掉这次提示。
- 邮件告警选预设后状态即时更新；端口与预设不符时不再藏起端口栏。

#### 配置文件

- 备份定时器模板注释里的安装目录现在写成 `/opt/starbot`。
- 配置值里带单引号或双引号时，行尾说明不再被当成值的一部分回显或保存。
- 静音时段这类「时:分」取值保存后会带引号，重启后不再被当成数字而失效。
- 配置备份同一秒连存不再互相覆盖：第二份起按序号另存，两次保存前的内容都找得回来。

## [5.1.2] - 2026-09-06

### 修复

- 「直播报告」改成和「直播间数据」一样挑主播：这里配了的主播在播就出报告，不再要求这里配过报告推送；可以加主播名指定；不在播时明说「X 现在没在直播」。
- 私聊里发「菜单」不再列出只能在群里用的命令（开播@我、动态@我、启用命令等）；在私聊里发这些命令名也会回菜单，而不是没反应。

## [5.1.1] - 2026-09-05

### 变更

- 设置页：开关旁的「立即生效／重启生效」标记与开关对齐；二次验证开关与别处同一套；配置文件路径改为点一下复制。

### 修复

- 设置页「通行密钥」卡片登记之后刷新页面看不到已登记的设备，现在能看到了；登录不受影响。
- 升级之后，已经在用的群和好友不再收到那句「想用我，先 @ 我」的首次提示，只有新加进来的会话才发一次；这句提示可以在设置里关掉。
- 开播通知里的 @全体成员 在 QQ 里被当成了文字（额度不扣、群友收不到提醒），原因是文字和封面图并在同一条里；现在带 @全体成员 的消息文字先发、封面图紧跟一条，@全体恢复正常。

## [5.1.0] - 2026-09-05

### 新增

- **下播报告可以列出这位主播当前全部大航海**，不只本场新开通的。拉不到名单时这一段会写明，不会让整张报告失败。

- **有新版时控制台会提醒，不催**。启动后查一次、此后每天查一次发布仓（哪个源可在配置里改），
  有新版时侧栏版本号旁出一枚「新版 X」药丸，点开是三行说明、一句「控制台不做在线更新：
  到服务器上换 jar 重启就是了」、完整更新说明的链接，以及「知道了，这版先不提醒」——
  按版本记住，更新的版本出来还会再提醒；首页待办里同有一条软提醒。
  刚装好还没配完的机器不挂药丸。检查失败只记一行调试日志，不打扰。

- **控制台的「日志」页能用了**：一条按天翻的时间线，一行一件事——几点、发生了什么、
  成没成、没成为什么。可以只看有问题的那些，也可以按类型、主播、群/私聊筛，或者直接搜。
  <br>**筛完的画面能收藏、能贴给别人**：翻到哪一天、筛了什么都写在地址栏里
  （例如 `#/log/2026-09-01?only=problem`），打开就还原成同一张画面。
  一天里记录很多时先给最近的一批，页脚有「看更早的」接着往回翻。
  记录按配置里的保留天数留着，过期的整日自动删掉，这一点写在页脚。
  <br>**筛选改成按大类**：顶部一排药丸——推送、连接……点一下筛出一整块事情，
  不用再从一串事件类型里挑。只列出真有事件归属的那几类：一枚点下去必然是空的药丸，
  会被读成「这台机器没发生过这类事」。
  <br>写在地址栏里的是 `#/log?cat=PUSH`，旧地址里的 `type=` 照样认，
  此时那一项会作为一枚可摘掉的药丸显示出来——筛着却没有任何控件显示它，
  屏幕上就是「明明筛着、看起来却没筛」。
  <br>**另外多了「工程日志」子页**（右上角进）：直接看 NovaBot 自己那份日志文件的尾部，
  不必再登服务器 tail。可按错误/警告/信息/调试四档筛、搜关键字、选读多少行，
  异常的堆栈跟着它那一行走，不会被筛散。
  <br>**时间线上每一条都能跳到「在工程日志里看这一刻」**：跳过去会定位到那一分钟并高亮，
  那一分钟没有记录时定到最近的一行并直说——不说的话，屏幕上高亮着一行毫不相干的日志。
  <br>另外三件：**跟随最新**（视窗停在底部时每 3 秒把新写的行接上去，翻着旧行时不打扰）、
  **复制这一段**（复制屏幕上此刻显示的那些，与看到的一字不差）、
  **翻别的日子**（读那一天的日志文件，那一天没有文件就直说）。
  <br>**口令、令牌与 Cookie 在送到浏览器之前已经打码**——日志里出现凭据从来不是有意写的，
  而是某个异常把整串地址或整个请求头裹进了报错，而看日志的时候人往往正在把屏幕给别人看。
  <br>**机器人那头（NapCat 等）的日志不在这里**，页上只留一个打开它自己界面的入口。

- **设置页「登录与安全」组现在能改口令、开关二次验证、管通行密钥**，改完当场生效，
  不必重启。改口令要填现在的口令（`POST /config/api/auth/password/change`）；
  改完之后**别处已经登录的会话一并注销**，当前这一个留着。
  <br>二次验证从关到开，要先扫码绑定并输一次码；**从开到关，要输一次现在的验证码**
  （`POST /config/api/auth/totp/disable`）——关掉的是一整道防线，而这个动作只需要一次点击。
  关掉时那把密钥会一并清干净，下次重开是新的一把。
  <br>页底新增「重新跑一遍初始设置」（`POST /config/api/setup/rerun`），只落一个标记，
  **已经配好的东西一个都不会清掉**。

- **用启动令牌进来时，控制台顶部常驻一条提醒**：先改口令，再把令牌通道关掉。
  那条通道绕过了口令与二次验证，而它开着这件事**没有任何其它现象**——不常驻一条的话，
  这两步只会被忘掉，那道门就一直开着。忘记口令的人可以用它免旧口令重设一次
  （`POST /config/api/auth/password/reset`）；这条路**只认凭启动令牌换来的那把会话**。
  <br>登录状态接口 `/config/api/auth/state` 随之多给三项：这次是不是走令牌进来的
  （`operatorSession`）、二次验证此刻开着没有（`totpEnabled`）、
  这个来源还要被锁多少秒（`lockedSeconds`）。

- **主播数据有了自己的一套接口**。`/config/api/streamers` 列出配置里的每一位主播：昵称、头像、
  直播间号、当前状态，以及最近七天的场次数、总时长、最近一场是什么时候和一条按日的小折线；
  `/config/api/streamers/〈平台〉/〈uid〉` 是某一位的详情——累计场次与时长、最近一次采到的基础数据
  （粉丝数等）、逐场分页的场次表，以及按周或按月的趋势。
  <br>状态是四选一：**在播**／**未开播**／**只采集不推送**（配了主播但一条通知都没开：照常采集、
  出场次与报告，但一条消息都不发）／**已停用**。名单取自推送配置文件而不是内存——**停用的主播
  在内存里根本不存在**，问内存要名单的话，「停用」在界面上看起来就成了「删除」。
  <br>从配置里删掉、但还有历史场次的主播不会被悄悄漏掉：他们连同各自的场次数单独列出来，
  免得这张表看起来像是「一共就这些主播」。
  <br>**查主播时那张确认小卡上多了粉丝数**：uid 打错一位仍可能查到一位真实存在的人，
  昵称与头像未必看得出不对，而粉丝数往往差着数量级。取不到时这一项留空，不写成 0。

- **告警的三路通道各有一个「发一条测试」**（`POST /config/api/alert/test?channel=`）。
  按下去走的是这一路真正的发送路径，不是模拟——地址填错一个字母、发件授权码过期、
  机器人被踢出那个群，三者在配置文件上都长得完全正常，而它们的共同后果是
  **真出事的那天没有人收到告警**。
  <br>结果分四种，各自说清下一步：没有这一路、这一路还没配好、发不出去（带上原因）、
  发出去了。测试不过收敛闸门，也不入失败重投队列：你要的就是当场的结果。

- **报告版式有了配得到的清单，并且能先看一眼**。`/config/api/report/layout-options` 列出下播报告
  全部 13 个版式项（键、名称、说明、类型、默认值、取值范围）；`POST /config/api/report/preview`
  按给定的一套版式画一张示例报告回来，配的时候就能看到调完长什么样。
  <br>此前这些开关只能在 `datasource.json` 的 `params` 里手写，而**使用者根本无从知道有哪些键**；
  开关名字与默认值分散在两处各写一份，改了一处忘了另一处，界面上勾的与实际生效的就会对不上——
  这种不一致不会有任何报错，只会让人以为「配了没用」。现在两处合成一份。
  <br>预览用的是内置示例数据，**不会去请求哔哩哔哩的任何接口**，也不读任何一位真实主播的数据。
  <br>预览按这个群的「金额可见」来画，群里看不到金额的，预览也不画。

- **控制台支持通行密钥登录**（WebAuthn／Passkey）。在设置页登记之后，登录时按一下指纹、
  面容或手机上的确认即可，**不用再输动态验证码**——二次验证要补的是「口令会被撞库、被键盘记录、
  被肩窥」这件事，而通行密钥的私钥从头到尾没离开过你的设备，这台机器上只存得下一把开不了门的公钥。
  <br>设置页可以给每把钥匙起名字（最长 40 个字），列表里显示登记时间与上次使用时间，
  也可以随时删掉丢了的那台设备。登记与登录都要求来源地址、适用域名与签名计数器逐项对得上，
  且每个挑战串只认一次。
  <br>需要 https 或 localhost 才能用（浏览器的限制）。**通行密钥是按访问地址绑定的**：
  换一条地址（比如从内网 IP 改成域名）进来时，得再登记一把。

- **事件时间线**：静音丢弃、暂停丢弃、推送成功、推送失败、状态变化、登录失效这几类事情，
  现在会按日记进直播数据目录下的 `timeline/YYYY-MM-DD.jsonl`，每行一条，人能直接看。
  落在静音时段、或全局推送开关关着的时候，被丢掉的那一次会写明**是哪一道拦的、丢的是谁的通知、影响几个会话**——
  一场开播只记一条，不是每个群刷一行。推送成功与失败也各记一条，**带上这一次投递花了多久**：
  秒回的失败与超时的失败，下一步该查的东西完全不同。
  <br>此前要回答「刚才那条推了吗」「昨晚为什么没推」，只能去翻运行日志——
  而运行日志是给排障用的：它把这些事和上千行无关记录混在一起，
  被静音丢掉的那一条还只写在 INFO 级别，默认配置下人根本不知道该去哪儿找。
  <br>配套的查询接口在 `/config/api/timeline`，可按日期、只看问题、类型、主播、
  推送通道与关键词筛选，`/config/api/timeline/days` 列出有记录的日期与条数。
  <br>保留天数由 `starbot.core.timeline.retention-days` 决定，默认 14 天，过期的整日文件删掉；
  设为 0 或负数即不自动清理。**长期保存的场次数据在 `sessions.jsonl` 里，不受此项影响。**

- 侧栏显示当前版本号，取自构建信息；从源码直接跑（未经打包）时不显示。

- **机器人在哪些群里、加了哪些好友，现在问得出来了**。新增两支只读接口：
  `GET /config/api/onebot/targets?type=group|friend&q=` 列出可选的推送目标，
  `POST /config/api/onebot/targets/refresh` 强制重新去问一遍。
  <br>群带群号、群名、人数与「机器人是不是管理员」；好友带账号、昵称与备注。
  已经配成某位主播推送通道的那几条标记为已配置——这一栏按「推送平台 + 类型 + 号」比对，
  同号的群与好友、以及两个机器人上的同号群，都不会互相误标。
  <br>名单缓存十分钟，刷新按钮强制重取。**取不到时保留上一次的名单**并标明它是什么时候取的：
  清空会让界面显示「一个群都没有」，那与机器人真的一个群都没进长得一模一样。
  <br>「是不是管理员」查不出时留空而不是填「否」——那一栏是使用者据以决定要不要配
  @全体成员 的依据，报一个没查过的结论比留白更糟。

- **每个配置项都标明了生效时机**，字段表接口 `/config/api/schema` 逐项给出。
  眼下当场生效的是这几项：全局推送开关、静音时段的起止、
  以及告警接收人（QQ 的平台/类型/号码、Webhook 地址、收件邮箱）；其余一律需要重启。
  「命令开关」与「金额可见」同样是当场生效的，但它们本就不在 `application.yml` 里，
  按会话记在运行状态中，改法也不在设置页。
  <br>没有「默认值」这一说：新加的配置项标不出生效时机，构建就会红。猜错的代价是具体的——
  说成已生效而实际没有，人会以为功能坏了；说成要重启而实际不必，人会白重启一次。

### 变更

- **控制台「找一下」查主播，只向平台问一次**。昵称、直播间号与粉丝数出自平台同一个接口的同一份响应，此前查一位主播要打两趟——第二趟只为取粉丝数；现在一趟全带回来。

- **主播推送配置每次保存都留一份带时间的备份，保留份数可以在设置里改。**

- **关掉或绑定二次验证时，连续输错动态码会按登录同一套次数锁定。** 已经登录的会话不能靠反复试六位码来卸掉二次验证；锁定与登录共用，一边试满另一边也进不去。

- **通行密钥会拒绝自相矛盾的备份标志、登录数据后面多出来的字节，以及过大的密钥整数**。同一把钥匙同时登两次，只有一次能成功。

- **设置页一次保存改不了登录口令、二次验证和「忘记口令」的启动令牌通道**。口令和二次验证只在「登录与安全」里改：改口令要现在的口令，关掉二次验证要验证器上的码。这次带着登录身份改的，别处已经登录的会话会一并退出；没带登录身份时，别处的会话保持不动。启动令牌通道那个页面关得了、开不了，要开须改配置文件再重启。

- **动态验证码用过一次就不能再用**。同一枚码不能再登录，也不能拿去换只读口令。口令填错的那一次不会把码作废。

- **推送接口的门禁改按框架认路的方式认路**。此前门禁拿请求里的原始地址逐字对照已登记的推送路径，
  现在改用与路由同一套口径认路：同一条推送接口的不同写法一律按原路径受检，认不出的路径先拒再说。
  地址里写坏了认不出的部分同样拒收；互为包含的两条登记路径同时命中时，令牌与限流按更具体的那条记账，
  不随登记先后变。

- **长连接的坏包不再炸出异常，压缩包的放大也有了整批的账**。头部长度盖过整包长度的包
  此前会抛一个数组负长度的错，现在按坏包丢弃；一批压缩子包此前各算各的解压上限，
  各自不超限、合起来上 GB 的批次会被放过，现在整批共用一份预算，合计超限整批拒收。
  <br>通行密钥数据里的重复字典键——包括首值是空值的那种——现在按不合规范拒收；
  protobuf 消息里带垃圾高位的超长 varint 与超出上界的字段号也按坏消息停下，不再带着错位继续读。

- **加主播时粘贴的链接，交给对应平台自己认**。粘一条谁都认不出的链接会直说「不认识这条链接」，
  不再回一句像是没填东西的提示；装了不止一个平台时，粘成另一家的链接会提示先把平台选对，
  而不会拿那串数字去问当前选的平台——直播间号与 uid 是两套编号，问错了也可能查到一位真实存在的人。

- **首次启动不再因 OneBot Token 留空而报错**。出厂配置里 HTTP 与 Websocket 两把 Token 本来就留空，
  此前一启动就在日志里按 ERROR 说「请完善配置」——那不是故障，是全新安装的必经状态。
  现在改按 WARN 提醒，并把后果说清：配好之前推送不可用。

- **首页「今日」第三格的各群明细，只在有两种及以上推送平台时才在群号前写平台名**，两个平台下同一个群号不会看成同一行。QQ 通道写成「QQ」；没有对照的通道仍用接口给的标识。只有一种平台时只写群号。没有明细时这一格与旁边两格一样点不开。已用与上限之间的斜线改成与别处相同的半角。

- **登录页口令框和设置页改口令三栏加了「显示／隐藏」**。默认藏着，点一下揭开，再点藏回去。四处是同一个小按钮，不会一处揭开、另一处还写着另一套字。口令框占满余下的宽度，按钮只按字的宽度，不再把输入框挤成小方块。登录被锁定时，「显示」也会一起禁用。

- **加主播时可以填直播间号**。初始设置第 4 步和推送页「找一下」除了 uid、个人空间链接，也收纯数字短号和直播间链接。短号会先按 uid 查，查不到再按房间号查一次；链接已经写明是直播间时只查房间号。找不到时，填的是直播间链接会按直播间号提示，不再一律写成 uid；不会反复重试。

- **危险操作改用控制台自己的确认框**。设置页改到危险档、放弃未保存的改动，推送页恢复默认、删除主播、移除通道，通道页「一键恢复」和「清空订阅名单」，注销所有设备、重走初始设置、不登录继续、不加主播继续、删通行密钥、吊销口令，不再弹出浏览器自带的那一句；新的一层有标题、一句后果、取消和醒目的确认。按 Esc 可以取消，关掉以后焦点回到刚才那颗按钮。

- **首页「今日」补上第三格「@全体成员 已用」**，以及一条「QQ 告警有死角，建议再配 Webhook」的软待办。
  第三格读的是今天账号维度已经 @ 了几次、上限是多少；不限额的机器只显示已用、不画分母。
  点开是各群明细，按已用从多到少，最多五行。
  Webhook 与邮件都没配时（QQ 配没配都一样），首页待办会出现「QQ 告警有死角，建议再配 Webhook」，点它滚到设置页告警那一组；Webhook 或邮件配好其中一路即不再出现。

- **「主播」页重做**：上来是一份列表——每位一行，写着现在是什么状态（在播／未开播／只采集，不推送／已停用）、
  最近几天播了几场多久、最近一场是什么时候，右边一条按日的小折线；顶上一张汇总卡。
  点进去是这位主播的详情，分概况、场次、趋势三页；**场次表里点任意一行，看的就是那一场当时发出去的报告图**——
  图过期了会现画一张，画不出来时说清是没开下播报告还是这一场早于「每场留明细」那个功能。
  <br>没开累计存储的机器，概况顶上有一条小横条说明群里只能查本场，与首页那条待办同一个判定。
  从推送配置里删掉、但归档里还有场次的主播单列一段，点名列出是谁——不这样的话，
  那些场次会成为「列表上找不到主人、总数里却算着」的数据。
  <br>场次表里两种采集缺口（程序停机、这个直播间断线）**分开说、不相加**：两段必然重叠，加起来是重复计数。
  「人气峰」一栏对早于该功能的老场次显示「—」而不是 0——**那是不知道，不是零**。
  此前这一列的位置是空的——峰值本来只在报告图上，而列表要在几十场里横向比一眼看出「哪一场最热」。
  下播归档时把每条曲线各自的**峰值与出现时刻**一并算好存进场次记录，列表与详情直接读这两个数，不去翻那份几百 KB 的明细；
  峰值与时刻**同源同一次算出**，分两趟会得出两个数各自都对、合起来是假的结果。
  <br>原先这一页上那张「监听中的主播」表与那块按周期堆总数的运营分析已并进来：
  前者只说此刻在盯着谁、不说播得怎么样，后者只有总数、点不进任何一场。

- **消息模板改成搭积木，右边同时画出这条消息在 QQ 里长什么样**。此前这一格是一行输入框，
  里面混着 `{uname}`、`{cover}` 这样的花括号，还有一个谁也说不清的 `{next}`——
  它其实是「从这里断开，发成两条消息」，写错一个字母就变成消息正文里的一串字符。
  <br>现在**一张卡就是一条消息**：卡里的文字与整行的图片各是一块药丸，抓住拖柄就能换顺序，
  卡左边的 ⊖ 删掉一条、列表底下的 ⊕ 再加一条，`{next}` 不必再出现在任何人眼前。
  能填什么占位符不用记：调色板上列着这类通知支持的每一个，点一下落到光标处。
  **@ 那一块自带三选一**——@订阅了的人、@全体成员、有权限就 @全体成员否则 @订阅的人。
  <br>右边是实时的 QQ 气泡预览，图片画成占位块，几张卡就是几个气泡；机器人在这个群不是管理员时，
  预览里的 @全体成员会被划掉并注明「会被自动摘掉」——这件事以前要发出去才知道。
  报告那一段同理：版式开关一勾一撤，右边直接出图。

- **默认模板现在改得动了，而且改一次，所有还用着默认的通道一起变**。
  左树顶上的「默认模板」用的是同一个编辑器，页面下方列着谁在用默认、谁已经自己改过。
  某个通道要单独写，先按「改为自定义」（从当前默认复制一份再改，不会拿到空白），
  想跟回大队按「恢复默认」——这一下会问一句再执行。

- **保存机器人连接时留空的字段不再写成空值**，而是从配置文件里去掉；配置文件里列表元素的空字段同样去掉、后面的项顶上。
  空值和「没写」在程序看来本该一回事，写成空值却可能让下次启动读不进去。Redis 地址清空即整行去掉的规矩不变（4.1.0 起就是）。
  一项的字段都清空后，这一项整条去掉，列表回到空表，不会留下一个空壳。
  机器人连接这类对象列表在生成配置文件时按字段写出，读得回来仍是一组字段。

- **初始设置第 4 步加完主播后可以直接去主播页**看这位主播的概况与场次。主播还没加进去时不会出现这个入口：查到人、还没保存的时候只有小卡和「推到哪」；加进去之后，完成页才有去主播页的入口。

- **QQ 推送页改成「主播 → 通道」两级**：左边先挑一位主播，看它推到哪几个通道；
  点开某个通道，右边就是这个通道自己的四段——推什么（四种通知的开关与这个通道最近几条推送）、
  消息长什么样、报告长什么样、本群设置。原先那张所有推送混在一起的记录表不再有：
  「我这个群到底收没收到」以前要在一整张表里翻，现在就在这个通道下面。
  <br>「本群设置」默认收起，摊开是四行——金额可见、命令、提醒订阅、@全体成员 的当前状态。
  命令那一行按组开关，可以细调到单条；**群里被管理员关掉的命令在这里点名列出，一键能全开回来**。
  这台机器没开累计数据时，带「总」字的两条命令在群里本来就不出现，这里会一并标灰、
  摘要里的条数也跟着少两条——屏幕上的数与群里数出来的对得上。
  <br>添加主播、添加通道都从机器人自己已经加入的群、已经加的好友里挑，没有手填号码的格子。

- **累计数据存储配好就能用，不用重启；中途挂了会自己降级、连回来自己恢复**。
  此前「有没有累计数据」是启动那一刻定死的：在控制台填好 Redis 地址、保存成功、界面说已生效，
  而群里那两条「总」字命令要等下次重启才出现，中间这段时间没有任何地方说得清为什么。
  反过来 Redis 半夜挂了，程序会一直宣称累计数据可用，**查出来却是一片 0——
  而 0 与「真的没人送礼」长得一模一样**。
  <br>现在这件事分两问：**配了没有**（地址填没填）与**此刻连不连得上**（每隔几秒探一次），
  两问都过才算可用。地址、端口、密码、库号四项因此改成**即时生效**，保存完当场换一个后端；
  菜单、首页那条待办、`/api/status` 的 `totalDataAvailable` 与「我的总数据」等命令
  读的都是这同一个判定，不会各说各话。
  此前四项连接参数里只有库号改完要重启一次，现在设置页「采集」组里的 `spring.data.redis.database`
  与另外三项一样，保存即落到运行中的程序上。
  <br>Redis 的键一个没动，老数据照旧读得到。

- **健康自检把「没配累计存储」与「配了却连不上」分开说**。此前两者都只显示一句
  「仅本场数据」——而前者是正常的部署形态，后者是真出了事，处理方式完全不同。
  现在没配时报正常并写清去哪儿填、填完不用重启；连不上时报**降级**，
  写出连的是哪个地址、以及连回来会自动恢复。

- **每一场直播的原始数据整份留下来了**。下播时在直播数据目录的 `details/` 下按场建一个目录
  （平台、uid 与开播时刻，与报告图同级），里面两份：`detail.json` 是六条曲线的**逐分钟全量**、
  各榜的**全量名次**（不是报告图上画的前几名）、词频表、高能时刻、标题变更与采集缺口；
  `danmu.jsonl` 是**弹幕原文逐条**——时刻、uid、昵称、原文、类型（弹幕／表情／付费留言）。
  此前这些只活在内存里，画完那张报告图就随下一次开播清零：不是算不出来，是当时就没留，
  于是「上个月那场第 40 名是谁」「那句刷屏的原话是什么」这类问题事后一概答不了。
  <br>**报告图随之降级成缓存**：点开一场时没有图就**从明细重画一张**（与出图那次同参数、同尺寸），
  因此图可以过期删掉而不丢东西——默认留 30 天（`live.report-cache-days`）。
  明细本身**默认永久保留**（`live.detail-retention-days` 为 0），它是唯一一份原始数据，
  删了连报告都重画不出来；要设上限就改这一项，改动需重启生效。
  <br>盘上占多少：一场四小时、两万条弹幕的直播实测约 2.5 MB，其中弹幕原文占 2.2 MB、
  明细本体 263 KB（排行 222 KB、曲线 30 KB、词频 10 KB）。**这个版本之前的场次没有明细**，
  点开会明说这一场没有留档，而不是给一张空图。
  <br>⚠️ 重画出来的报告与当时发出去的那张有两处必然不同：**封面与头像取不到**（那是当时现拉的图，
  没有留在明细里），画的是占位块；**粉丝数、粉丝牌数、大航海数**同理留空，
  「本场变化」那一块因此整块不画——拿开播那一刻的快照减自己会得出「本场 +0」，那是一句假话。
  曲线、排行、词频、高能、标题这些**本场数据一律照旧**。
  <br>同一场推给几个通道也只留一份，且留的是**带金额的那一份**——这份留档是给这台机器的主人看的，
  他本就看得到金额。取回接口是 `/config/api/streamers/〈平台〉/〈uid〉/sessions/〈开播时刻〉/report`。
  没出过图的场次（没开下播报告、或当时画失败改发了文字版）点开会明说没有图，不会给一张空图。
  此前报告只存在于发出去的那条消息里，想再看一眼只能翻聊天记录，而聊天记录会被清理。

- **初始设置改成五步引导**：刚装好的机器打开控制台，先走五步——给控制台上把锁、
  连上 QQ 机器人、登录直播平台、加第一位主播并选好推到哪、发一条试试。
  每一步做完当场落盘，中途可以回上一步改，也可以点「稍后再说」先进控制台，
  首页会一直挂着「5 步里完成了 N 步」。
  <br>**上锁这一步不能跳过**：没上锁的控制台，任何能连上这台机器的人都进得来，
  而后面四步配好的东西全在它后面。想同时登记一把通行密钥可以，但要先设口令——
  没有口令时通行密钥签出来的会话打不开任何门。
  <br>不登录直播平台、不加主播这两条路都留着，但各要过一次后果确认：前者采到的数据是
  残缺的而界面一切正常，后者会让这台机器起来什么都不做，两件事都要过很久才发现。
  <br>推送目标只能从机器人自己给的群与好友名单里挑，没有手填号码的格子。
  最后一步会摊开「已替你定好的初始值」，每一项都从这台机器的现值算出来，可以逐条去改。
  <br>走完之后想再走一遍，去「设置 → 登录与安全」里的「重新跑一遍初始设置」，
  已经配好的东西不会被清掉。

- **连上 QQ 机器人之后不必重启**：连接参数测通并存下来的那一刻，这条连接就接上了，
  接着往下走第 3 步即可——以前存完要重启一次，否则第 4、5 步面对的是一个什么都没连上的
  程序：群和好友的名单是空的、消息发不出去，而屏幕上没有任何东西解释为什么。
  <br>改了地址、端口或 Token 再存一次同样当场生效：旧连接会断掉，新的接上，
  连按几次保存也只会有一条连接。存下来之后界面回的那句话说的是**此刻通没通**
  （实现版本与登录着哪个 QQ 号），而不再是「已保存，重启后生效」。
  <br>一台还没有 `application.yml` 的全新机器上，这一步会顺带把机器人那一条完整地写进
  配置文件——此前它会报一句「保存失败」，而那句话指不到任何可改的地方。

- **不必先写配置文件就能起**：发行包里不再放 `application.yml` 与 `datasource.json`。
  解压、起程序、打开控制台，程序按代码里的默认值跑起来，控制台会先把你领到初始设置页，
  而不是摆一张「0 个主播、0 条推送」的空首页。刚装好的机器同意使用协议之后，会直接带到初始设置，不再先落在首页。
  <br>**配置文件由程序自己写出来**：你在控制台里第一次保存设置时，它会写出一份完整的
  `application.yml`——键、说明与默认值都从代码里现取，因此**不会缺项**，
  此后这份文件归你所有，手改过的地方不会被盖掉。`datasource.json` 同理，
  在你加第一位主播时生成。
  <br>包里改放一份 `application.example.yml`，只作示例：想在起程序之前就把配置写好时
  照着抄，或看一眼列表、嵌套这类写法。它不保证列全所有配置项，全集看设置页。
  <br>⚠️ **`install.sh --port` 在全新安装上不再生效**（那时还没有配置文件可改），
  端口仍是默认的 7827；要换端口去控制台的设置页，或用 `JAVA_OPTS="-Dserver.port=端口"` 起。
  升级安装不受影响。

- **设下控制台口令后当场生效**：以前设了口令要重启才认，而那段时间里界面说着「已上锁」、
  门却还开着。现在设下第一把口令的那一刻就切成登录表单，**启动令牌随之作废**——
  终端不再打印令牌，地址栏里那一串也不再管用。没设口令时令牌一切照旧。
  刚装好设下第一把口令时，这一趟继续往下走，不会被踢回登录页；带着地址进来登录的，登录完停在要去的那一页，而不是一律回首页。
  <br>「是否要求二次验证」这一项同样改成立即生效，设置页上那个开关本来就是当场生效的，
  而配置说明此前写着「需重启」，同一件事有了两种说法。

- **「连接」页改成三张卡**：哔哩哔哩账号、QQ 机器人、外部面板各占一张。每张卡把
  「连没连上、卡在哪一步、下一步该点哪」写在同一处，不用再在几块之间来回对照着猜；
  首页链路的那三段现在点一下直接落到对应的卡上，而不是只把你送到页首。
  <br>哔哩哔哩那张卡会写明凭据到期时间与自动续期开没开——续期关着、或这次登录没拿到
  刷新口令时，到期那天需要重新扫码，这件事以前要等到期之后才看得出来。
  QQ 机器人那张卡在掉线时会写出还有多少条消息卡在队列里、丢了多少条。
  <br>原先夹在哔哩哔哩那一块里的「监听中的主播」表挪到了「主播」页，内容不变。
  <br>哔哩哔哩连接卡登录后同时显示昵称和 uid，取不到昵称时仍只显示 uid。未登录且卡上已经有二维码时，健康自检改为请扫描连接卡上的码；卡上还没有二维码时，仍提示去看启动日志。

- **「发一条试试」改为从名单里挑**：发给谁从机器人自己知道的群与好友里选，不再手填号码。
  号码填错一位不会有任何报错，消息只是发去了别处，而看起来像是机器人坏了。
  名单可随时刷新，机器人没连上时这一块会直接说明原因。

- **开播与动态通知从两条并成一条**：文字与封面（动态图）现在发在同一条消息里。
  分成两条原是为了保住文字——图片拉不到会让整条消息发送失败，而不是只丢图。
  现在这件事由发送侧兜底：含图消息失败时会剥掉图片段重发一次纯文字，
  于是那个分条只剩下「每次开播响两声」的代价。
  <br>**你自己改过的模板一个字都不会动。** 只有存着的模板与旧的默认模板**一字不差**时
  才跟着新默认走——差一个空格都算你改过。装了旧版本、从没动过通知模板的，
  升级后自动变成一条；反过来，如果你就是想要分两条，在模板里保留 `{next}` 即可。
  <br>**@全体成员 那一下也并进同一条通知里**。「@全体成员」与「@全体成员，不行就 @订阅的人」
  这两档此前是「@ 一条、正文另一条」，于是群里仍旧响两声——而通知本身刚刚才合成一条。
  现在 @ 与正文同处一行。
  <br>**@ 不出去的那一次跟着变干净**：机器人不是管理员、或今天的 @全体成员 额度用尽时，
  以前是那条只剩 @ 的消息整条不发（看起来像少了点什么），现在只是正文前面少了那一截；
  选了「不行就 @订阅的人」的，订阅名单就地顶在正文前面。你自己在模板里写的
  `{at=all}` 与 `{at}` 照旧生效，位置也照旧。

- **命令的两处口径**：私聊里不再讲「本群」——数据查询类命令在已配推送的好友会话里一样能用，
  那里没有「本群」这回事，照着去群里找配置只会白找一趟。说不出主播的那两句与
  「这条命令被关掉了」那一句都改口说「这里」。
  <br>**「启用命令／禁用命令」只认菜单里列得出的命令**：这台机器没开的（例如没配累计数据
  存储时的「直播间总数据」）回一句「在本机没开」；在这个会话里用不上的（例如本群开播通知
  配成了「@全体成员」时的「开播@我」）回一句「在本群用不上」。
  两者本就不在菜单里，关掉它们只会在设置里留下一条谁也验证不了的记录；
  把能力配好、或把本会话的配置改回来之后，它们自己就回来了。

- **「忘记口令」的启动令牌通道改为默认关闭**（`config-ui.auth.operator-token`）。
  开着它时，设了登录口令的实例仍会在启动日志里打印一个带令牌的地址，
  用它可以绕过口令与二次验证直接进面板——而令牌走地址栏，会进反向代理的访问日志。
  <br>**忘记口令时仍然进得去**：把这一项改成 `true` 重启，启动日志会打印那个地址，
  进去改完口令再改回 `false`。关闭时启动日志会写清这一步，不会只说一句「已关闭」。
  没设登录口令的实例不受影响，访问方式照旧。

- **图片底部的署名行带上仓库地址了**：`NovaBot v〈版本〉 · github.com/FrostNovaOrg/NovaBot`。
  此前只写产品名，顾虑是「仓库搬家后这条地址就指向不存在的地方，而收到图的人无从知道它已经失效」；
  该顾虑的前提是没人为这条地址负责，而这就是本项目宣告的发布仓。
  <br>反过来，一张不写出处的图，收到的人拿它没办法：既问不出这是什么程序，也找不到它从哪来。
  地址与整行同为浅灰、不带 `https://` 前缀——图里的字点不动，写成链接的样子只会让人去点它。
  <br>开头不写「Running on」这两个不添信息的词；字号取比正文小一档的脚注档——
  这一行是出处不是内容，正文档的宽度在 900 宽的图上几乎顶到左边。

- **动态图没送到时也计入下播报告那一行**。报告里「本场有 N 条推送的图片未送达」此前只数开播封面
  那一路，动态图丢了不进账——而一个少数了一路的 N，和一个数对了的 N，在报告上长得一样。

- **一个会话里配了多位主播时，命令不再要求你把名字打对**。`直播间数据`、`数据排行榜`、
  `开播@我` 这类命令没带主播时，此前会把名字列一遍让人重发一次，而重发时还得打对。
  现在回一份带序号的清单：正在直播的排最前、今天播过的其次，
  其余折成「还有 N 位，回「全部」展开」，**2 分钟内回一个序号就照办**。
  <br>**只有一位在播时不问，直接办那一位**，回复里会先有一行「本次用的是：〈某某〉」——
  图里虽然也有主播名，但看图的人未必会去核对，而这种没点名的一次正是最该被核对的。
  只配了一位主播的会话照旧不必带名字。
  <br>**不会替你记上次选的是谁**：每次没点名都照样问一遍。记着的那位与你这次想看的那位
  不是同一位时，回来的是另一位主播的数据，而那张图看起来完全正常，你也无从知道
  它正替你记着什么。
  <br>清单的序号在问出去的那一刻就定死：追问与回答之间某位主播开了播，
  重排出来的第 2 位就换了人，而照着旧清单回的那个 2 会换来另一位主播的数据——
  那张图看起来完全正常。带主播名（可以只打一部分）或 uid 的用法照旧，一次到位。

- **那条记性撤掉之后，它已经写下的数据现在也一并清走**。早期版本替每个会话记的
  「上次选的主播」存在 `state.json` 里，记性本身撤了，可已写下的那一段不会自己消失——
  写它的代码走了，数据还留在原地。现在启动时见到就删，并在日志里记一行；
  其余内容（命令开关、订阅名单等）一个不动。

- **机器人向某个会话成功推出第一条通知之后，会紧跟一句用法提示**：
  群里是「想用我，先 @ 我。试试 @ 我并发「菜单」」，私聊里是「想用我，直接发「菜单」」。
  <br>每个会话只发一次。此前群里的人只看得见通知，看不见「还能问它点什么」——
  命令表在控制台里，而看得见控制台的只有机器人的主人。
  <br>推送失败时不发（群里根本没见过它说话）；静音时段整条通知本就被丢弃，
  自然也不发，且不算提示过，静音过去之后的第一条照样带上。
  控制台的「发送测试消息」不带提示：那个按钮点一次就该只出去一条。

- **群里的命令改成「@ 机器人」触发**，@ 后面接命令名与参数；没 @ 的消息一概不理，
  没配过推送的群里 @ 了也不理。私聊仍是直接发命令名。
  <br>随之撤掉「命令前缀」设置（`starbot.core.command.prefix`）：它原是给「一个群里好几个机器人
  互相撞词」准备的，而 @ 谁本身就已经把这件事说清楚了，再让人多记一个符号只是多一层门槛。
  老配置文件里留着这一行不影响启动，程序会在起动时提示一句它已不起作用。
  <br>**认不出的命令不再沉默**：已经 @ 过机器人（或是在私聊里），说明这话就是对它说的，
  此时回一份菜单，顶行写清在当前会话该怎么发命令。**被本群关掉的命令也会回一句**
  「本群已关闭「〈命令〉」命令」，此前是一声不吭，看起来和机器人坏了没有区别。
  <br>回菜单、回「已关闭」、回「没权限」与执行命令**共用同一份 3 秒冷却**——
  分开计算的话，连发无效消息就能绕过它。

- **账号绑定停用，命令由 19 条减到 14 条**。`绑定` / `确认绑定` / `解绑` / `我的数据` /
  `我的总数据` 不再是命令：发过去按「认不出的命令」处理，也就是回一份菜单，菜单里不再列它们。
  控制台「群与成员」页的账号绑定块一并撤走，解绑接口回 410。
  <br>撤它的理由在绑定这件事本身：没有接口能证明某个 QQ 号与某个哔哩哔哩账号属于同一人，
  绑定只核对昵称防打错，而个人数据查询正建立在这条对应关系上——一条打错的对应关系，
  查出来的是别人的互动数据。
  <br>**已有的绑定记录原样留在 `state.json` 里**，只是不再显示、不再响应：
  删掉它们是个不可逆的决定，而这件事随时可能改回来。

- **数据查询类命令在私聊里也能用了**：`直播间数据`、`直播间总数据`、`数据排行榜`、
  `总数据排行榜`、`直播报告` 与 `菜单` 不再仅限群聊，在**已配置推送的好友会话**里直接发命令名即可。
  「@我」订阅与命令开关仍仅限群聊——@ 在私聊里没有对象，而开关改的是全群的可用功能。
  <br>此前 `菜单` 自己也被划进「仅限群聊」，于是私聊里把名字发对了反而没动静，发错了倒能收到菜单。

- **没配累计存储时，「总」字命令不再出现在菜单里**。`直播间总数据` 与 `总数据排行榜` 依赖外部存储，
  没配时列出来只是请人白跑一趟。仍然发过来的会收到一句
  「本机没开累计数据，只能查本场。发「直播间数据」看本场。」，同时在日志里记一行——
  菜单已经不列了还有人发，说明有人照着旧习惯在用，而这件事在机器主人那边没有别的痕迹。
  <br>配好累计存储后自动恢复，不用重启。`/config/api/state` 新增 `totalDataAvailable`，
  每条命令另带 `available`。

- **适配器不再自己回复裸 `status`**。此前 OneBot 适配器收到内容恰为 `status` 的消息会直接回一句版本号，
  这条路绕过了核心统一把关的全部约束：不看有没有 @、不看这个会话配没配推送、不吃冷却、也不进菜单，
  在没配过推送的群里同样会应答。版本号在控制台侧栏可以看到，不另立命令。

- **控制台不再提供配置文件编辑**。设置页里「直接编辑配置文件」那一整块（编辑框、历史备份下拉、
  回滚按钮）与推送页的「编辑原始 JSON」都已撤走，对应的接口一并移除。
  <br>撤它的理由是失败方向：在控制台上改整份 `application.yml`，改坏了程序下次起不来，
  而控制台随之一同挂掉——远程部署时人就被挡在门外，只能登进服务器手工改。
  <br>要直接改文件请到服务器上改，**设置页底部现在显示配置文件的完整路径与「复制路径」**。
  改坏之后的正门仍是安全模式：程序起不来时它会接管端口，提供一个只能改配置文件的极简页面。
  <br>每次保存前自动备份配置文件这件事**照旧**，只是不再有界面入口去列出与回滚，
  备份文件就在配置文件旁边。

- **底部改成统一的改动条**：所有页面共用一条，显示「N 处改动」并带保存与放弃。
  N 是现算的——设置页数改过的字段，推送页与上次载入时的内容逐条比，改回原样即不再计入。

- **弹幕词云改由自己排版**，不再依赖第三方词云库（依赖表里少了一项，没有添新的）。
  <br>换掉它是为了两件它给不了的事：**同一场直播的词云现在是固定的**——此前每画一次都随机撒点，
  同一份报告推给两个群、或推送失败重发一次，收到的是两张不一样的图，看的人会以为是两场直播；
  **词与词之间至少隔 8 像素、且随字号加宽**——按相邻两词中较大的那个折算，
  56 像素的大词旁边留 20 像素上下，不再挤得像连着读；**词与边框之间留 8 像素**，此前词会贴着框边压上圆角。
  <br>版式也跟着定下来：**一律横排**，字号按词频分布在 18～56 之间，从中心向外沿椭圆铺开直到四个角，
  排不满则整体放大——**放大之后也不超过 56 像素**，并且**最热的那个词最多比第二名大三成**。
  此前这两条都没有：一句刷屏弹幕能把一个词顶成 99 像素，压着旁边一片小字；
  颜色按名次分六档。**同一场弹幕能上图的词比原先多出一半还多**（同一批语料实测 467 → 726 个）。
  **词数从 8 个降到 5 个就出图**——冷清场次此前连词云一起省掉了；
  这类场次的词云不再靠把字放到极大去撑满整块，词少就是画得小一些。
  <br>入词云的词也多筛了一道：**一个字的词不收**（此前只有基本区的汉字被挡住），
  **同一个英文词大小写不同的不再各占一格**——`dog` 与 `Dog` 现在并成一个词。

- **下播报告的采集缺口改为逐段标注**：互动曲线上没采到的那几段不再画成贴地的面积，
  改画浅色斜纹并在两侧压一条细线；**真的没人说话的那几分钟仍画一条贴着底线的细面积**，
  两者一眼分得出。此前两种情形在图上完全一样，读的人无从分辨
  「这几分钟没人说话」与「这几分钟我们没在听」——而这两件事对主播的含义正好相反。
  <br>时长后面那句话随之改成「采集缺口 共 X：维护 a／重启 b／断流 c」，
  成因只有一种时写成「采集缺口 共 X·维护」。**成因是推出来的**：上一个进程正常退出记维护，
  崩溃或被强杀记重启，某个直播间自己断线记断流；推不出来时如实写「原因未定」，不挑一个像的填上。
  <br>停机与断线两段必然重叠（程序停着的时候每个房间当然都是断的），
  分栏时让停机优先占位，**各栏时长加起来正好等于总时长**，不会把同一秒数两遍。

- **保存配置之后不再一律说「重启后生效」**。有几项是当场就管用的，此前也被说成要重启，
  于是有人为了让「暂停推送」生效而重启了整个程序——而重启会把正在采集的场次打断。
  <br>现在保存后回的是「已保存 N 项，已生效」或「已保存 N 项，其中 M 项需重启」，
  M 由每一项自己声明的生效时机算出来。改动条上跟着显示「其中 M 处需重启生效」，
  **这句提示一直挂到程序真的重启为止**，刷新页面或换台机器打开都还在。

- **开播与动态通知的「@ 谁」改成三选一**，两类各选各的，配在每个通道对应通知的推送参数里：
  键名 `at_mode`，取值 `subscribers`（只 @ 订阅了提醒的人）、`all`（@全体成员）、
  `all_or_subscribers`（@全体成员，@ 不出去时改 @ 订阅的人）。
  <br>**选了第三档时**，机器人不是群管理员、或当天 @全体成员 的额度用完了的那一次，
  原本放 @全体成员 的位置换成订阅名单，通知照发；选第二档时那一次就不 @ 人了。
  两种情形都在时间线上记一条「未 @ 全体」，写明是权限还是额度——此前这件事只在日志里，
  而「说好的 @全体成员 呢」是配置的人要知道的。**订阅名单三档都不会被删掉。**
  <br>**本群这类通知配成 @全体成员 时**，`开播@我`／`取消开播@我`／`开播@名单`（动态同理）
  不再出现在菜单里，发过来会收到一句「本群开播通知会 @全体成员，不用单独订阅」——
  列一条做什么都不会有效果的命令，等于请人白跑一趟。同一个群里配了好几位主播时，
  **要每一位都选了 @全体成员 才藏**：只要还有一位是按名单 @ 的，订阅就仍然有用。
  第三档下命令照常，菜单那一行后面补一句说明。
  <br>默认模板里的 `{at}` 随之去掉，@ 块改由模式生成；**使用者自己改过的模板一个字不动**，
  里面的 `{at}` 与 `{at=all}` 照旧生效，模板里已经写了的那一处不会再补第二遍。
  旧配置里的 `at_all` 仍当「@全体成员」用，直到在界面上选过一次为止。
  @全体成员 的权限与额度规则一如既往（账号每天 10 次、单群每天 20 次）。

- **控制台首页改成回答一个问题：现在好不好**。自上而下是出事时那条横条、一张链路图、
  要人动手的待办、现在谁在播与今天推了多少、六项探针、以及「今天发生了什么」的最近几条。
  <br>链路图把整条路画成三段——直播平台一侧、本机、机器人一侧——每段的灯取该段探针里最重的那一个。
  哪个探针算哪一段由探针自己声明，不是界面按名字认的：按名字认的话，探针改个显示名，
  那一段就从此永远绿着。点「本机」会滚到下面的健康自检——此前这一站在连接页上没有对应的卡，点下去地址栏变了、屏幕没动。**静音时段与暂停推送期间，机器人那一段是灰的而不是绿的**：
  链路确实通着，但这一段此刻不走消息，绿灯会让人以为推送正常发出去了。
  <br>顶部横条一次只出一条：已暂停、静音中、还没配置完、机器人连不上、直播平台有异常。
  待办只放「不动手就一直不好」的事：初始设置没走完、控制台还没上锁、机器人要重新登录、
  累计数据没开。会自己恢复的异常不进待办，它们在探针那一栏里逐条列着。
  <br>`/config/api/status` 随之补上四项：静音时段（此刻在不在、区间是什么）、此刻在播的主播、
  今日推送成功与失败条数（按日历上的今天数，取自事件时间线，重启不归零）、
  以及控制台上没上锁、累计存储开没开两位。
  <br>原先嵌在首页顶部的四步「首次配置向导」撤走，它做的事归初始设置页；机器人连接表单与
  登录二维码仍在「连接」页上。堆内存、线程数与监听主播数挪到侧栏的「运行」三行，
  「最近推送」那张表挪到「QQ 推送」页。

- **设置页改成按你要办的事分组**，不再按配置键的前缀自动切成二十来组。常用的六组在上面——
  推送、告警、命令与权限、采集、报告外观、登录与安全；日志与调试、服务两组折在页底的
  「高级 · 工程用」里。组名之下写着这一组管什么，**整组都要重启的组把牌子挂在组标题上**，
  混着的组仍逐项标。
  <br>顶上多了一个搜索框，**标题、说明与键名一起匹配**——你记得的是「静音」，
  运维手里拿到的是 `starbot.core.push.quiet-start`，两种都该搜得到。旁边两个开关：
  「显示键名」把键名附在每项末尾，「只看改过的」只留下值与默认值不同的那些。
  搜索或筛选命中折起来的地方时，那一块会自己展开。搜索框下面一行是组目录，点一下滚到那一组。
  <br>**改过的项旁边打一个「已改」的记号**，并给出「默认：X · 恢复默认」——
  按一下就填回出厂值。口令、令牌这类不给这个按钮：它们的默认值多半是空，
  而按下去等于把口令清掉，那是个该显式做出的决定。
  <br>**四个改错了后果不小的项加了围栏**：「忘记口令」启动令牌通道、监听地址改成 `0.0.0.0`、
  外部程序触发、匿名模式。这四项行左有一道警示边，改到危险的那一档时先问一句后果，
  点取消就把控件退回原样、也不计入改动条。
  <br>监听地址 `server.address` 与服务端口 `server.port` **这次才出现在界面上**：
  「把接口暴露到网络」此前只能在服务器上改文件完成，控制台连提醒一句的机会都没有。
  <br>字段表接口 `/config/api/schema` 随之逐项给出所属的组与组内序号；
  一条分组规则也匹配不上的配置项**不会被塞进某个「其他」组**，而是单列出来——
  塞进去之后界面照样显示得好好的，于是「这一项还没人给它安排位置」就再也看不见了。

- **告警那一组改成两行加三张卡**。留在行里的只有「启用告警」与「同一问题最短间隔」，
  其余按路收成 QQ、Webhook、邮件三张卡，每张卡右上角写着这一路配好了没有。
  <br>**QQ 告警的接收人改从机器人已经在的群、已经加的好友里挑，没有手填号码的格子**——
  填错一位数不会有任何报错，只是告警发去了别处，而这件事只有真出事那天才会被发现。
  <br>Webhook 与邮件各带几个预设：选 Bark、Server 酱、PushDeer 或 QQ 邮箱、163、Gmail，
  提交方式、字段名、服务器与端口会一起填好，选「自定义」才露出那几栏。

- **登录页重排：通行密钥在前，口令是第二条路**。登记过通行密钥的机器，进来先看见
  「用通行密钥登录」，口令那一段折在下面。二次验证的 6 位框只在真要输码时才出现。
  <br>口令连错到被锁的时候，页面上是一条写着**实际剩余时间**的横条，并把表单整个禁用——
  它每秒往下走，到点自己放开。文案里写明「锁定期内即使输对也会被拒，这不代表口令错了」：
  少了这一句，人会去重置一个根本没问题的口令。口令框下面也写清了填的是**控制台的口令**，
  不是 OneBot 实现（如 NapCat）界面的那一个，两者互不相干。
  <br>页底新增「忘记口令？」，点开是三步说明加一个令牌输入框——口令没有找回，只能重设。

### 修复

- **只读口令签发或吊销写不进磁盘时，不再当成已经签好或已经吊销**，控制台和面板自助换口令的接口都会用人话说明，不再报服务器错误；吊销失败和「没有找到」也分开说。磁盘恢复之前，那把口令不会悄悄生效。

- **二次验证开着时，通行密钥登录会确认是本人在用。** 只按一下、不认指纹或 PIN 的安全钥匙这时登不进来，界面会说明原因；关掉二次验证后仍和以前一样。

- **修复下播报告曲线错位**：开播时刻不在整分上时（例如 12:00:30 开播），六条曲线与
  高能时刻整体错一格、开播那一分钟在图上丢失。曲线取时间格的对齐法与逐分钟累计
  那一条统一为按绝对分钟取整，两条路同一对齐法。

- **Redis 连不上时不再拖住控制台**：累计存储的探活挪到后台线程定时刷新，打开首页
  与菜单只读缓存的结果。此前主机不可达（地址写错、服务器丢包）时一次建连要等满
  超时才死心，这期间页面被挂住整整一个超时；连接超时也显式收到与命令超时同级。
  <br>刚填好地址、或刚换过地址后的头几秒，首页与菜单先按「不可用」说——探活只在
  后台做、结果最多缓存几秒，晚几秒说的是真话；换来的是打开页面的人从不等一趟
  可能等满超时的建连。

- **修复程序无法启动**：下播报告的绘制器有了一个继承它的「版式预览」绘制器之后，
  两者在容器里成了同一个类型的两个候选，而下播报告推送与「直播报告」命令按类型取其一，
  于是启动时报 `NoUniqueBeanDefinitionException` 并直接退出。正式绘制器现已标为首选，
  版式预览那一屏仍取预览绘制器，行为不变。
  <br>随之补上两处此前谁都答不了「这个包起不起得来」的检查：一条容器解析的测试，
  以及 `tools/boot-smoke.sh`——把产物拷进临时目录、在没有任何配置文件的情况下起一次、
  探控制台的登录状态接口。全部单元测试通过与程序起得来是两件事，此前没有任何一处把它们钉在一起。

## [5.0.1] - 2026-09-05

### 修复

- **B 站改用新格式的礼物消息后，礼物不再从报告里消失**。平台已开始把部分直播间的礼物消息
  换成新格式下发，此前这些房间的礼物收不到：直播报告少记收入，感谢也不会触发，且不会有任何报错。
  现在新旧两种格式都认，送礼人、礼物、勋章信息照旧入账，金额按折扣价、实扣按平台给出的数记。
  新格式里还没见过的盲盒开箱与背包礼物先按普通礼物入账，等真样本出现再细分。
- **加入监听时已经在播的主播，不再被当成没在播**。运行期新加的在播主播，直播报告与实时数据
  此前会全程答「当前没有正在直播的主播」。现在照实记成在播——人不是刚开播，不补推开播通知；
  已有本场数据的不动，这场下播时报告照出。

## [5.0.0] - 2026-09-04

### 新增

- **事件源协议 v2 的规格随代码进了仓库并就此冻结**：[`docs/protocol.md`](docs/protocol.md)。
  写插件、写面板或写任何一个消费方的人，以这一份为准。
  <br>此前这份规格长在消费方那一侧，标题还写着「版本 1（草案）」，而线上跑的已经是 v2；
  两边对不上时没人说得清该信谁。**现在它与代码同仓、随代码走**：改协议要抬版本号并同步这一件，
  版本号的真源是 `NovaEventEndpoint.PROTOCOL_VERSION`。
  <br>规格逐条写明每种消息的信封键、字段类型与可空性、握手与认证、心跳、回补、
  关闭码取值与示例帧，并单列一节「自 v1 草案的变化」。
  <br>**代码一字未改**——冻的就是在发的。其中几处是把实现现状写进了规格：
  弹幕的 `msgId` 在本实现里恒为空串（去重优先用 `seq`）、礼物的 `combo` 恒为 `null`、
  点赞未聚合故 `count` 恒为 `1`、`room_stat` 的三项是可选键而非可空字段。

### 变更

- **事件源相关的类挪进了新的 `novacore` 模块，包名与行为不变。**（写插件的人需要看这一条）
  **包名一个字没改**：事件、事件模型、数据源、事件输出协议、平台标识以及那几节配置类
  仍在 `com.starlwr.bot.core.*` 下，原来的 `import` 一行都不用动，插件依赖
  `starbot-core` 的写法也不用改——`novacore` 会作为它的依赖一并带上。
  配置键、`datasource.json` 的写法、事件输出协议全部照旧。
  <br>挪的是构建上的落点：这些类此前与控制台、推送、绘图、告警同处一个模块，
  谁都能引用谁。现在它们单独成一个模块，而这个模块<b>不依赖运行壳</b>——
  于是「事件源不许反过来依赖上层功能」这件事由编译器当场判定，
  往事件源里写一句对控制台类的引用会直接编译失败，不必等到有人想起来去查。
  <br>**插件迁移**：不需要任何改动。只有一处例外——用事件输出协议校验器
  `NovaProtocolSchema` 写测试的，测试域依赖从 `starbot-core` 的 `test-jar`
  改为 `novacore` 的 `test-jar`（`groupId` 与版本不变）。

- **数据源这一节配置也改为独立的类，数据源不再需要认识事件处理器。**（写插件的人需要看这一条）
  **配置文件一个字不用改**，`datasource.json` 的写法与内容也一个字不用改：
  `starbot.core.datasource.json-path`、`starbot.core.datasource.json-auto-reload`
  仍是原来那两个键，界面上的字段与说明照旧。用不着写插件的人不必看这一条。
  <br>改的有两处。一是这一节与网络、日志那几节一样单独成件，叫 `DatasourceProperties`，
  在 `com.starlwr.bot.core.config` 包下。二是数据源基类 `AbstractDataSource` 不再依赖
  事件处理器服务：处理器实例、事件类型、推送参数的补全此前写在数据源里面，
  而这些都是推送侧的事——读一份推送配置，本不该连带背上整条推送链路。
  现在这一步交给 `PushMessageInitializer`，由推送侧实现并装配；数据源只按它的回答
  决定一条推送消息还留不留。**行为一字未变**：处理器认不出来时照旧写一条 error
  并丢掉那条消息，参数合并的规则也照旧。
  <br>**插件迁移**：自定义数据源的构造方法，第三个参数由 `StarBotEventHandlerService`
  换成 `PushMessageInitializer`（在 `com.starlwr.bot.core.datasource` 包下），
  直接沿用容器里那一个即可；直接构造 `JsonDataSource` 的，第四个参数由
  `StarBotCoreProperties` 换成 `DatasourceProperties`（`properties.getDatasource()` 取得到）。
  <br>另外 `PushMessage.getHandlerInstance()` 的返回类型由 `StarBotEventHandler`
  变为 `PushMessageHandler`（`com.starlwr.bot.core.model` 包下的空接口，
  `StarBotEventHandler` 已继承它）。**写处理器的人不受影响**，一个字都不用动；
  只有取出这个实例再调用它的代码需要先 `instanceof StarBotEventHandler` 收窄一次。

- **网络、线程池、日志、直播这四节配置改为各自一个独立的类。**（写插件的人需要看这一条）
  **配置文件一个字不用改**：键名、默认值、界面上的字段与说明全部照旧，
  `starbot.core.network.*`、`starbot.core.network-thread.*`、`starbot.core.log.*`、
  `starbot.core.live.*` 仍是原来那几个键。用不着写插件的人不必看这一条。
  <br>改的是代码里的落点：这四节此前是 `StarBotCoreProperties` 的内部类，
  而那个类同时还装着控制台口令、告警通道、绘图字体等一整套配置——
  只为读一个网络超时值，就得依赖整份配置。现在它们是四个独立的类：
  `NetworkProperties`、`NetworkThreadProperties`、`LogProperties`、`LiveProperties`，
  各自不带任何框架注解，用哪一节就依赖哪一节。
  <br>**插件迁移**：把 `StarBotCoreProperties.Network` 这类类型引用换成对应的新类名
  （`.Network` → `NetworkProperties`，`.NetworkThread` → `NetworkThreadProperties`，
  `.Log` → `LogProperties`，`.Live` → `LiveProperties`，均在
  `com.starlwr.bot.core.config` 包下）。经 `StarBotCoreProperties` 取值的写法
  （如 `properties.getNetwork().getConnectTimeout()`）**不受影响**，一个字都不用动；
  也可以直接注入其中某一个类，容器里四个都在。

- **直播平台不再由一份写死的清单列出，改由各直播平台插件自己登记。**（写插件的人需要看这一条）
  `LivePlatform` 此前是一个枚举，里面列着八个平台名，其中七个一行实现也没有——
  装了这套程序的人在界面上看得见那些名字，填下去却什么也不会发生；而想接一个不在名单里的平台，
  哪怕插件写好了，也得先改一次主程序才能把名字加进去。
  <br>现在 `LivePlatform` 是一个开放标识：插件用 `LivePlatform.of("<标识串>", "<显示名>")`
  登记自己的平台，主程序只认标识串、原样透传，不认识任何一个具体平台。接新平台不必再改主程序。
  <br>**存出去的东西一个字没变**：`datasource.json` 里的 `platform`、数据库里按平台分键的
  直播数据与绑定关系、事件上的平台字段，写的仍是原来那个串（哔哩哔哩仍是 `bilibili`）。
  既有配置与既有数据<b>不需要任何改动</b>。读到没有插件登记过的标识串时，
  程序照原串收下，不会因为「不认识」而把那条数据丢掉。
  <br>**插件迁移**：把 `LivePlatform.BILIBILI` 这类引用换成自己插件里的常量，例如
  `public static final LivePlatform EXAMPLE = LivePlatform.of("example", "示例直播");`；
  取标识串的方法由 `getName()` 改为 `id()`，另有 `displayName()` 取显示名。
  标识串一经发布不要再改——它就是存在使用者数据里的那个值。

- **事件输出端点改由核心提供，配置项换了位置。** 这条本地事件流此前长在哔哩哔哩插件里，
  可它推的是与平台无关的事件，接它的面板也不关心数据从哪个平台来。现在端点、编号回补与
  口令校验都由核心提供，往后接别的直播平台时，事件流仍是同一条、同一个地址、同一套协议。
  <br>**协议一个字没改**：信封字段、序号与断线回补的语义、心跳节奏、口令在连接后首帧出示、
  握手里不许带凭据、只接受本机连接——全部照旧，地址默认也仍是 `/nova/events`。
  已经在用的客户端**不需要任何改动**。
  <br>配置项从 `starbot.bilibili.event-stream` 挪到了 `starbot.core.event-stream`。
  **旧位置仍然认得**，只写旧位置的照常生效，启动时会打一条提醒；两处都写时以新位置为准，
  新位置没写到的项才取旧位置的值。改不改、什么时候改，由你决定。

- **控制台正确显示按旧位置生效的事件输出配置。** 接着上一条：旧位置仍然认得、程序照你写的值在跑，
  可控制台此前只认新位置——只写旧位置的配置，设置页里那一节显示的是「关闭」，而事件流其实开着。
  看着关、实际开，照着界面去排查只会越查越远。现在这一节显示的就是**真正生效的那个值**，
  并在下面注明它来自旧位置、新位置在哪里。
  <br>想迁过去，在这一节保存一次即可，提示随之消失；**旧位置的内容不会被改动**——
  那是你的配置文件，那几行要不要清掉由你决定。

- **控制台里的「哔哩哔哩」页改为随哔哩哔哩插件走。** 这一页此前长在控制台自己身上：
  哪怕这台实例没装哔哩哔哩插件，页签条上也照样立着它，点开是一张永远空着的页——
  而使用者无从判断是插件没装、还是它坏了。现在装了插件才有这一页，把插件挪走页签就跟着消失。
  页面内容、账号扫码登录与登出、监听中的主播一览都**与此前完全一致**，接口地址一个也没变。
  <br>同一件事的另一头：添加主播时的直播平台不再写死成哔哩哔哩，而是按这台实例
  **实际加载了哪些直播平台插件**来定。只装了一个平台时与从前无异，不必多选一次；
  装了不止一个时，添加那一行会多出一个平台选择；**一个直播平台插件都没装时，
  那一行会直接停用并写明原因**，而不是让人填完点下去、再被服务端回一句查不到。

- **首次配置向导的「登录」那一步，也照实际装了哪个平台来写。** 那一步此前把一个平台名写死在
  标题与提示里，于是没装对应插件的实例，向导第二步指的是一件根本做不成的事，还会一直算作
  「未完成」催着人去做。现在标题与提示都取自这台实例真正加载的平台：只装一个就照旧显示它的
  名字，与从前无异；装了不止一个时会停在还没登录的那一个上并点明是谁；一个都没装时写明原因，
  不再计入未完成的步数。

- **某个平台页出问题时，控制台只少它那一页。** 页签清单里只要有一项填得不对、或者取值时出了错，
  此前会连累整条清单——控制台上一个平台页都不出现，看起来像是插件全没装上，而真正出问题的
  那一个反倒认不出来。现在坏的那一项被单独跳过并记进日志，其余的页照常可用；
  某一页刷新时出错，同样只影响它自己。

### 修复

- **推送配置还没填完时，控制台会说清楚差的是哪一条、哪个字段，而不是打不开。**
  安装包里的 `datasource.example.json` 是一份留空的样板，uid 与群号都等着你填。
  照着它改、还没改完就去控制台看看——「群与成员」这一页此前会直接报
  「载入运行状态失败」，页面上再没有别的话，既看不出是配置的问题，
  更不知道该回去改哪一行。
  <br>现在这一页照常打开，页首直接写明「推送配置中的第 1 位主播尚未填写完整，
  暂未加载，还差：uid、targets[1].num」。没填完的条目不会被加载
  （半份配置本来也推不出任何东西），启动日志里同样会有一行提示，多条只写一行、不刷屏。
  <br>**已经配好的推送一切照旧**：填齐了的配置，加载与推送的行为一个字没变。

- **设置页里的开关不再无论如何都显示「已关闭」。** 控制台会把口令、密钥这类字段遮成
  `********`，免得面板开在直播画面里就把它们摆了出去。而判断「哪些该遮」此前只看名字里
  有没有 token、password 这类字眼，于是把两个**开关**也一起遮了：
  事件输出的「要求出示只读口令」（`require-token`），以及登录设置里的
  「保留『忘记口令』的启动令牌通道」（`operator-token`）。
  开关拿不到真值就画不出正确的状态，于是**无论实际开着还是关着，界面上一律显示「已关闭」**。
  <br>这不是显示得难看，是**读数骗人**：你以为事件输出没要口令、以为启动令牌通道已经关掉，
  而它们其实都开着。装了反向代理却因此以为开了口令校验的，等于面板前面没有门。
  <br>现在遮不遮**按字段类型判**，与名字无关：开关只有开和关两种状态，遮起来什么也藏不住，
  一律照实显示。口令、密钥、令牌值等真正的机密**一条不少地照旧遮着**；
  配置项写在旧位置（`starbot.bilibili.event-stream`）时同样显示正确。

- **升级之后不会再看到上一版的旧页面。** 控制台的界面文件由构建产出，而上一版留下的同名旧文件
  有可能混进新包——一旦混进去，它会盖过新的那一份：页面照常打开、大小也正常，
  内容却仍是上一版的，**从外面完全看不出来**。现在每次构建都从头做起，出包前还会逐条核对
  包里的界面文件是不是都出自这一版的源码，对不上就不出包。

- **平台插件的页面脚本若与控制台自带的界面文件重名，这一页会被拒收并在日志里点名。**
  重名意味着同一个地址底下有两份内容，而取到的是哪一份，从外面同样看不出来。
  与其悄悄选一方，不如把这一页停掉并说清是谁、重的是哪个名字——插件作者换个文件名即可，
  而换名字这件事，得先知道重名了才做得到。控制台自带的页面与其余插件页都不受影响。

## [4.4.1] - 2026-09-03

### 修复

- **控制台的使用协议改成「先登录，再同意」。** 4.4.0 把它排在了登录之前，于是那一步
  **不需要任何凭据就点得动**：只要能连上控制台监听的那个端口——默认虽然只有本机，
  但那也包括本机上跑着的任何一个程序，以及从远端转发进来的一端——就能替你把「已同意」
  写进配置文件。记录一旦写下，你本人就再也不会被问第二次，那行记录也就
  **证明不了是你看过并同意的**。
  <br>现在协议出现在登录之后：输过口令、或凭「忘记口令」的启动令牌进来之后，
  才谈得上问你同不同意。在你点下「同意并继续」之前，控制台的接口一律回
  「请先阅读并同意使用协议」，首页给的也仍是那张协议页。
  <br>同意记录里因此多了一项：**从哪条通道点下的**（输口令，还是启动令牌），
  与版本号、时间一起留在配置文件里，日后要回答「这是谁点的」时，答案在盘上而不靠人回忆。
  <br>⚠️ **4.4.0 里已经点过同意的，会在下次登录之后再请你确认一次**——那时留下的记录
  只有版本与时间，说不出是谁点的。协议文案一个字没改，确认这一次之后就不会再问。

## [4.4.0] - 2026-09-02

### 新增

- **单个实例最多同时监控 10 位主播。** 推送配置里写多了不会让机器人起不来，超出的那几位
  只是不被加载；**被挡下的会在日志里逐个点名**（uid 与昵称都写出来），不会悄悄少几位——
  「明明配了却没有推送」这种事，日志里得有话说。
  <br>在控制台里更直接：保存推送配置时若启用的主播超过 10 位，会当场拒绝保存，
  并告诉你上限是多少、这次提交了多少位，不必等到翻日志才发现。总览页的「监听主播」
  那一格也改成「已用 / 上限」，顶格了一眼就看得出来。

- **控制台第一次打开会先显示使用协议，点了「同意并继续」之后才能登录。** 协议就一屏，
  说清 NovaBot 是做什么的，以及使用时的几条约定：只限公会与主播自用、不做大规模数据抓取、
  不碰他人隐私、单个 NovaBot 最多同时监控 10 位主播、遵守直播平台的规则。
  <br>同意记在配置文件里而不是浏览器里：换台电脑、换个浏览器打开的仍是同一个使用者，
  记在浏览器里就成了每换一处再点一次，而那会让「同意」变成一个随手点掉的动作。
  <br>「忘记口令」的启动令牌地址同样要先过这一步。那条通道本就绕过口令与二次验证，
  它要是连协议也绕过去，照着启动日志进来的人一次协议都不会看到。
  <br>协议文案日后若有改动，会请你再确认一次；已经登录着的浏览器不受影响。

## [4.3.1] - 2026-09-02

### 新增

- **日志到期会自己清理了：排障日志留 30 天，事件调试日志留 7 天，三棵树各配一个体积兜底。**
  此前没有配保留期，也就是**一天都不删**——盘迟早被写满，而且这些文件里逐条带着
  观众昵称与 uid，堆着不只是占地方。
  <br>**分两档的依据是内容性质，不是体积**：事件调试日志（`EventDebug`）攒的是旁人的身份数据，
  留得越久攒得越多，所以只留 7 天；排障要的是「往回看得多远」，主日志与网络调试日志留 30 天，
  取的是与下播记录保留期同一个数，好让「这台机器上的东西留多久」只有一个答案。
  <br>**30 天是声明出来的默认值，不是量出来的结论**——手上只有两次「回头翻日志」的实际样本、
  都落在一周之内，不足以定分布。配置注释里写明了它的来历，免得下一个人把它当测量值。
  <br>另给三棵树各配了一个宽松的 `totalSizeCap`（`1GB` / `1GB` / `128MB`），防的是某天量
  突然翻十倍时把盘写满，平时碰不到。取法是「当前日量 × 保留天数 × 10」，实测日量与算式
  一并写在配置里；**日量随主播数与调试开关变化，开关一改就要重算**——把直播间原始报文
  写进日志的那个开关开着时，实测日量比稳态高一个量级。
  <br>🔴 顺带钉住一处容易漏配的写法：`totalSizeCap` 必须与 `maxHistory` 同时写才生效。
  只给上限时 logback 照常启动，只在配置期的状态流里留一行 WARN，然后把上限当没看见——
  那行 WARN 淹在启动噪声里，所以另加了一条构建期检查钉住它。

### 变更

- **发行的 systemd 单元把内存上限抬到 `MemoryHigh=1.2G` / `MemoryMax=1.5G`。**
  此前是 `920M` / `1G`，只比「堆 512m + 元空间 192m + 线程栈与直接内存约 150M ≈ 854M」
  高出一点，余量薄到经不起主播数或功能开关的变化。新的一组按「**实测稳态峰值 896 MB
  + 说得出理由的余量**」取，并已按这两个值连续运行验证过：`memory.events` 的
  `high` / `max` / `oom` 三个计数一直是 0。
  <br>⚠️ **896 MB 是那个场景下的稳态峰值，不是上界。** 它的条件是「2 位主播、功能全开、
  连续运行 63 小时」；主播更多、瞬时高峰更猛时它还会往上走，而那种场景**没有测过**。
  这句话现在写在单元文件与手册里那个数字的旁边，免得有人拿它当官方容量——
  主播数明显更多时请自己往上调。
  <br>**代价是 1 GB 的机器装不下这一组了**，要连同 `-Xmx` 一起往下调。这条指引现已放到
  README 安装那一步的显眼处，手册里给了一组按公式算的值。**别只压两个数中的一个**：
  只压硬上限等于把 OOM 提前，只压软上限换来的是一种完全静默的变慢。配错了不用自己算，
  启动自检会读 cgroup 的上限与 JVM 上限比对，明说是哪一条、差多少、该往哪改。

- **下播报告里的「分享直播间人次」不再标着「实验性」，正式作为一项指标。**
  它从加进来那天起就与「关注人数」共用同一套解析——标实验性不是因为解析不一样，
  而是**见过的分享太少**（当时只有 5 次），说不准分享的消息里会不会冒出没见过的内容。
  <br>现在样本够了：**2026-08-12 至 08-30 的运行记录里有 13 次分享，跨 2 个直播间，
  每一条都取得到用户身份**；这些消息用到的字段，全都在「关注」与「进房」这两类早已正式的
  消息里出现过，**没有一个是只在分享里出现的**。
  <br>显示与算法都没有变化，改的只是这行标注。

### 修复

- **含图推送失败时不再连文字一起丢——发送侧自动剥掉图片段重发一次纯文字。**
  一条消息里文字与图片混排时，**图片下载失败会让整条消息发送失败**，不是只丢图
  （2026-08-11 实测，NapCat / OneBot 11：404 与 DNS 失败两种都试过，消息 id 均为空、
  群里一条都没出现）。于是封面拉不到的那一次，开播通知**整条消失**。
  <br>4.3.0 靠默认模板里那个 `{next}` 把文字与封面分成两条来规避，
  但**注释拦不住配置行为**——使用者删掉它就重新暴露。现在拦在所有消息的汇合点
  （`StarBotMessageSender.doSend`），图片占位符无论来自处理器还是模板手写都管得住。
  <br>**触发三条件缺一不可**：发送失败 + 含图片段 + 剥掉之后还剩东西。
  少了第二条会让所有失败的重试次数翻倍；少了第三条会朝群里发一条空消息。
  <br>**刻意不判断「失败是不是图片引起的」**：解析错误文案是用错量，NapCat 版本一改
  判据就静默失效，而失效方向是「再也不降级」。不判断成因也不会双发——非图片故障
  （被禁言、被踢、群号错、Token 错、出网故障）下纯文字重发同样失败。
  <br>唯一可能双发的是「接口谎报失败」，已收成一个有定义的条件：
  **只在响应没带消息 id 时才重发**。降级**只发一次、不走重试**，
  最坏是「原内容 N 次 + 纯文字 1 次」而不是 2N 次。
  <br>降级会留三种痕：送达、重发仍失败、剥完为空各一行日志，
  **都带上原始失败原因**（降级不能掩盖根因）；报告侧另记一笔，见下。
  <br>**4.3.0 已知问题里「删掉 `{next}` 会让文字跟着图片一起丢」这一条到此修复**，
  手册与两处默认参数的警示同步收掉——模板现在怎么写都不会丢文字。

- **下播报告注明本场有几条推送的图片没送到。** 零则不显示。
  兜底之后开播通知不再整条丢失，但主播收到的那条是**没有封面的**——
  这是他能感知的差异，而它此前只在日志里有记录，只有运维看得见。

## [4.3.0] - 2026-08-12

### 新增

- **匿名模式：不登录也能采集直播。** 开关是 `starbot.bilibili.account.anonymous`，
  开启后**完全不使用凭据**——不读已保存的凭据、不弹二维码、不做复检与续期。
  直播采集与事件输出照常，动态推送与自动关注不可用。
  <br>顺带修掉一处连坐：登录失败此前会整个 `return`，把不需要登录态的直播采集也一并跳过了，
  而绝大多数使用者要的恰恰是直播那半边。现在两条通道分开走。
  <br>**匿名模式在启动日志、运行状态页与手册里如实标注，三处共用同一份文本**，
  说清它拿不全数据；配置界面也不再对着一个永远不会出现的二维码干等。
  新增[《在本机把真源跑起来》](docs/runbook-local-source.md)：零凭据十分钟接上事件流。

- **本地事件输出（WebSocket）。** 把直播间事件按「事件输出协议 v2」实时推给本机的其他程序，
  自建面板、独立统计这类需求不必再去解析日志或改代码。
  <br>协议要点：`hello` 握手声明能力集、`seq` 在会话内严格递增（控制类消息不占号）、
  断线后可用 `resume` 回补、环形缓冲、15 秒心跳、每条事件附带平台原始报文。
  <br>**默认关闭**，且**只接受来自本机回环地址的连接**——这一点与 `server.address` 无关：
  即使为了对外提供推送接口把监听地址放开，事件流仍然只认本机。
  需要跨机读取请自行建立 SSH 隧道，直接暴露到公网风险自负，
  因为流里有观众的昵称、uid 与消费金额。
  <br>**弹幕里的内联表情逐个给出**：`inlineEmojis: [{placeholder, url, w, h, count}]`，
  按占位符去重（同一表情重复出现时只给一项，次数放 `count`，用平台自带的那份，
  实测 304/304 与正文实际出现次数吻合，所以下游连数都不用数）。
  <br>**为什么是数组而不是一个对象**：因为确有弹幕带**两个以上不同的表情**——
  实测 4/298，最多 4 个不同表情。比例不高，但给一个对象就装不下。
  <br>（另有 80/298 的内联弹幕里表情**出现两次以上**、最多 10 次，
  那种情况由 `count` 表达、不需要数组。**这两个数一度被混用来论证数组的必要性，现已分开写。**）
  <br>`emoji` **只在 `dm_type = 1`（整条就是一张表情图）时非空**，
  于是 `emoji != null` 是精确的「这条是不是整条表情」判据；
  想判断「这条带表情」要读 `inlineEmojis`。
  <br>配置见 `starbot.bilibili.event-stream`。25 项测试覆盖编号、回补边界与握手顺序，
  协议校验器另有正例与**反例**（`emoji` 与 `inlineEmojis` 同时非空必须被拦住）；
  **82618 条实抓报文**（20 个直播间，虚拟 / 娱乐 / 网游赛事 / 团播，匿名与登录态各有）
  映射出 **53276 条信封、违例 0**——映射后与按出口那组序列化参数写出后**各校验一遍**，
  **这一遍是在本版最终形态（协议 v2、`inlineEmojis` 已落地）下跑的**；
  真实房间连续跑 **70 分钟 690 条、0 违例**，seq 无缺号无重号，
  与内部事件计数逐类对账一致（弹幕 274=274、礼物 21=21），全程 0 次重连。
  <br>语料回放可自行复现，见[《拿实抓语料回放协议校验》](docs/runbook-protocol-corpus.md)。

- **纯监听房间被开关挡掉时会说出来。** 纯监听房间指配了主播但不配任何推送目标的那种——
  照常连接、照常累计、照常把事件送进事件输出，但一条 QQ 消息都不发。
  `only-connect-necessary-rooms` 为真时这类房间会被整个跳过，
  <b>表现是「加了主播却什么都没发生」</b>，此前日志里连一行痕迹都没有，现在逐个列出来。
  <br>配置模板此前写的是 `true`，与代码默认值相反，等于默认关掉了这项能力；
  现已统一为 `false`——**配了的主播全都连**。要靠它省连接数的大规模部署再手动开。

- **醒目留言补齐了停留时长、消息 id 与起止时间。**
  此前这四项在事件模型里根本不存在，下游想做「这条 SC 还剩多久」只能自己猜。
  字段名与量纲取自 2026-08-07 的实抓样本：平台给的起止时间是**秒级**时间戳，
  对外输出时统一换算成毫秒——协议里其余所有时间都是毫秒，让 SC 独自用秒，
  下游只要有一处忘了换算就会把 2026 年算成 1970 年。

- **弹幕事件带上了房管标志。** 取自报文的 `info[2][2]`（实测 1346 条弹幕中 24 条为 1）。
  <br>**只有弹幕消息带这个标志**，礼物、上舰、进房的报文里没有它，因此那些场景下它是「空」。
  空的含义是「这条消息没说」而不是「不是房管」——同一个人发弹幕时是房管、送礼时是空属于正常现象。

- **匿名模式那句提示改成给出实测数字。** 原先写「部分房间的弹幕可能被服务端限制下发
  （实测已证实）」——「部分房间」「可能」都太软，读起来像个别情况下的小概率事件，
  而实测是**个人主播的直播间普遍只有约一成**（七格 8.8%~12.5%，跨虚拟、娱乐、网游）。
  <br>现在明说「只能拿到约一成弹幕（七格实测 8.8%~12.5%）」。
  **给出数字才拦得住「那我就不登录了」这个决定。**
  启动日志、运行状态页与手册仍是同一份文本（`ANONYMOUS_NOTICE`），三处一起变。
  测试断言跟着从「必须含某个词」改成「必须给出那个数字」。

- **凭据维护改成分阶段退避，复检与续期各自独立。**
  此前两件事共用一个固定周期，且**失败不退避**：出网劣化时会按 10 分钟一次一直打接口，
  既帮不上忙又给风控送素材。现在一件连续失败就逐级拉长自己的间隔（一倍、两倍、四倍……到上限），
  成功即回到基准周期。
  <br>**两者互不影响**：出网劣化时续期查不动，不该把复检也一起拖慢——
  复检是我们判断「凭据到底还在不在」的唯一途径。
  <br>⚠️ **难点不在退避本身，在「什么算失败」**。这两个方法原来的布尔返回值都不是「尝试成不成功」：
  `verify()` 返回的是登录态，网络故障与成功都返回 `true`；
  `refreshCookiesIfNeeded()` 把六种情形压成一个 `false`（停机中、开关关着、缺刷新口令、
  查询失败、服务端说不需要、续期失败），**其中只有两种是失败**。
  照这两个值退避，会得到两个相反方向的错：
  **「问出了明确的已掉登录」被当成失败**——那恰恰是一次成功的复检，
  而掉登录之后正该按基准节奏盯着，才能在人重新扫码后及时发现恢复；
  **「服务端说不需要续期」被当成失败**——那是最常见的正常答复，几个周期就会把间隔推到上限，
  于是真正需要续期的那天我们正好在等。
  <br>因此内部拆出「本次尝试的结果」（做成了 / 压根没做 / 试了没成）三态，
  与业务返回值分开，两个公开方法的语义一字未改。
  <br>退避本体不持有时钟，时刻由调用方传入，**每一级都用固定时刻确定性地测出来，没有一处 sleep**：
  靠 sleep 撞出来的时序测试必然时好时坏，那种测试会训练人去重跑而不是去看。
  <br>另有一条溢出防线：失败次数攒到 31 次时 `1L << 31` 就溢出，间隔会变成负数、退避静默失效，
  **而这恰好发生在「跑了很久 + 长时间故障」这个最需要退避的场景**。移位次数封在 30 以内，
  测试直接喂 5000 次失败。
  配置见 `starbot.bilibili.account.maintenance-backoff-cap`（默认 3600 秒）。

- **网络日志加了同类去重抑制。** 打开 `network-log` 之后每个请求写一行，而本程序的请求
  绝大多数是轮询：三个房间十秒一轮，一小时上千行几乎一样的记录，
  **要查的那一条异常正好淹在里面**。排障日志的用处取决于它读不读得下去。
  <br>同类按「请求方法 + **去掉查询串**的地址」判定——查询串里装的正是房间号、uid
  这些每次都不同的东西，带上它就没有两条请求是同类的，抑制永远不会生效。
  <br>三条边界写死在测试里：**失败一律放行**（抑制的目的就是让异常显出来）、
  被抑制的条数随下一条同类日志一起报出来（悄悄丢掉才是真丢信息）、
  **抑制按「一次请求」决定而非按「一行日志」**——`->` 与 `<-` 必须同进同出，
  分别抑制会留下孤立的半条，而这两行配起来才算得出耗时。
  配置见 `starbot.core.log.network-log-suppress-window`。

- **断线日志改成带归因的窗口摘要。** `1006` 这个关闭码把四件毫不相干的事说成同一句话：
  认证被服务端拒、家里网断了、服务端重启、被踢。现在按「关闭码 + 这条连接活了多久 +
  认证过没有」归因成**握手期被切断 / 认证后很快被切断 / 网络抖动 / 服务端正常关闭 /
  其它关闭码**五类，每类附一句下一步提示——**归因不写建议，等于只把问题换了个说法**。
  <br>归因只用手上已有的信号，**不为它增加任何接口调用**：需要排查的时候往往正是网络不好的时候。
  <br>逐次那行降到 DEBUG，改按窗口汇总一条：这段时间断了几次、分别归因于什么、
  集中在哪几个房间、连接平均活了多久。**逐次一行在断线风暴里恰好最没用**——
  十个房间各断五次就是五十行，数不清次数，也看不出是集中在一个房间还是所有房间一起断，
  而这两者的处理方式相反。没有断线的窗口不打日志。
  配置见 `starbot.bilibili.live.disconnect-digest-interval`。

- **崩溃时进行中的那一场不再整场丢失。** 此前程序被 `kill -9` 或机器掉电时，
  那一场没有下播事件因此从未归档，而**下一次开播的清零会把它连同全部统计一起抹掉**。
  现在下一次开播会先检查「状态还挂在在播、开播时间不是这一次的」，
  命中就按**异常中断未闭合**归档，指标、参与人数一条不少。
  <br>结束时刻取的是**最后一次落盘的时刻**——那是能证明的最后一刻，
  所以这类场次的时长只是下界，界面与归档都标出来、不参与趋势对比。
  <br>不在启动那一刻判，是因为启动时并不知道那一场结束了没有：主播可能还在播
  （此时归档就会造出一条重复的半截记录）。开播这个时刻则必然到来且信息完备。
  <br>实测：`kill -9` 之后盘上剩下的正是最后一次自动保存（相差 3 秒）、
  「上次退出异常」标志为真；重启后如实记出 44 秒缺口。旧数据文件没有这些字段，
  读成「不知道」而不是「没漏」。

- **发不出去的告警会入队重投，恢复后补发并标注原始发生时刻。**
  <br>**需要告警的时候往往正是发不出去的时候**：出网劣化、QQ 掉登录、Webhook 服务抖动，
  三者本身就是要告警的事，而告警的出口同时也断了。此前发送失败只留一行日志，
  于是「没收到告警」被读成「没出事」。
  <br>补发的正文开头写明原始发生时刻与延迟了多久——不写的话，出网中断半小时后收到
  「登录已失效」，人会以为刚刚才失效，去查一个早已结束的现场。
  <br>判失败的口径写清了三种情况：有通道可用但全失败才重投；**一个通道成功就不重投**
  （人已经收到了，重投只会让他收到第二条一样的）；压根没配通道也不入队。
  队列满时丢最旧的**并在日志里说明丢了哪一条**，不静默截断。
  <br>⚠️ **队列只在进程内，重启即丢**，这一点在退出时连同「丢掉的是哪几条」写进日志，
  不假装队列可靠。配置见 `starbot.core.alert.retry-*` 三项。
  <br>实测：真实告警对着一个连不上的地址失败入队，把接收端起起来后下一轮补发成功，
  接收端收到的正文带着 `【补发】原始发生时刻 …，已延迟 15 秒`，原文完整。

- **画图失败时改发文字版报告，而不是什么都不发。**
  <br>此前绘制失败会让 `{report}` 占位符变成空串，默认模板只含这一个占位符，
  于是整条消息成为空白、发送环节直接跳过——**主播看到的是「这场没有报告」，
  而真相是「报告画不出来」**。
  <br>文字版不追求版式对等，只保证这一场的关键数字送到；
  金额可见性与图片版共用同一份判据，**降级不是放宽口径的理由**。

- **Webhook 告警的成败只看 HTTP 状态码，不再取决于响应体能否解析。**
  <br>原先走的是把响应体转成字符串的调用，于是响应的 `Content-Type`
  参与决定成败——一个返回 200 却不带 `Content-Type` 的自建接收端会让
  HTTP 客户端抛「无法提取响应」，明明送到了却被判为失败。
  配上上面那条重投之后这个误判更贵：同一条告警会被反复补发，接收端收到一串重复。

- **维护重启造成的采集缺口会记进本场数据，报告如实标注。**
  下播报告的时长后面多一句「其中 X 分 X 秒因维护未采集」，
  数据分析页对应场次的时长带 ⚠ 且悬停可看缺了多久。
  <br>**缺口的起点是最后一次落盘的时刻，不是进程死掉的时刻**：
  那之后收到的消息随进程一起没了，同样没进任何统计。
  <br>按**全局区间**存而不按主播存——停机是进程层面的事，
  同时监听 10 位主播时 10 场共享同一段缺口，各场只截取与自己重叠的部分，
  跨越开播时刻的那一段里开播之前那一截不算。

### 变更

- **⚠️ 礼物排行榜改用「主播到手价值」，与礼物总额同口径。** 此前榜单按**实扣**排、
  卡片按**到手价值**显示，两个数在同一份报告里，界面上没有一处说明它们不同口径。
  <br>差别出在背包礼物上：它实扣恒为 0（礼物是抢红包、活动或签到白得的），
  于是**送出一份 80 元的背包礼物，在榜上记 0、在总额里记 80**。
  实况是礼物榜显示 ¥1.1、礼物总额 ¥86.6。
  <br>原先按实扣排的理由是「按到手价值排等于按运气排名：花 100 元开出一堆小心心的人排榜尾，
  花 10 元中了大奖的人排榜首」。这个理由**只对盲盒成立**，
  而盲盒那份顾虑本来就有一张独立的**盲盒盈亏榜**在表达；为它把主榜改成实扣，
  代价要每个送背包礼物的观众来付。
  <br>礼物榜、我的数据的礼物卡片、榜单命令三处都**附一句口径说明**（三处共用同一份文本）：
  「礼物按主播到手价值计，与礼物总额同口径；背包与盲盒礼物同样如此，不是观众实付」。
  <br>**实扣仍然如实记在 `gift_paid` 里**，只是不再拿它排榜。
  <br>**跨场累计的历史榜单数据是按旧口径攒的，不做回算**（没有留原始礼物流，回算做不到），
  也不归零（那是让老观众替一次口径决策买单）。代价是**累计榜里同一个数含两段口径**，
  正撞上面那条规矩，所以**累计范围下的礼物榜与礼物卡片会多出一句口径变更标注**，
  写明变更发生在升级到本版那一刻。本场范围只有一段口径，不显示这句。
  <br>由此定下一条规矩：**同一张报告或界面里同名指标必须同口径，口径不同就必须改名或标注。**

- **新装默认不再记录事件与网络调试日志。** 配置模板里的 `starbot.core.log.event-log`
  与 `network-log` 默认值由 `true` 改为 `false`。
  <br>它们是排障工具，不是运行必需，而默认开启有两个代价：
  `EventDebug/` 写的是**完整事件转储，含观众昵称与 uid**——默认配置不该让每个部署者
  在磁盘上无限期累积他人的个人信息；两个目录还**按日期分文件、不会自动清理**，
  实测三个房间约 3.8 MB/日。
  <br>**升级不改动既有配置**（`install.sh` 保留 `application.yml`），
  所以已经在用的部署行为不变，想关要自己改。
  排查时把这两项打开、查完关掉并清理目录，见 `docs/troubleshooting.md`。

### 修复

- **每次正常停机都在 journal 里留一行「Failed with result 'exit-code'」。**
  `systemctl stop` 之后 JVM 正常退出的码是 143（128 + SIGTERM），
  而 systemd 单元没把它列为成功退出码。关停本身是干净的——
  直播数据已落盘、Web 容器优雅停机完成、发送线程正常停止。
  <br>单元加上 `SuccessExitStatus=143`。那行是纯噪声，但**它训练人忽略 `Failed` 字样**，
  而下一次真的失败长得一模一样。
  <br>**升级即生效，不用手工改**：`install.sh` 每次都会用模板重写
  `/etc/systemd/system/starbot.service` 并 `daemon-reload`。
  但因为升级的顺序是「先停、再装、后起」，**装上这一行的那一次升级自己仍会留下那行日志**，
  再往后才干净。
  <br>⚠️ 顺带一句给改过单元的人：**升级会用模板覆盖单元文件**，
  在服务器上手工调过的内容（如内存上限）会被改回模板值——要长期生效得改模板。

- **⚠️ 断线归因把「我们自己为重连而关的」说成「服务端正常关闭」。**
  本端判定连接不可用（心跳超时、发包失败、断流后先重连一次验证）时会主动关掉会话，
  而这样关出来的**关闭码正是 1000**，与服务端发来的正常关闭帧一模一样。
  旧判据只看「是否已被永久关闭」与关闭码，于是一律归到服务端头上。
  <br>**关闭码 1000 的含义只是「有人正常关闭了连接」，「有人」是谁不在这个字段里。**
  现在「是谁关的」由连接器显式告知，入参用枚举而不是布尔——
  类型逼着调用方回答这个问题，而不是留一个默认值悄悄错下去。
  <br>新增一类归因「**本端主动重连**」，与「主动关闭」（停止监听、程序退出）分开：
  后者不必处理、不进断线摘要；前者要处理，而且**提示指向本机**——
  出网、机器被挂起、本机时钟都会让本端误判连接失效。
  <br>**这个错的方向最坏**：把本端问题说成对端问题，看日志的人会一路往外查。
  <br>⚠️ **旧日志里那批 1000 的归因不可信**，它们是用错标签写下的；
  改判据救不了已经写下的行，翻历史分布时这批要排除在分类统计之外。

- **⚠️ 一次断线会排两次重连，退避间隔因此比设计的翻一倍。**
  主动重连先关会话再排队，而关会话触发的关闭回调也会排一次，同一次断线于是让
  连续失败计数加 2。多排的那次本身是空转的（第二次建连看到状态已连上就返回），
  代价落在**后续**重试上：从第二次真实重试起每次退避都是设计值的两倍，
  退避阶梯爬到上限所需的真实重试次数减半——平台恢复之后要多等一截才重试。
  <br>实况日志里两条「第 N 次重连」相隔 5 毫秒，是同一次断线：
  `第 1 次 / 退避 1000 毫秒`、`第 2 次 / 退避 2000 毫秒`，
  紧接着的一次连接失败排的是「第 3 次、退避 4000 毫秒」，按设计本该是「第 2 次、2000 毫秒」。
  <br>**这个缺陷只在退避时长上露头**：从握手次数、连接状态、日志的行数上都看不出异常，
  所以此前的测试全绿。新增的四条断言看的是「排了几次队 + 最近一次要求等多久」。

- **⚠️ 启动时有两个竞态会让刚建好的长连接被自己掐掉。**
  `afterConnectionEstablished` 回调发生在建连的 future 完成之前，而会话字段要等 future
  返回才赋值；回调里启动的心跳可能跑在赋值之前，拿到空会话被当成发送失败，
  于是给一条刚建好的连接白排一次重连，时机再差一点会直接关掉它。
  <br>改为在回调里就记下会话——回调参数里那个就是权威的。
  <br>**修第一个竞态时引进了第二个**：会话提前赋值之后，回调里启动的心跳真的发得出去了，
  而认证包排在回调之后，于是心跳抢在认证包前面发出。服务端要求认证包打头，
  未认证发包同样会被切断。因此心跳改由建连流程在认证包发出之后启动。
  <br>回调现在只做「记录事实」，一切「开始做事」留给建连主线。
  <br>同时补上**连接器测试脚手架**：把握手、认证、心跳、断线、重连的先后次序变成显式可控的
  调用，竞态因此可以确定性复现而不是靠概率撞。用真线程写的竞态测试必然时好时坏，
  那种测试会训练人去重跑而不是去看。

- **⚠️ 推送接口的 `type` 实际是按枚举序号解析的，而不是文档写的取值。**
  手册与配置校验都写着 `0` 私聊、`1` 群聊，但代码从未锁住这一点，
  一直靠「默认行为按序号解，而这两个的序号恰好等于取值」在工作。
  实测：`type` 写成 `2` 会被解析成内部的「未知」并**在运行期安静丢掉这条推送**
  ——调用方收到成功响应、群里什么都没有；而真正的 `-1` 反倒被拒绝。
  <br>现在把取值锁成对外格式，反解只接受 `0` 与 `1`，认不出当场报错。

- **⚠️ 下游变慢时图片推送会被丢弃，文字却照常送达。**
  核心此前把消息 POST 给自己的服务端口，由自己的控制器收下再转给 OneBot 实现。
  这一圈自环把投递压在服务端口那几个工作线程上（默认 8 个），而控制器转发下游时是同步
  阻塞的：下游一慢线程就被占满，**没有线程去读请求体**。
  <br>体积小的文字消息一次写进 socket 缓冲区就完事，照常送达；而图片是几百 KB 的内联
  base64，必须服务端一边读才写得完——于是卡满读超时被丢弃。
  **表现为「文字能发、图片发不出去」，两端日志都看不出所以然。**
  <br>现改为**同进程直调**：适配器与核心在一个进程里时直接把消息交过去，没有 socket，
  也就没有「谁来读请求体」的问题。**对外的推送接口与它的鉴权原样保留**，外部程序照常可用；
  未提供进程内入口的推送平台（例如部署在别处的适配器）仍走 HTTP。
  <br>参数装配改为逐字段手写而非借道序列化器：`createTime` 上挂的是 Jackson 的
  `@JsonProperty("create_time")`，而项目内部用的是 fastjson，两者对同一个对象的理解并不
  一致，用错了会**悄悄丢掉时间戳**。

- **⚠️ 发行的 systemd 单元把 `MemoryHigh` 设得比 JVM 实际占用还低，服务一热起来就一直被节流。**
  单元里是 `MemoryHigh=768M`，而 `-Xmx512m + MaxMetaspaceSize=192m` 加上线程栈、代码缓存与
  直接内存约 150M，合计约 854M，实测稳态峰值 **896M**。
  <br>**`MemoryHigh` 是节流，不是警戒线**：超过之后内核不杀进程，而是对每一次内存分配
  施加睡眠惩罚并强制同步回收。表现为整个进程被均匀拖慢数秒——本机回环的 HTTP 调用要
  2~11 秒（`curl` 打同一地址只要 1.3 毫秒），而 **CPU 几乎不动、堆完全健康、健康探针全绿、
  日志里一个字都没有**。带图的推送会因此超时丢弃，文字消息因为体积小反而活着。
  <br>现改为 `MemoryHigh=920M`（`MemoryMax=1G` 不动）。**这不增加任何内存需求**——
  那些内存本来就在被占用，抬限只是停止在预算内自我惩罚。
  <br>同时新增**启动自检**：读 cgroup 的 `memory.high` / `memory.max` 与
  `-Xmx + MaxMetaspaceSize + 余量` 比对，装不下就在日志里明说是哪一条、差多少、该往哪改。
  软硬上限分别给不同的说明——一个是会被 SIGKILL，一个是会静默变慢，处置办法不同。
  只写日志，不阻止启动。
  <br>文档里「`MemoryHigh` 被突破不会静默劣化」这句话是错的，已更正。

- **⚠️ 运行状态页只判「通不通」，不判「通得多快」，因此看不见一整类故障。**
  实测遇到过这样的形状：OneBot 接口连续十小时每次调用要 2~11 秒，
  接口一直返回 200、账号一直在线，状态页因此**全程显示正常**，
  而带图的推送已经在丢——图片是几百 KB 的内联 base64，转发一慢就没有工作线程去读它，
  最终卡满超时被丢弃。**文字消息体小，塞进 socket 缓冲区就完事，所以文字能活、图片会死。**
  <br>现在「机器人连接」多一项耗时判据：超过 `slow-threshold-millis`（默认 2000 毫秒）
  即判定为降级，并在建议里给出**分辨慢在哪一侧**的办法——用 curl 直接调一次 OneBot 实现，
  curl 快而本程序慢说明慢在本程序这一侧，两边都慢才是 OneBot 实现的问题。
  <br>判据取最近若干次的**中位数**而非最近一次：健康时也有偶发慢调用
  （实测中位 0.00 秒、p99 却有 2.4 秒的毛刺），只看最近一次会被毛刺反复翻黄；
  真退化时是整体抬升（中位 2.83 秒、p90 10.4 秒），中位数能把两者分开。
  样本不足三次时不作判断，不拿两个样本硬出一个结论。

- **⚠️ 进房、关注、分享三类事件恒为 0 条，下播报告的「进房人数」永远是 0。**
  平台把 `INTERACT_WORD` 换成了 protobuf 承载的 `INTERACT_WORD_V2`，三次独立抓包
  **全是 V2、0 条 V1**。老格式的解析器认不出新消息类型，于是这三类事件彻底消失，
  <b>而且不会有任何报错</b>——报告里只是安静地写着 0。
  <br>现补上 V2 的解析。**没有引入 protobuf 运行时依赖**：官方不公开 `.proto`，
  字段语义只能靠反推，生成代码带来的强类型是假的；而 wire format 只有四种编码，
  读起来不需要 schema，因此自带一个最小 wire 读取器，未知字段自动跳过、
  截断的报文按已读到的字段照常出事件。**V1 的解析保留**，平台随时可能回滚。
  <br>字段号由 2026-08-10 实抓的 2122 条**登录态**报文逐条统计反推，
  并与 `protoc --decode_raw` 抽样核对。反推必须用登录态语料：匿名连接下 uid 被抹成 0，
  而 proto3 不序列化零值，uid 字段会整个消失。
  <br>对账：2122 条报文按类型 2117 / 4 / 1，解析产出进房 2117、关注 4、分享 1，**逐类一致**，
  0 条丢弃、0 条读取失败；uid 全部非零，853 条带粉丝勋章、29 条带大航海、3 条来自推广位。
  平台给的时间戳是**秒级**，对外输出照 SC 的先例统一换算为毫秒。
  <br>**关注只有 4 条样本、分享只有 1 条，分支照写但不声称已验证**，代码注释里逐条注明。
  <br>ℹ️ **2026-08-12 补**：样本数后续增至关注 10 / 分享 5，关注已于本版转正、分享仍为实验性。
  （上面那两个数是写下本条时的事实，按「不追改历史条目」保留原样。）

- **⚠️ 发行包的配置模板起不来。** `starbot.core.log` 在模板里写了两遍，
  YAML 不允许重复键，SnakeYAML 直接抛 `DuplicateKeyException`——照模板装的人
  **第一次启动就掉进安全模式**，看到的只是一句 YAML 报错。
  两处已合并。原有的模板检查是按行扫键路径的，看不见「同一个键写了两遍」，
  现补一条用启动时真在跑的那个加载器解析模板的检查，故意加回重复键验证过它会失败。

- **自动生成的令牌可能撞上弱片段，生成时改为重掷。**
  随机的 Base64 串里恰好出现 `test`、`admin` 这类片段并非不可能——43 个字符位置、
  每处命中一个指定四字母序列的概率是 `(2/64)^4`（弱片段比对会折叠大小写），
  一次生成约千分之一。后果不只是测试偶发变红：**自动生成的令牌会被程序自己的弱令牌检查拒掉**，
  开了 `fail-on-weak-config` 的部署会因此起不来，而使用者对着一个看起来完全随机的串
  根本无从判断哪里弱。连续 64 次都弱则直接抛异常——那说明随机源出了问题。

- **手册里「只推直播可以不登录」这句会误导人，已改写。**
  开播判断确实是公开信息，但**直播间里的弹幕与礼物不是**：2026-08-07 同房间同时段的两条连接，
  登录那条对独立基准的到达率 **100%**，未登录那条只有 **4.3%**——29.3 分钟只收到 6 条弹幕，
  而进房、排行、点赞一条不少。这个坑的形状是「有数据但少得多」而不是「报错」。
  未登录时**弹幕、礼物与进房的**发送者 uid 与真实昵称也拿不到
  （这三类的 uid 基本恒为 0，昵称打码成「首字 + `***`」），按人统计的功能会整片失效。
  <br>⚠️ **「uid 恒为 0」这句原先没有限定，那是错的**：抹除是**按消息类型**的，
  不是整条连接一刀切。同一条匿名连接上，**点赞、关注、分享照常带真实 uid**
  （关注与分享实测 11/11 全带，进房 9286 条里只有 1.0% 带）。
  <br>因此**不能用 `uid == "0"` 去识别匿名源**——收到一条关注就会把整路误判成登录态。
  匿名模式也不等于「不含个人身份」，逐 kind 的实测清单见手册。

- **「直播间数据风控」的判据换掉了，原判据会误报。**
  主判据改为**业务消息（弹幕/礼物/上舰/醒目留言）连续断流**，
  进房消息占比降为辅助信号，只用于把观测说清楚，不参与判定。
  <br>原判据是「进房消息占比 ≥50%」。2026-08-07 实测证伪：热门房间人来人往，
  进房消息天然刷屏——一个 41 万人气的游戏区房间进房占比 53% 被判风控并**真的发出了告警**，
  而它同时段每分钟收 71 条弹幕、对独立弹幕历史基准的到达率 **93.3%**，完全正常。
  <br>**「进房占比高」与「收不到业务消息」是两回事。** 真被限制下发时是**整条流按比例削减**：
  2026-08-07 的登录态对照实验里，被限制那条连接 29.3 分钟收到弹幕 6/141 = 4.3%，
  而环境消息（进房、排行、点赞）133/2479 = **5.4%**——两者被削得一样多。
  2026-08-10 同房间同时刻的成对连接同样如此，按 cmd 分类逐项 10~13%。
  <br>此处一度写作「环境消息照收、业务消息几乎为零」，**那是错的**：
  只看被限制那条连接内部的构成（环境 133 条对业务 6 条，差 22 倍）看着就像环境没被削，
  但那是房间流量本来的构成，**没有登录侧做分母，这个对比什么都说明不了**。
  判据本身不受影响，受影响的只是机理描述。
  <br>**冷清的直播间由样本量下限挡住，而下限只数逐用户事件（业务 + 进房类）。**
  排行、观看人数、点赞总数这些是平台按秒下发的，**与有没有人在互动无关**——
  任何在播房间几分钟内都必然攒够十几条，冷清房也一样。
  <br>⚠️ 这一点是 2026-08-10 深夜的生产误报换来的：下限原先数的是**消息总量**，
  于是「主播那三分钟没人说话」被读成了「业务消息断流」。
  实测那次判定是「13 条消息、业务 0、进房类 1」，**13 条里 12 条是排行与看过**；
  同期成对观察量到基准 3 条弹幕 / 我们 3 条，采集根本没停。
  <br>成因当时就写在判据自己的注释里：上一句说「环境消息照旧涓涓地来，轻松越过下限」，
  下一句却断定「下限挡的是冷清」——**同一个理由推出两个相反的结论**。
  改成只数逐用户事件之后，触发形状只剩一种：**进房类有量而业务为零**；
  「两者皆零」那一类（冷清、没在播、连接刚建）自然落在下限之外。
  <br>**「未开播不判定」与这条下限各挡一件事，不能互相代替**：轮播房也可能有人进出。
  <br>**攒满断流之后先重连一次再判，判定降为第二道防线。**
  重连是我们手上最便宜的动作（退避与闸门都现成），而它恰好就是区分
  「连接半死」与「平台真限制」的那个实验：半死的连接重连即恢复，真被限制时换一条照样收不到。
  少了这一步，一个重连就能自愈的故障会被报成平台问题，人也就白查一趟。
  <br>一段断流只重连一次，**业务消息恢复之后才重新获得这次机会**——
  不加这个限制会在断流期间反复重连，而那本身就会招致风控。
  <br>告警文案改为**只陈述观测**（收到多少条、其中逐用户事件几条、业务几条、进房几条），
  不再断言「该直播间的弹幕、礼物等事件将无法接收」——
  从这里分不清是平台限制、协议变更导致解析不出，还是真的没人说话。
  <b>连接状态的显示名也一并从「直播间数据风控」改为「业务消息断流」</b>，
  自检探针那句同样收敛：先建议去看该直播间是否确有人在互动，那是最便宜也最能分辨的一步。
  <br>配置项 `auto-detect-live-room-risk-ratio` 已删除（新增
  `auto-detect-live-room-risk-windows` 取代），**21 项测试**钉住边界，
  含那两组实测数据与**上面那次误报的原始窗口回放**。

## [5.0.0-beta3] - 2026-08-07

> 这一轮是**协议正确性急救**：把「现有数据可能已经是错的」逐条查实并修掉，
> 而不是加功能。共同点是这些问题都**不报错、不进日志、数据上完全说得通**——
> 靠人翻日志才发现，等于没有发现机制。本轮同时补上了发现机制本身。
>
> 支撑结论的实测规模：11 轮抓包、42 条并发连接、101264 条消息，
> 外加一组以 HTTP 弹幕历史为独立基准的到达率对照。

### 修复

- **修掉了会把自己打进平台限流的那个启动竞态（1006 死循环）。**
  握手包里的 `uid` 改为取自 `ConnectInfo` 的身份快照，与 token 同一瞬间产生。
  <br>原先 token 在 `getLiveRoomConnectInfo()` 取、`uid` 在握手完成后的 `sendVerify()` 才读，
  中间隔着一次 WebSocket 往返。**登录若恰好在这个窗口内完成，就会发出「匿名 token + 登录 uid」**，
  服务端遇到身份不一致会握手后立刻切断且不发关闭帧——表现为 1006；
  重连再拿一个新的绑定 token 又被拒，形成死循环。
  <br>这不是假想：2026-08-04 14:21:32 扫码登录成功后 206 毫秒发起首次建连，
  **32 秒内 96 次 1006**，随后被平台以 `-352` 限流。三天日志里 110 次 bilibili 1006，
  **109 次是连上不到 1 秒就断，中位数 0.00 秒**，且没有一次发生在房间正在直播时。
  <br>代码注释里其实早就写明这种不一致会导致 1006，**只是没意识到自己的调用时序正好在制造它**。

### 新增

- **加了全局连接放行闸门，首连与重连排进同一条时间轴。** 新增 `BilibiliConnectGate`，
  按 `live-room-connect-interval` 依次放行。
  <br>此前是两条互不知情的时间轴：启动时由房间服务按房间累加延迟错开首连，
  重连则由每个连接器自己指数退避。**房间一多，同时断线就会叠成请求洪峰，没有任何东西拦它。**
  <br>退避与闸门是叠加关系：既不会早于本房间自己算出的退避，也不会和其它房间挤在同一瞬间。
  <br>**这不是反风控手段**，是「礼貌客户端」承诺的技术兑现——对平台的请求密度应当自我约束，
  不依赖平台来教我们做人。

- **风控与静默降级接进了告警体系。** 新增健康探针，把「不报错但确实出问题了」
  的几类信号变成可被自动发现的状态：真实 HTTP 412、业务码 -352/-401/-509、
  风控质询与验证码、长连接 1006、**开播快照项缺失**。
  <br>探针实现 `HealthProbe` SPI，所以自动进入运行状态页与既有的告警收敛体系。
  <br>412 按响应状态码统计而不是日志文本 grep——日志时间戳里的 `.412` 会大量误匹配。
  <br>开播快照按「应记 N 项、实记 M 项」判定：三项各自失败互不影响的设计意味着
  少记一项的表现是**报告里那张卡整个消失、数值不会变成 0**，按数值告警永远不会触发。

- **`-352` 时重算 WBI 签名并重试一次。** 此前收到业务码直接抛异常、零重试。
  两道闸：密钥不足 900 秒不重算（重算也是同一份），已有线程在重算则不重复发起。
  <br>刻意不写成循环——目的是「给一次自救机会」，不是「重试到成功」，
  后者会在限流时把请求密度推得更高，正好是限流想拦的行为。

- **礼物事件带上背包标志 `fromBag`。** 解析器早就用 `bag_gift` 判过了，只是没往上传，
  下游只能靠 `charged == 0 && value > 0` 反推。
  <br>契约写进 javadoc：**要区分背包礼物一律以此字段为准，不要用金额反推**。
  `charged == 0` 目前恰好只有背包一种来源，但那是当下的巧合而不是约定。

- **观看心跳修好并接入。** `hb` 参数原先直接拼裸房间号，规范格式是
  `base64("间隔|房间号|1|0")`；心跳只在长连接存活期间上报，断开即停，间隔 60 秒。
  <br>只做标准存活心跳：不提交播放状态、不提交切屏与点击、不请求播放地址。
  <br>实测记录：这个接口对 `hb` 传什么都回 `code:0`，
  **所以「返回码正常」不能用来验证心跳是否生效**。

- **大航海事件带上了陪伴天数。** 平台没有给这个字段，只把它写进播报文案
  （「今天是TA陪伴主播的第 1171 天」），此前完全没有被用起来。
  <br>这是整条报文里最有感情价值的信息，比金额更值得展示。
  <br>因为是**解析文案而非字段**，改版随时会让它失效，所以取不到时一律留空，
  **绝不填 0**——「陪伴 0 天」作为假信息出现在感谢文案里，比没有这个信息糟得多。
  同理，匹配到超出常理的数值（大于平台自身年龄）也当作没取到。
  <br>目前只做到事件层，接进报告的呈现方式待定。

### 变更

- **大航海人数改用 `topListNew` 端点。** 旧端点 `topList` 仍可用，切换是为了不等它被下线。
  <br>同一时刻同一房间比对了 5 个直播间（大航海人数 0 / 3 / 16 / 117 / 405），
  两个端点的 `data.info` **逐字段一致**，名单顺序与昵称也逐条相同。
  <br>**但列表项结构变了**，将来要用名单时注意：旧端点扁平的 `uid` / `username` / `guard_level`
  在新端点挪进了 `uinfo.uid` / `uinfo.base.name` / `uinfo.guard.level`，
  新增 `score`，**去掉了 `is_alive`**。我们只取 `info.num`，所以这次切换不受影响。
  <br>顺带核对：粉丝团人数用的 `getFansMembersRank` 没有新旧之分。

- **开播动态默认不再推送，同一个群不会收到两遍开播。** 新增开关 `dynamic.push-live-dynamic`，默认关闭。
  <br>哔哩哔哩会在 UP 主开播时自动发一条「直播推荐」动态（`DYNAMIC_TYPE_LIVE_RCMD`），
  动态推送把它当普通动态照单全收，文案恰好是「开播了」，于是**开播这件事被推了两遍**。
  <br>关掉的是劣化的那一条：实测 2026-08-07 某场，开播在 11:10:45，
  这条动态的发布时间是 11:20:45（**整 600 秒**），出现在动态流里已是 11:41:59，
  **一共迟到 31 分钟**；同期两条普通动态的检测延迟只有 4 秒和 6 秒（轮询间隔 10 秒），
  所以延迟在平台侧，不在轮询。开播推送则是实时的，还带标题、封面，能 at 全体成员。
  <br>只看动态的**顶层类型**：转发开播动态是 UP 主自己的动作，照常推送。
  <br>**只配了动态推送、没配开播推送的 UP 主会因此收不到开播消息**，需要时把开关打开。
  <br>这是权宜之计：两条通道各推各的，彼此并不知道对方存在。彻底的做法是按推送目标去重
  （该目标已收到开播推送就不再推开播动态），涉及跨通道的推送状态，留待后续。

- **金额口径改名：「实付」→「实扣」，并明确放弃一个测不出来的口径。**
  `StarBotLivePurchaseEvent.paid` 改为 `charged`。
  <br>改名的理由不是措辞，是**这个字段一直被理解错了**：它是「平台从账上扣了多少」，
  不是「观众花了多少人民币」。账上的余额可能白来（抢红包、活动、签到），
  也可能是以某个充值优惠价买来的——**那笔充值交易根本不经过直播间**。
  正因为叫它「实付」，背包礼物才会被想当然地认为「`total_coin` 应该是 0」，
  而实测它等于礼物原价。名字错了，基于名字的推断就跟着错。
  <br>同时明确：**「观众实付人民币」与「主播实收人民币」在弹幕流里结构上不存在**，
  不再作为目标。已核对三类消息的完整字段清单，没有任何支付渠道字段
  （`GUARD_BUY` 只有 9 个字段）；iOS 抽成一类的差异只体现在充值环节，不经过直播间。
  <br>界面上那一对指标改为「礼物价值」与「观众消耗」，挨着展示——差额就是盲盒运气与背包礼物。
  不用「实扣」当界面文案：主播容易读成「从我这儿扣钱」。
  <br>**落盘键 `gift_paid` 保持不变**，与新叫法不一致是有意为之：线上 `data.json` 存着历史数据，
  改键会让历史变成孤儿。要改得连同口径版本化与历史回算一起做。

## [5.0.0-beta2] - 2026-08-07

> 这一轮全部围绕**金额记账的正确性**。三个写进代码的假设被真实报文推翻，
> 一个被证实。共同的教训是：手工构造的样本只能证明代码按假设运行，
> 证明不了假设成立——本轮把几处这样的单测换成了实抓报文。

### 新增

- **红包事件**（`POPULARITY_RED_POCKET_START` 及其 V2 形式）。此前解析器完全不认这条消息。
  <br>红包**刻意不算作收入**：钱进的是红包，主播这一笔一分钱没拿到，
  只有中奖者把奖品换成礼物送出主播才参与分成。所以 `RedPocketEvent` 不继承
  `StarBotLivePurchaseEvent`——继承了就会被算进营收，凭空多出一笔主播没拿到的钱。
  <br>但它值得播报：红包会把人吸引进直播间，主播通常都会当场感谢送红包的人。
  事件带上了送红包者、花费金额、奖品名称与数量。
  <br>**开启消息会被周期性重播**——实测一条报文的 `start_time` 是十分钟前而 `current_time` 就是当下，
  因此按 `lot_id` 去重，并且用红包自己的开始时刻而不是收到消息的时刻，
  否则首次见到的若已是重播，红包就会被记晚十分钟。认不出 `lot_id` 时宁可不播报，
  也不冒把同一个红包反复感谢的风险。
  <br>**尚未做**：把这个事件接到下播报告或推送上，呈现方式待定。
  中奖名单（`..._WINNER_LIST`）也暂不处理——它同样有 v1/V2 两种形式，
  而目前只抓到两个不同 `lot_id` 的样本，无法证明同一个红包会不会同时下发两版，
  贸然处理有重复计数的风险。
  <br>还有一处**已知但未修**的错位：中奖者随后送出的礼物会被记成他自己的消费，
  而钱是发红包的人出的。消费排行榜因此会排进没花钱的人，真正出钱的那位反而不出现。

### 修复

- **背包礼物被记成观众自己花了钱，而他其实一分钱没掏。**
  背包里的礼物来自抢红包、活动或签到，钱是别人出的；主播照样有收益，但这笔不该算进
  送礼人的消费。此前的判别依据是「背包礼物的 `total_coin` 为 0」——**这个假设是错的**。
  <br>2026-08-06 实测两条真实的背包礼物，`total_coin` 都**等于礼物原价而不是 0**
  （小花花 100、红包中奖后送出的人气票 100）。也就是说光看金额永远分不出背包礼物，
  它和一笔普通付费礼物在数值上完全一致。线上实测的后果：一份白得的礼物被记成
  「该用户实付 ¥0.1」，消费排行榜会因此排进没花过钱的人。
  <br>真正的判别字段是 **`bag_gift`**：背包礼物为一个对象，普通礼物为 `null`。
  现按此判断，背包礼物的实付记 0，到手价值仍按面值计。
  <br>此前支撑那个错误假设的单测，`total_coin: 0` 是手工构造的——
  它只能证明代码按假设运行，证明不了假设成立。现已换成实抓的报文。
- **盲盒的单测换成真实报文，并钉死一个容易踩的字段陷阱。**
  此前「盲盒亏损时实付应是盒子的价」用的是构造样本。现换成 2026-08-07 实抓的一笔：
  花 ¥15 的心动盲盒开出 ¥9 的棉花糖，实付 15、到手 9，方向由真实数据钉死。
  <br>顺带记下一个陷阱：这条报文里 `combo_total_coin` 是 **9000**（开出物的价）
  而不是 15000。误用它会让盲盒实付系统性地记成开出物的价，
  **而这个错误在「保本」的盲盒上完全看不出来**——恰好就是此前唯一那条真实样本的形态。
- **大航海的营收按挂牌价记账，实测高估 15.4%；另有 14% 的上舰从头到尾没被记录过。**
  同一次开通最多由三条消息播报，此前只认前两条，且「谁先到留谁」——
  而 `GUARD_BUY` **恒定先到**（60 笔实抓样本里 25/25，且只差一条消息之隔），
  于是留下的永远是它。偏偏它的 `price` 是**挂牌价**：35 条样本的舰长价一律 198000，
  一次都没变过；实际成交价只有 toast 才有（实测 138000 / 168000 / 198000 三种都出现）。
  25 笔配对样本按挂牌价合计 ¥6750，按实际成交价合计 ¥5850。
  <br>现改为把 `GUARD_BUY` **压 5 秒等 toast**，等到就作废、等不到才发出。
  实测两条消息到达相差不超过 1 秒，5 秒留足了余量，这点延迟对推送没有影响。
  <br>同时补上了 `USER_TOAST_MSG_V2`：它与 `USER_TOAST_MSG` 是同一件事的两种格式，
  而此前解析器根本没注册它。49 笔上舰里有 **7 笔只以 V2 形式下发、且没有 `GUARD_BUY` 兜底**，
  这 14% 此前完全不可见，**而且丢了不会有任何报错**。
  <br>三条消息之间靠 `payflow_id` 去重（实测 72 条 toast 无一缺失，同一流水号下金额完全一致），
  缺流水号时才退回按「人 + 等级 + 时间窗」判断。**判重不看时间**——两条消息的时刻来源不同
  （v1 取 `send_time` 毫秒，V2 缺该字段时回退到 `guard_info.start_time` 秒），
  拿时间去否决流水号会让同一笔双双放行，记成两笔。
  <br>**尚未修完**：只有 `GUARD_BUY`、等不到 toast 的那 18%，金额仍只有挂牌价可用，
  真实值在 138/168/198 之间无从判断。记挂牌价偏高、记空又丢了量级，
  正解是把「纸面价值」与「实收」拆成两个字段分开存，留待专门一轮处理。

## [5.0.0-beta1] - 2026-08-06

> 5.0 开发线的正式起点。此前的 4.3.0-beta1 与 4.4.0-beta1 已经在做 5.0 的内容，
> 版本号补上而已；那两个 beta 的条目仍列在下方，未做合并。
> 4.x 稳定线在 4.2.1 冻结，不再接收新功能。

### 修复

- **登录口令的哈希要人手工抄进配置文件，抄错就再也登不进去。**
  程序只在日志里打出那串 80 个字符的哈希，让使用者自己替换过去——
  实际发生的是复制时掉了最后一位，此后任何口令都登录失败，
  **而报错与「口令输错了」一模一样**，人只会以为是自己记错了密码。
  <br>现改为程序自己完成：界面上填的明文在写入配置文件前就被哈希掉，
  配置文件里手写的明文也会在启动时换成哈希写回。**配置里从此不会留下明文，
  使用者也不必知道哈希这回事。**
  <br>另外，哈希串损坏时启动会明确报错并指出是哪种损坏
  （段数不对 / 迭代次数非法 / 盐或哈希长度不足），不再让人对着「口令不正确」猜。

## [4.4.0-beta1] - 2026-08-06

### 修复

- **大航海事件可能一直在被静默丢弃。** 解析器只监听 `USER_TOAST_MSG`，
  而实测线上跑到现在**一次大航海都没记录过**——计分表 0 人、累计指标里
  `captain_count` 等键根本不存在，5 场归档也全是 0。同期第三方在 45 个房间/小时的
  匿名抓包共观测到 3 次大航海开通，**3 次都只来了 `GUARD_BUY`**，无一伴随 `USER_TOAST_MSG`。
  <br>证据一致但不算铁证（3 例太少，也可能只有续费或特定档位才发），因此**两条都收**：
  `GUARD_BUY` 是当前确定收得到的那条，`USER_TOAST_MSG` 保留以防它只是登录态才推。
  <br>**两条都收就必须去重**——大航海是金额最大的一类事件，算两遍会让营收凭空翻倍，
  而这个错误在数据上完全说得通，不会有任何地方报错。
  <br>**（2026-08-06 更正）** 上面「3 次都只来了 `GUARD_BUY`」这条依据已被推翻。
  自行抓了 60 笔上舰后：两条消息**通常同时下发**（25 笔同时收到），
  `USER_TOAST_MSG` 并未废弃，反而是覆盖面最广的那条。当时那 3 例只是采样太少。
  「两条都收」的结论侥幸没错，但理由是错的——详见 [未发布] 中的修复条目。
  <br>时长单位优先取消息自带的 `unit`，取不到才从 `start_time`/`end_time` 的天数推；
  两者都推不出时留空而不是猜一个，猜错会在报告里变成假信息。

- **保存一个带换行的配置值会写出一份解析不了的 application.yml**，而接口照样回报「已保存」，
  问题要到下次重启才暴露成安全模式。多出来的那行顶在第 0 列，整份配置从此不合法。
  <br>现改为当场拒绝并指出是哪一项。字符串列表不受影响——那里换行本就是分隔符。
  <br>顺带查证了同一条路径能否**注入出新的配置项**（比如把 `allow-ips` 改成任意地址）：
  不能。值里带 `: ` 时会被引号包住，折行后仍是同一个字符串。相应用例一并留下，
  将来若放宽了对换行的限制，注入才是真正危险的那一面。

- **礼物排行榜在按运气排名。** 盲盒的计分用的是「开出物面值」而不是「观众实付」，
  于是**花 100 元开出一堆小心心的人排在榜尾，花 10 元中了大奖的人排到榜首**。
  而普通礼物那一支的注释白纸黑字写着「排行榜比的是送了多少钱」——两者从一开始就对不上。
  <br>现改为一律按实付计分。

### 变更

- **配置界面前端改为 ES module**，不引入任何构建步骤——浏览器原生支持，改完刷新即可。
  <br>原先是一串 `<script>` 按顺序加载、靠共享全局互相调用。那份顺序是隐性约定：
  谁依赖谁只存在于人的记忆里，插错一行就是 `undefined`。现在依赖写在 import 里，由浏览器解析。
  <br>跨页签共享的可变状态集中到 `store.js`。**不能各自 export 一个 `let`**：
  ES module 的导入是只读绑定，`import { tab }` 之后再 `tab = 'push'` 会直接抛 TypeError。

### 新增

- **配置界面支持口令登录，为开到公网做准备**（`starbot.core.config-ui.auth`）。
  单用户、无账号名。**二次验证默认要求**：设了口令却没绑验证器时，界面会持续提示去绑，
  可以「暂不绑定」跳过本次登录，要永久关闭得显式改 `auth.totp`。
  只有口令的面板一旦开到公网，安全性就全押在这一个口令上，而口令是会被撞库、被键盘记录、被肩窥的。
  <br>绑定走界面上的扫码引导，**必须先输一次验证码才算绑定成功**——少了这一步，
  扫码没扫上的人会以为绑好了，下次登录被自己的二次验证挡在门外，那时已经没有界面可以撤销了。
  确认后密钥自动写回配置文件，先落盘再启用。
  <br>**默认不开**：不填口令时面板维持原样——仅回环地址可达、地址栏带令牌即进，
  单机使用不必多输一次密码。填了口令才切换形态，此时**访问令牌不再作为凭据**：
  公网地址栏里挂着长期令牌等于把钥匙贴在门上，而会话有期限、可注销、可一键全部失效。
  <br>会话设两道期限：**绝对期限**从登录起算、不因使用而顺延（约束的是被偷走的 Cookie 还能用多久），
  **闲置期限**从最近一次操作起算。会话只在内存里，不落盘——
  写进磁盘等于凭空多出一份需要保护的长期机密。
  <br>写请求要求 `X-CSRF-Token` 请求头，配合 `SameSite=Strict` 与来源校验。
  用自定义头是因为跨站页面能让浏览器自动附上 Cookie，却加不了自定义头。
  <br>登录失败按来源 IP 锁定并逐次翻倍，**不做全局锁定**——那等于让攻击者一键把主播关在门外。
  另设并发闸门：校验一次口令要独占一个核心约 570 毫秒（PBKDF2 60 万次迭代），
  **未登录的请求因此可以直接消耗 CPU**，不限并发的话几十个请求就能把弹幕处理线程饿死。
  <br>口令与验证码错误的提示完全相同：分开说等于告诉攻击者口令已经猜对了。
  <br>另加「退出全部设备」。没有它，怀疑 Cookie 泄漏时唯一的收回手段是重启进程，
  而重启会断开全部直播间长连接，正在直播时代价不小。
  <br>**启动令牌保留为运维通道**：忘记口令或验证器丢了时，拿启动日志里的令牌仍能进去，
  换到一个正常会话后照常使用。能读到启动日志的人本就对这台机器有完全控制权，
  因此不构成额外泄漏；没有这条路，忘记口令就只能改配置重启。
  该通道只认地址栏参数与 `Authorization` 头、**不认 Cookie**——认 Cookie 就等于把它也变成一个
  CSRF 面；反过来，跨站页面既读不到令牌也设不了自定义头。每次使用都写一条 WARN 日志。

- **配置界面不再把口令、令牌与密钥摆在画面里**。设置页此前把所有字段一视同仁渲染成明文输入框，
  其中躺着登录口令、二次验证密钥、访问令牌、邮箱与 Redis 口令。
  **主播直播中打开面板，这些就直接进了画面**——而二次验证密钥一旦泄漏，
  二次验证就永久失效且当事人不会察觉。
  <br>现在接口只回传占位值，界面渲染成密码框并附一个显示开关；保存时原样送回的占位值会被剔除
  （不剔除的话，改别的字段一起保存就会把 `********` 写进配置，口令与密钥当场全废）。
  <br>判定按字段名而非注解：一半的机密项来自框架，我们没有那些类可以标注；
  更要紧的是**将来新增的机密字段默认就是遮住的**，而注解方案漏标一次就是泄漏一次。
  <br>「高级：直接编辑配置文件」仍显示原文——要编辑就必须看得到，界面上已就此加了提示。

- **接上看过人数与高能用户数**（`WATCHED_CHANGE` / `ONLINE_RANK_COUNT`）。
  这两条消息一直在长连接上流着，只是被丢掉了，接上不增加任何网络开销。
  报告新增「看过人数」曲线，数据分析页也能看到这两项。
  <br>命名上如实叫「看过人数」而非「在线人数」：它是本场累计来过多少人，只增不减，
  有信息量的是曲线的**斜率**。标错名字比不做更糟。
  <br>另新增 `maxLiveSeries`：这类**瞬时读数**每分钟下发好几次、每次给的都是当前值，
  沿用累加的话，一个真实值 8000 的读数下发 5 次就成了 40000——
  **而这个数看起来完全合理，不会有任何地方报错**。
  <br>顺带记录：心跳响应里的「人气值」实测恒为 1，哔哩哔哩早已废弃，不予采集。

- **礼物金额拆成「实付」与「到手价值」两个口径**（新指标 `gift_paid`）。
  两者只在盲盒与背包礼物上不同，普通礼物完全相等：
  盲盒是「付了盲盒的钱、收到开出物的价值」；背包礼物则是观众一分钱没花而主播照样有收益，
  因为钱是买礼物红包的那个人出的。
  <br>实付取自服务端给出的 `total_coin`（实际扣除额），比自己拿单价乘数量可靠——
  打折与背包礼物都体现在它身上。已用一条真实报文核对
  `total_coin == discount_price × num`；另有一条实抓的盲盒确认
  `total_coin` 跟的是盲盒价而非爆出面值。
  <br>字段取不到时事件上的实付**留空**而不是填一个算出来的值——
  空表示「平台没告诉我们」，下游据此才能分辨「两个口径确实相等」与
  「取不到才回退成相等」。回退到到手价值这一步交由消费方做，
  **但绝不能回退成 0**：把「不知道」记成「没花钱」会让营收凭空少一截且不会报错。

- 日志级别转换器。`starbot.core.log.console` / `.file` 的类型是
  `ch.qos.logback.classic.Level`，此前缺转换器，**这两项只要填值就会让程序进入安全模式**。
  配置模板里原本没有它们，一致性测试因而也没覆盖到。

- 数据分析页新增「逐场流水」视图。周期汇总回答不了「上周六那场到底怎么了」，
  而被切流、只播十分钟这类事情在周汇总里全被平均掉了。被中断的场次单独标出。

## [4.3.0-beta1] - 2026-08-06

> 5.0 开发期的第一个 beta。4.x 稳定线已在 4.2.1 冻结。

### 新增

- **区分「主动下播」与「被平台切断」**。此前一律按主动下播归档，于是一场被掐掉的直播
  会以「时长 12 分钟、营收几乎为零」的面目混进趋势图里，看起来只是「这天状态很差」——
  **这是数据正确性问题，不是缺个说明**。
  <br>判据取自直播间长连接自身下发的 `CUT_OFF` / `ROOM_LOCK` 指令，不需要额外请求接口。
  先试过回查直播间状态：`get_info` 根本不返回封禁字段，而 `getInfoByRoom`
  在补齐 WBI 签名与 buvid 之后**实测仍被风控挡回 -352**，这条路走不通。
  <br>违规警告（`WARNING`）也一并接上——它是切流之前的最后一次提醒，处理及时就不会真被切。
  三者都走**告警通道**而不是粉丝群推送：需要它们的是主播本人，把「本场被平台切断了」
  广播给观众除了尴尬没有别的作用。

- **场次内记录标题与分区变化**。开播记一次初始标题，此后每次改动各记一条，
  进下播报告也进场次归档。一场直播改了几次标题、什么时候改的，配上互动曲线才看得出
  换标题之后人气有没有起来。平台在主播打开设置又原样保存时照样下发通知，内容没变的不计。

- **高能时刻**。复用互动曲线现成的分钟分桶，挑出本场弹幕最密集的几个时段，
  给出**距开播多久**——也就是回看录播时要拖到的位置。
  <br>三条约束使结果可信：同一段热度只算一个时刻（不会报成「第 30、31、32 分钟」）；
  基线取中位数而非均值（均值会被高峰自己抬高）；**并且要过绝对门槛**——
  一场总共二十条弹幕的冷场不会硬凑出三个「三倍于基线」的时刻，那一栏干脆不出现。

- **事件触发外部命令**（`starbot.core.exec`，**默认关闭**）。开播启动录播、上舰写台账、
  被切流发提醒这类内置功能覆盖不到的用法，都可以从这里接。
  <br>命令以**参数数组**执行，绝不走 `sh -c`：事件里带的是昵称、弹幕正文这类完全由陌生人
  决定的内容，一旦拼成命令行字符串，一个昵称叫 `; rm -rf ~` 的观众就能让脚本执行任意命令。
  走参数数组时这段文本无论长什么样都只是一个参数——注入在结构上就不成立。
  <br>另有超时强杀、并发上限（超限直接丢弃而非排队）、输出截断、独立线程池。

- **非直播期间的数据留档**（`snapshot-interval`，默认 6 小时）。定时采样粉丝数、
  粉丝团人数、大航海人数，补上两场直播之间的空白。此前所有数据都只存在于直播期间，
  「这周涨了多少粉」只能答成「每场开播时的粉丝数分别是多少」，一周没播就是一片空白。
  <br>取不到的项**缺席而不是记成 0**，否则趋势图上会多出一个假的断崖。

### 变更

- 场次归档新增 `endReason` 与 `titles` 两个字段。4.3.0 之前的记录没有这两项，
  读取时按「正常结束、无标题记录」处理——**宁可把一场被切的算成正常，
  也不能把正常场次误标成事故**，后者会让人去追查一个不存在的问题。

- 下播报告新增 `highlights` 与 `title_changes` 两个版式开关，默认开启。
  两者都只在有内容时才占版面。

## [4.2.1] - 2026-08-06

> 4.x 的最后一个稳定版。此后进入 5.0 开发期，只发 beta。

### 修复

- **开播推送可能被一句「开播@我」打断**。运行状态存储的 `namespace()` 文档写的是「只读视图」，
  实际返回的是**活对象本体**。调用方拿到之后在锁外遍历，而写入来自消息线程——
  群里有人订阅的同时开播推送正在读订阅名单，遍历中途结构变化就抛
  `ConcurrentModificationException`，**偏偏是在最要紧的那一刻把推送打断**。
  <br>现改为返回深拷贝。深而不是浅：订阅名单是「会话 → 订阅者集合」的两层结构，
  只换外层的话，真正被遍历的内层还是那个活对象。
  <br>受影响的还有控制台「群与成员」页的三处列表（绑定关系、命令开关、金额可见性），
  它们同样在锁外遍历。
  <br>这个类自己的注释里写着「写入来自消息线程，与自动保存的序列化并发时会让 fastjson2
  在遍历中途遇到结构变化」——危险是知道的，只是 `namespace()` 把守卫漏掉了。

## [4.2.0] - 2026-08-06

### 新增

- **「谁能看到金额」成为会话自己的属性**，在「群与成员」页每个会话卡片顶部开关，
  **默认私聊显示、群聊隐藏**。它同时管住下播报告与全部数据查询命令。
  <br>此前报告只有「要不要这个区块」这一个轴，而主播真正要表达的是「这份东西给谁看」——
  两者正交，用区块开关去逼近隐私需求的结果是关了六个开关还漏。更要命的是
  `直播间数据`、`数据排行榜 礼物` 这些命令谁都能在群里打，它们不属于任何推送目标、
  读不到报告的版式配置，只关报告等于没关
  <br>关掉后并非少画几块：概览行的收益整段省略，卡片改用人数与条数，
  曲线保留形状但不标峰值（归一化面积图不含绝对值，正是最适合给大群看的氛围），
  而礼物榜、醒目留言榜、盲盒盈亏榜**整榜不出**——那三张榜的每一行都是「某人花了多少钱」，
  只抹掉数字仍然是在公开排消费。于是「大群放词云、私聊看全量」不必维护两套版式

### 修复

- **下播报告的「本场收益」此前没有任何开关**。即使把其余区块全部关掉、只留一张数据卡片，
  这一行照样把整场收入写在报告最显眼的位置
- **群里发「直播报告」命令时，界面上给该群配的版式完全不生效**。这条路径用的是一套默认值，
  关掉的区块照样画出来，金额也照样带着。现已改为取该会话所配的那条推送的参数

## [4.1.1] - 2026-08-05

### 修复

- **掉线判定不再看「群里多久没人说话」**。此前判断 OneBot 长连接是否存活，
  用的是「多久没收到聊天消息」，而群里有没有人发言与 OneBot 实现活没活着毫无关系：
  夜里没人说话就必然告警。线上七天误报 **35 次**，其中 20 次是同一个通宵每半小时一次地连续触发，
  而那段时间连接一直正常。代码本身跑得没错，错在它测量的是另一件事。
  <br>现在改看 OneBot 实现推送的**心跳**（`meta_event/heartbeat`，NapCat 默认 30 秒一次），
  与聊天活跃度无关。连续三个心跳周期没来才判失效，一次网络抖动不会误报。
  心跳被关掉时静默不再具备判据意义，程序提示一次并停用这条判据，改由 HTTP 检测兜底——
  拿一个失效的判据继续告警，比不告警更糟
- **同一次故障不再收到两条内容雷同的告警**。OneBot 的 HTTP 检测既写连接状态又自己发一次告警，
  而连接状态又会被健康探针读走再发一次，两条只是标识不同。现统一走
  「连接状态 → 健康探针 → HealthAlertMonitor」这一条路，且改为状态变化时才通知，
  不再每个检测周期重复播报
- **连接断开后不再残留存活检测任务**。旧任务持有的是上一条连接的计时器，
  会在重连期间把状态改回「已连接」，覆盖掉「正在重连」的真实状态

### 变更

- **「QQ 账号是否在线」从 HTTP 状态里拆了出来**，健康自检现在分三项显示
  `HTTP / 账号 / WS`。账号掉线时 OneBot 实现本身完全正常、接口照样返回 200，
  只是消息谁也收不到；混作「HTTP 异常」会让人去查一个根本没坏的东西。
  账号掉线判定为不可用，并直接提示去重新扫码
- 心跳自带 `status.online`，因此账号掉线现在**半分钟内**就能发现，
  此前最长要等一个 HTTP 检测周期（默认 5 分钟）
- 配置项 `starbot.adapter.onebot.detect.websocket-detect-interval`
  更名为 **`websocket-silence-timeout`**，默认值 1800 秒改为 **120 秒**。
  旧名字描述的是「检测周期」，而它实际是超时阈值；如今检查固定每 30 秒跑一次，
  超时按心跳间隔取，两者不再是一回事。实际超时不会小于三个心跳周期，
  因此该项配得比心跳间隔短也不会误报

## [4.1.0] - 2026-08-05

> 正式版。beta1 与 beta2 的全部内容都在其中，另外补上了控制台的两个新页与若干修复。
> 使用 beta1 的请务必升级——那一版会绕过 QQ 的 @全体成员 权限，有账号风控风险。

### 修复

- **控制台上的四处排版与措辞问题**，逐个页签看下来发现的：
  健康自检的每一行都排错了（建议文字想独占一行，但那一行不换行，
  于是把「结论」那一列压到 47 px，一个字一折行——而总览是打开界面第一眼看到的地方）；
  设置页「包含高级选项」的开关被样式规则拉成 140 px 的灰条；
  「添加主播」输入框窄到显示不全提示语；机器人适配器没起来时，
  推送规则里的平台下拉显示「（尚无可用的机器人）」，而配置里其实是有值的——
  保存不会把它写没，但界面在说一句与事实不符的话，现已改为列出该值并标注「当前不可用」

- **累计数据与邮件告警的配置项终于能在界面上填了**。这两处此前是死胡同：
  健康自检提示「累计查询需配置 `spring.data.redis.host`」，而使用者到设置页去搜
  什么也搜不到——配置界面按设计只展示 `starbot.` 命名空间，框架自身的配置一律隐去。
  现在把 NovaBot 实际依赖的少数框架配置显式加入白名单，说明自行撰写为中文，
  讲的是「配了它 NovaBot 会多出什么能力」而非框架自己的用途
- **清空 Redis 地址不再让程序起不来**（把该项搬上界面后新引入的坑）。
  写成 `host:` 这样的空值时，Spring 启动阶段直接抛 `'host' must not be empty`，
  连配置界面都起不来，只能手改文件。现在这类「空值致命」的配置项在界面上清空
  等于删掉整行，语义回到「不配置」

### 新增

- **控制台新增「数据分析」页**，回答「上周播了几场」「这个月比上个月如何」。
  按周或月聚合 `sessions.jsonl` 里的场次，可只看某一位主播，
  给出场次、总时长、场均时长与各项互动指标的趋势（柱状图用内联 SVG 画，
  页面仍不引入任何外部依赖）。**只在后台呈现**——运营数据是给机器人主人做判断的，
  发到群里既没人看也涉及主播的经营信息
  <br>三条规则决定了数字的对错，都写进了界面说明而不是藏在代码里：
  **整场算在开播那一天**（跨零点的直播不拆成两半）、**按本地时区分周**
  （凌晨开播那场不会被 UTC 推到上一周）、**没播的周期照样列出并显示 0 场**
  （空格子本身就是信息）
  <br>两处**故意不给**的数字：各计分表相加得到的是**人次**不是人数——归档只存了
  每场的人数、没存人的名单，周月的独立人数根本算不出来，写成「人数」就是拿一个
  偏大的数字骗人；开播时的粉丝数等**快照类指标一律不参与累加**，
  把十场的粉丝数加起来不是任何一个真实数字
  <br>指标的中文名与「能不能相加」由平台插件通过 `LiveMetricCatalog` 声明，
  核心只负责聚合。没有对应实现时降级为只统计场次与时长，并在界面上说明原因
- **控制台新增「群与成员」页，`state.json` 的内容不再是黑箱**。命令开关、
  「@我」订阅名单、账号绑定这三类数据全由群成员在聊天里产生，此前只存在于状态文件中——
  机器人的主人打开界面看不到任何痕迹：**某个群为什么突然不应答**（管理员把命令关了）、
  名单里积了多少人、谁绑了哪个 uid，一概无从得知，只能登服务器读文件。
  现在按会话分卡片列出全部命令及其开关，订阅与绑定各一张表，可移除单人、清空名单、解绑。
  改动**立即生效并立刻落盘**，不受底部「保存」按钮管辖
  <br>只提供「关闭」与「移除」，**不提供代人订阅或代人绑定**：这两件事都以本人意愿为前提，
  尤其绑定本就无法验证归属，替别人建立绑定等于凭空造出一条看似可信的对应关系
  <br>两类残留也一并显示而非藏起来——从推送配置里删掉但状态仍在的会话（命令还关着，
  一旦重新配置推送就立刻生效），以及指向已不存在命令的禁用记录（改过名或已删除，可一键清掉）。
  **状态文件里有的，界面上就得看得见**，否则这一页只是换了个地方骗人
- **直播场次归档**，运营分析的地基。此前只有两份数据：本场数据（**每次开播清零**）
  与累计总数（**没有时间维度**），两者都答不出「上周播了几场」「这个月比上个月如何」——
  每一场直播只留下「它对总数的贡献」，场次本身不复存在。
  现在每次下播追加一条记录到 `sessions.jsonl`（主播、开播/下播时刻、时长、
  全部指标与各计分表人数），周月统计由此只是按时间区间聚合。
  <br>存成每行一条 JSON 的追加式文件而非放进 Redis：不依赖外部服务（没配 Redis 的
  部署一样有历史）、追加写没有读改写周期（不会因崩在中途毁掉既有记录）、
  体量极小（每条 300~800 字节，5 个主播每天 3 场跑一年也就几 MB）且人能直接看。
  <br>**跨零点的直播整场算在开播那一天**，否则同一场会在两个统计周期里各算半截。
  没有记录到开播时间的场次不归档——算不出时长也归不进任何周期，留着只会污染统计

## [4.1.0-beta2] - 2026-08-05

> 预发布版本。**修掉了 beta1 里一个有风控风险的行为**，若已下载 beta1 请换用本版。


### 安全

- **不再绕过 QQ 的 @全体成员 权限**（4.1.0-beta1 的问题）。实测发现：一个在群里只是普通成员、
  客户端根本点不出 @全体成员 的账号，**经 OneBot 接口发送却能真的 @ 到全体**——
  服务端这一路没有做客户端那道权限检查。这是平台的漏洞，能做不等于该做：
  绕过权限强行 @ 全体属于滥用，有招致账号风控的风险，而风控的代价是整条推送链路停摆。
  现在发送前先查机器人在群里的角色，不是群主或管理员就把占位符摘掉，正文照发。
  查不出角色时放行——不该因为一次接口抖动就悄悄吞掉使用者明确配置过的 @全体成员。
  新增扩展点 `AtAllPermissionResolver`：核心定策略，平台适配器供事实

### 修复

- **@全体成员 配额模型改对**（4.1.0-beta1 的问题）。原先按会话计数、上限 10，
  并注释为「与 QQ 一致」——**那是错的**。实测（`get_group_at_all_remain`）：
  QQ 的额度有两个维度，**账号 10 次/天由全部群共享**，群 20 次/天各群独立。
  按会话记 10 意味着推 3 个群时会放行 30 次，而平台第 10 次就截断了，守卫等于没守住。
  现在两个维度都记、都要有余额才放行（`at-all-daily-limit` 与
  `at-all-session-daily-limit`），且被拒的那次不再吃掉另一维度的额度
- **权限判定排在配额之前**。否则一个注定被摘掉的 @ 会白白吃掉账号那份全局额度
- **配置界面的「发送测试消息」不再因跳过一条就中断**。`{next}` 会把「`{at=all}`」
  与正文拆成两条，前者被有意跳过时原先会 `break`，导致**正文一起没发出去**，
  界面还报「发送失败」。现区分「有意跳过」与「失败」，并在结果里说明跳过了几条。
  真实推送路径不受此影响（每条消息独立出队投递）

## [4.1.0-beta1] - 2026-08-05

> **这是预发布版本。** 本次改动量大（15 个提交，路线图六个阶段外加一个权限漏洞修复），
> 其中不少功能只过了单元测试与本地出图，**尚未经过真实直播的完整验证**——
> 待验清单见下方「本版待验证」。生产环境请等 4.1.0 正式版。


### 安全

- **管理类命令补上权限校验**（漏洞）。此前命令层没有任何权限判断，
  **群里任何人都能发「禁用命令 直播报告」把功能对全群关掉**，也能把别人关掉的开回来，
  且不留任何记录。现「启用命令」「禁用命令」仅群主、群管理员或配置的超级管理员可用
  （`starbot.core.command.admins`，跨群生效——机器人的主人未必是每个群的管理员）。
  判定统一在分发器完成而非各命令自行判断：规则散落在各处，漏写一处就是一个洞。
  被拒绝时明确回一句而非沉默（否则使用者只会反复重试），成功与被拒都记审计日志。
  私聊一律不算管理员——私聊没有群角色，若在此放行，任何人私聊机器人都能改动群里的开关。
  为此 OneBot 适配器开始解析原先被丢弃的 `sender.role` 字段

### 新增

- **@全体成员 每日配额**。QQ 对 @全体成员 有每日上限，用超之后**平台会静默忽略**——
  消息照发但 @ 不生效，配置的人往往过很久才发现「怎么没人被 @ 到」。现在自己这一侧先记账
  （`starbot.core.push.at-all-daily-limit`，默认 10），超额时主动把 @ 摘掉、退化为普通消息。
  检查点放在消息发送器而非各推送处理器：处理器只经手 `at_all` 参数，
  而模板里手写的 `{at=all}` 同样会 @ 全体，只有在发送器这一处才拦得全。
  **占位符独占一条消息时整条跳过**——`Message.create` 在 `{next}` 处就把消息拆开了，
  摘掉占位符会剩下一条空消息发进群里（这个 bug 是补测试时发现的）
- **头像地址在存储层压缩**。存的是地址不是图片，但地址本身不便宜：实测平均 69 字符，
  其中 `https://iN.hdslb.com/bfs/face/` 前缀就占 30 字符，而昵称平均只有 6 字符。
  按容量上限十万用户折算，光这段重复前缀在累计存储里就是约 3 MB／每个主播，
  且累计数据不会清零。现存为 `0:abc.jpg` 形态，每条固定省下 28 字符
  （折算比例随路径长短而变，哈希型地址约 38%，短路径可达 60%）。
  **CDN 编号必须保留**——线上实测确实会下发 i1 节点，砍成固定前缀等于替服务端选节点。
  认不出的地址原样存放，还原对未压缩值幂等，因此升级前存下的完整地址不必迁移

- **排行榜显示用户头像**。此前没做是怕撞风控：一张榜十几个人，绘制时逐个查接口既慢又危险。
  改成与昵称同一个思路——头像地址在弹幕事件里现成带着，**在事件到达时顺手记下**，
  绘制时只需下载图片、完全不打接口。图片按地址做 Caffeine 缓存（500 张 / 6 小时），
  同一个人在多张榜、多份报告里只下载一次，且用 `@30w.webp` 取缩略图而非原图
- **健康自检新增「数据存储」项**，显示当前跑的是哪种直播数据实现、累计查询可不可用。
  此前这件事只在启动日志里说过一次，而「查不到累计数据」既可能是没配 Redis、
  也可能是 Redis 挂了，两者的处理方式完全不同


- **推送内容的参数可在界面上配了**。处理器在 `StarBotEventHandler.options()` 里
  声明自己有哪些参数，配置界面按类型渲染成开关与数字框——界面本身不认识任何具体参数名，
  因此第三方插件声明了 `options()` 同样能被配置。首个受益者是下播报告的 11 项版式选项
  （封面、卡片、本场变化、互动曲线、大航海名单、词云与五张排行榜的名次数），
  此前只能在 `datasource.json` 里手写，而使用者根本无从知道有哪些键、取值范围是多少。
  只有改动过的项会写进配置文件，没碰过的继续跟随处理器的默认值；
  超出区间的数字在界面上当场夹回，不必等保存后才发现被改掉
- **下播报告支持自定义标识**。`starbot.bilibili.live.report-logo-path` 填本地图片路径即可，
  绘制在署名之上，按固定高度等比缩放；与动态图片的标识分开配置，因为两者未必想打同一个标。
  路径写错只会跳过绘制并记一条警告，不会让整张报告失败
- **下播报告新增互动曲线**。弹幕、礼物、醒目留言、盲盒、大航海各一条面积图，
  横轴是本场直播的时段，一眼能看出哪一刻热起来。数据来自新增的时间序列存储
  （一分钟一格，随本场数据清零，不并入累计）。
  **每条曲线各占一张、各按自身峰值缩放**：弹幕以「条」计、礼物以「元」计，
  量级动辄差两个数量级，共用纵轴的结果是除最大的那条外全被压成一条直线
- **下播报告新增粉丝、粉丝团与大航海的本场变化**。开播时记一份快照，
  出报告时取实时值相减即得涨幅——因此直播中随时拉的实时报告与下播报告
  走的是同一段逻辑。三项各自独立降级：某个接口取不到就只跳过那一项；
  程序在直播中途才启动、没有开播快照时只显示当前值，不编一个涨幅出来
- **账号绑定与数据查询命令**（9 条）。`绑定 <uid>` 查到昵称后回复 `确认绑定` 完成关联，
  `解绑` 可退出；随后 `我的数据` / `我的总数据` 查本人在直播间的互动与名次，
  `直播间数据` / `直播间总数据` 查整体，`数据排行榜` / `总数据排行榜` 查榜单并可翻页
  （每页 10 人，支持弹幕、礼物、醒目留言、盲盒、盲盒盈亏、大航海六张榜）。
  结果统一以图片呈现，为零的项不占位——只玩过一项的人不会看到五个「0」。
  **绑定核对的是 uid 有没有打错，不是账号归属**：没有接口能证明两个平台的账号属于同一人，
  这一限制在代码与手册中都写明了；可查到的也只是此人在直播间里公开可见的互动量。
  「总」字类命令需要配置 Redis，未配置时明确回复「需要配置 Redis」而非返回 0——
  后者会让人以为是数据丢了
- **`菜单` 按分类分组**。命令数量已上到 19 条，平铺的清单没人看得下去。
  新增 `StarBotCommand.category()` 扩展点，插件自带的命令同样可以归类；
  分类顺序取首次出现的顺序，因而稳定，不会每次发菜单都换个模样

- **聊天命令系统**。实现 `StarBotCommand` 并注册为 Bean 即可成为一条命令，
  核心不维护任何注册表，插件可自带命令。三条横切约束由分发器统一处理，
  避免每个命令各写一遍、写漏一处就成事故：**只在已配置推送的会话响应**
  （机器人常同时在多个群，在无关群应答等同于打扰）、**未知命令保持沉默**
  （群里正常聊天不该被机器人纠正）、**同会话冷却**。
  内置 `菜单`（列出可用命令）、`启用命令` / `禁用命令`（按群开关，
  这三个自身不可禁用——否则关掉就再没有入口开回来）。
  命令前缀可配置（`starbot.core.command.prefix`），默认留空
- **「@我」订阅**。群成员发送 `开播@我` / `动态@我` 即可在主播开播或发动态时被 @，
  另有 `取消开播@我` / `开播@名单` 等共六条命令。订阅按「群 + 主播 + 类型」独立，
  本群配置了多位主播时会列出让人指定而非替其猜测。
  推送模板新增 `{at}` 占位符承载订阅者，无人订阅时自动消失不留空行。
  订阅数据存于新的运行状态文件 `state.json` 而非 `datasource.json`——
  后者是使用者手写的配置，程序去改会覆盖人的编辑意图

- **下播报告的五类排行榜与大航海名单**。弹幕、礼物、醒目留言、盲盒数量、盲盒盈亏
  各成一榜，另有本场开通大航海的观众名单。每行含名次、昵称、按榜首归一化的比例条与得分，
  前三名以金银铜配色区分。榜单内容与展示与否可**按推送目标分别配置**：
  同一场直播推给不同群时，可以各自决定展示哪些区块、每榜取前几名
  （`danmu_ranking` 等填数字，0 为不展示；`cover`/`cards`/`guard_list`/`danmu_cloud` 为开关）。
  越界取值会夹到合法区间而非让报告画崩。
  昵称在事件到达时随计分一并记下，绘制榜单无需逐个请求接口——一场直播的榜单动辄数十人，
  逐个请求既慢又容易触发风控


- **Webhook 告警通道**。QQ 告警走的是机器人自身的推送链路，OneBot 掉线或 QQ 掉登录时
  告警也一并失效——而那恰恰是最需要收到通知的时刻。本通道只依赖一个外部地址，
  不经过机器人链路，适配 Bark、Server 酱、Telegram 等服务；请求方式与 JSON 字段名可配置，
  另支持附加请求头以适配需鉴权的服务。已真机验证：停掉 NapCat 后告警确实经 Webhook 送达。
  同时补全配置模板中邮件告警的说明——此前只列了 `spring.mail.username`，
  而实际还需 host/port/password 与 `starbot.core.alert.mail.default-to`，
  导致这条同样不依赖 QQ 的通道形同虚设
- **本场直播数据统计与下播报告图**。直播期间自动累计弹幕（条数与参与人数）、付费礼物
  （价值与送礼人数）、免费礼物、盲盒（个数与盈亏）、醒目留言、大航海（舰长/提督/总督人次
  与价值）、新增关注、进房人数、点赞与分享，新增「下播报告」推送处理器在下播时
  把统计绘制为报告图片推送，为零的条目自动省略。统计指标随直播数据一并持久化，
  程序中途重启不丢；开播时清零，`LiveDataService.resetLiveData` 至此有了实现
  （上游遗留的空方法体）。统计能力以 default 方法加在 `LiveDataService` 上，
  第三方替换实现未覆盖时静默降级，不影响启动。
  报告版式：直播间封面横幅 + 白边圆头像压在横幅下沿，时长与收益概览行，
  三列数据卡片栅格，底部为**弹幕词云**（jieba 分词 + kumo 渲染，全横排、哔哩哔哩粉蓝配色）。
  词云统计的是弹幕到达时即时分词得到的**词频**而非弹幕原文——体积有上界、可随直播数据
  持久化，超大直播间也不会把数据文件撑爆；停用词、单字与纯数字不入词云。
  封面或词云数据不可得时对应区块整体省略，版式自动收紧
- **「直播报告」消息命令**。在群里发送「直播报告」即可拉取正在直播主播的实时数据报告，
  不必等到下播；直播中的报告以当前时刻为时长终点，标注「直播中」。
  仅响应**配置了下播报告推送的群**，其他会话一律沉默；同群 30 秒冷却防刷屏。
  底层为新的通用远程消息事件：OneBot 适配器把收到的聊天消息发布到事件总线
  （`StarBotRemoteMessageEvent`），各平台模块凭此实现自己的消息命令

### 变更

- **下播默认模板内置直播时长**。时长计算 4.0.0 就已实现，但默认模板不含 `{time}`，
  未自定义模板的使用者实际见不到这个功能。现默认渲染为「〇〇 直播结束了，本场直播时长 2 分 8 秒」。
  时长不可得时（如程序在开播后才启动，未记录到开播时间），`{time}` 所在的**整个分句**
  会连同前导标点一并移除，消息自动退化为「〇〇 直播结束了」，不会留下「本场直播时长 」
  这样的悬空半句。该规则对自定义模板同样生效，分句以中英文标点或换行为界，不跨越 `{next}` 分条边界。

### 修复

- **扫码登录改走 TV 端接口，凭据得以自动续期**（长期困扰）。Web 端扫码登录时服务端返回的
  持久化刷新口令实测恒为空串（已排除 source 参数、设备标识、User-Agent 等猜想），
  导致自动续期从来没能真正跑起来，凭据到期后只能重新扫码。改用 TV 端登录接口后，
  服务端会在响应体的 `cookie_info` 中直接下发 Cookie，并一并返回有效期 180 天的
  `access_token` 与 `refresh_token`，续期走 oauth2 接口，在到期前 30 天自动完成。
  续期沿用既有的「验证通过再落盘」顺序，中途任何一步失败都不会让账号失去可用凭据。
  可用 `starbot.bilibili.account.qr-code-login-mode` 切回 `web`（不建议，仅作退路）。
  升级后需重新扫码一次以取得可续期的令牌；旧凭据在到期前仍可继续使用

### 本版待验证

以下功能已上线运行，但**尚未在一场完整的真实直播中验证过**：

- 累计数据首次并入 Redis（需一次真实下播触发）
- 下播报告里的互动曲线、粉丝/粉丝团/大航海涨幅、带头像的排行榜
- 群内各条数据查询命令的端到端流程
- 管理员权限的实际拦截效果、@全体成员 配额（需真的用满 10 次）

已在真实环境确认的：数据跨重启不丢、时间序列开始记录、
头像地址被记录且压缩生效（新旧两种存储形态可共存）、健康自检五项全 OK。

## [4.0.0] - 2026-08-04

### 安全

- **推送接口补全身份校验**。上游 `OneBotController` 注册的推送接口无任何鉴权，任何可访问该端口的
  程序都能借由 StarBot 发送任意消息。补全了核心中已预留但未实现的 Token 校验契约
  （见 `Sender#token` 的原始注释），采用恒定时间比对防止通过响应耗时爆破 Token。
- **新增来源 IP 白名单**，支持精确 IP 与 CIDR 网段，兼容 IPv4 与 IPv6，默认仅放行回环地址。
- **新增推送接口频率限制**，令牌桶实现，按「接口路径 + 来源 IP」维度独立计数。
- **新增鉴权失败审计日志**，记录来源 IP 与拒绝原因，不记录 Token 本身。
- **新增启动期弱配置自检**，Token 过短或包含常见弱口令时告警，可配置为直接终止启动。
- **登录凭据改为加密存储**。哔哩哔哩凭据由明文改为 AES-256-GCM 加密，密钥独立成文件，
  凭据与密钥文件均以 0600 权限创建。检测到旧的明文文件会自动迁移并备份原文件。
- **凭据类禁止输出内容**，`Cookies` 与 `WebSign` 的 `toString` 强制脱敏，防止在日志或异常堆栈中泄露。
- **IP 匹配拒绝主机名**，只接受 IP 字面量，避免请求处理线程上发生 DNS 解析（可被用于阻塞线程）。
- **长连接数据包解析增加边界校验**，长度字段非法时终止解析（原实现会死循环）、
  解压产物超过 32 MB 放弃（防解压炸弹）、递归展开限制在 3 层内。

### 新增

- **内置配置界面**。启动后访问日志中输出的地址即可逐项修改配置，含常规配置、推送配置、
  配置文件直编与运行状态四个页签。界面字段由程序读取编译期生成的
  `spring-configuration-metadata.json` 自动生成，字段说明取自代码 Javadoc，
  因此界面与代码中的配置项在结构上不可能不一致。保存时逐行写入 `application.yml`，
  完整保留原有注释、顺序与格式，写入前自动备份。
- **配置界面访问控制**。默认仅监听回环地址并要求访问令牌，令牌未显式配置时每次启动随机生成
  并输出到日志；支持来源 IP 白名单。
- **一键安装脚本 `install.sh`**。自动检查并安装 JDK 17 与中文字体、从源码构建、
  创建系统用户与 systemd 服务、升级时保留既有配置与登录凭据。
- **配置项一致性测试**。校验三件事：不存在声明了却从未被读取的配置项、配置模板中不含已删除的
  配置项、每个配置项都有中文说明。任一条不满足即构建失败，从机制上防止配置体系出现漂移。
- `starbot-bilibili` 模块的独立实现。上游未公开该模块源码，仅以二进制形式分发，
  本仓库依据哔哩哔哩公开接口重新编写，不含任何反编译代码。对外暴露的事件全限定类名与配置项
  命名与上游保持一致，既有 `datasource.json` 与 `application.yml` 可直接沿用。
- 明确以公开源码为基准。上游 3.0-beta8 二进制构建于 2025-12-09，公开源码仓库的提交在其之后，
  两者的推送配置格式已不同：二进制用 `event`（事件类名），公开源码改用 `handler`（处理器类名）。
  本仓库跟随公开源码，`datasource.json` 需填 `handler`；从 beta8 升级时需相应调整。
- 单元测试 232 项，覆盖鉴权、限流、IP 匹配、凭据加密、配置文件读写与校验、配置项一致性、
  数据源的增删改与 JSON 解析、消息发送链路、推送闸门、健康探针、告警通道、插件依赖下载、
  Cookie 续期、WBI 签名、数据包编解码、直播间消息解析与动态绘图。
  其中 WBI 签名以哔哩哔哩接口文档中的公开示例作为测试向量校验。
- GitHub Actions 持续集成：构建、测试与依赖漏洞扫描。
- `build.sh` 一键构建脚本，`dist/templates` 下的 systemd 单元、Dockerfile 与配置模板。
- 文档：[用户手册](docs/user-guide.md)、[排障与 FAQ](docs/troubleshooting.md)、
  [架构说明](docs/architecture.md)、[性能实测](docs/performance.md)，
  以及 README、SECURITY、CONTRIBUTING。

- **配置保存前校验**。此前 `/api/raw` 保存 `application.yml` 时不做任何检查，连 YAML 能否解析
  都不验证。远程部署时写坏一个缩进，下次重启程序起不来，而配置界面随之一同挂掉，
  使用者就被锁在门外，只能 SSH 进去手工改文件。现校验 YAML 语法（指出出错行号）与字段类型；
  类型表取自编译期元数据且**不受界面展示前缀限制**，因此 `server.port` 这类框架配置同样覆盖。
  推送配置增加语义校验：处理器类名是否已注册、推送平台是否存在、type 与 num 是否合法、uid 是否重复。
  校验一律宁可漏报也不误伤——查不到类型的配置项直接放行，以免把合法内容挡在外面。
- **安全模式**。主程序因配置问题启动失败时，不再直接退出，而是接管端口提供一个只能编辑配置文件的
  极简页面。该页面**刻意不使用 Spring**：坏掉的正是 `application.yml`，再起一个 Spring 上下文
  只会以完全相同的方式失败。仅监听回环地址并要求访问令牌。
  实测「写坏配置 → 进入安全模式 → 网页改回 → 重启正常」闭环可用。
- **配置备份改为多份留存并支持回滚**。此前只有一个每次覆盖的 `.bak`，连续保存两次后
  最初的内容就再也找不回来了。现每次保存生成带时间戳的独立备份，保留最近 10 份，
  界面可选择版本回滚；回滚本身也会先备份当前内容，因此误回滚同样可以再滚回来。
- **健康探针机制**。各模块实现 `HealthProbe` 并注册为 Bean 即可把自身健康状况汇入统一视图，
  核心无需反向依赖插件。运行状态页据此展示各项检查的状态、描述与**修复建议**——
  异常时只说「异常」而不说怎么办，是本项目排障成本高的主要来源。
  现有四个探针：哔哩哔哩登录态、机器人连接（HTTP 与 Websocket 分别判定）、直播间长连接
  （区分「连不上」与「被数据风控」）、推送活动（最近是否真的推成功过）。
  探针约定为廉价且不阻塞：只读内存中已有的状态，实际探测由各自的定时任务完成。
- **引导式首次配置**。新增「首次配置」页签，四步走完即可从零跑通：连接机器人 → 登录哔哩哔哩 →
  添加第一个主播 → 发送测试消息，每步实时显示完成状态。
  第一步会先**真实测试连接**再允许保存——把明显错误的配置写进文件毫无意义；
  测试失败时区分「连不上」「Token 不对」「端口填成了 Websocket 端口」并各自给出修复方式，
  笼统报「连接失败」帮不上忙。
  机器人连接信息位于 `senders` 列表元素内部，常规配置页按设计不展示列表元素，
  为此新增了列表元素字段的逐行写入能力：同样保留注释、对齐与其余字段不动。
- **配置项按重要程度分层**。全部配置项有八十余项，但真正需要使用者关心的只有十余项，
  界面一视同仁地平铺会让人无从判断该改哪些——问题不在配置项太多，而在没有区分
  「必须懂」与「可以不管」。新增 `@ConfigLevel` 注解，常规配置页默认只展示必填与常用项
  （实测 83 项中展示 17 项），高级项折叠并标明数量，可一键展开。
  重要程度标注就写在字段旁边，与配置项同增同减，无需另外维护清单；
  未标注的一律按高级处理，避免常用区随时间推移不断膨胀。
- **消息模板可视化编辑**。推送内容原本要在 `datasource.json` 的 `params` 里手写带转义的字符串，
  占位符还得翻 README 才知道有哪些，写完什么效果全靠脑补。现在占位符做成可点击插入的标签，
  下方以假数据实时预览渲染结果。占位符列表由处理器自行声明，插件自定义的占位符同样会出现。
- **全局推送开关与静音时段**。半夜被机器人吵醒是这类通知产品被投诉最多的点。
  新增 `starbot.core.push.quiet-start` / `quiet-end`，支持跨零点（23:00 ~ 08:00 表示当晚至次日）。
  静音期间的消息直接丢弃而非攒着——攒下来会在解除的瞬间集中轰炸，比不推更糟。
  运行状态页提供「暂停全部推送」按钮，**同时改写内存与配置文件**：只改文件要等重启才生效，
  而「临时静音」恰恰要求立即生效；只改内存则重启后又会悄悄恢复推送。
  「发送测试消息」不受该开关影响——它是用来验证配置的，被静音拦下只会让人误以为配置又出了问题。
- **推送规则改为表单化编辑**。此前「推送配置」页签是一个裸文本框，使用者要手写完整的
  `datasource.json`，其中包括处理器的全限定类名（如
  `com.starlwr.bot.bilibili.handler.BilibiliLiveOnPushHandler`）——那本是实现细节，
  不该出现在面向使用者的界面上，也是整个产品最大的上手门槛。
  现改为主播卡片：输入 uid 或直接粘贴个人空间链接，先拉取昵称与直播间号让人确认
  （避免 uid 打错一位却配了个陌生人，这类错误在推送真正发生前完全无法察觉），
  再勾选「开播 / 下播 / 动态」并填写推送目标。
  勾选项由新增的 `/api/handlers` 驱动，插件新增处理器时界面自动出现，前端不硬编码任何类名。
  保留「高级：编辑原始 JSON」入口，自定义插件的处理器仍可在此配置；
  表单未覆盖的字段（如自定义 `params`）在往返编辑中原样保留。
- **事件处理器可声明展示信息**。`StarBotEventHandler` 新增 `displayName`、`description`、
  `platform`、`placeholders` 四个**默认方法**——既有第三方处理器无需改动即可继续工作，
  只是在界面上显示为类名。
- **推送历史视图**。「刚才那条推了吗」「为什么没推」此前只能翻 journalctl。
  现记录最近 50 条推送的时间、目标、内容与结果（失败时含原因），在运行状态页展示。
  容量固定，不做持久化——推送记录是排障线索，不是业务数据。
- **告警通道抽象**。告警此前只有邮件一个出口，而这类机器人的使用者大多没有配置发件账号，
  等于没有告警。现抽象出 `AlertChannel`，除邮件外新增 **QQ 通道**（复用既有推送链路直接推给管理员，
  无需另配发件服务）。收敛逻辑统一由告警服务按「问题标识」处理，各通道不再各自维护一套间隔。
- **告警覆盖面扩大到全部健康探针**。此前只覆盖 OneBot 的两处异常，登录失效、直播间被风控、
  长连接持续重连失败等同样导致推送停摆的情况一个都没有。现在只要实现了探针就自动纳入，
  且只在状态<b>变化</b>时通知——异常出现和恢复各报一次，不每周期重复播报。
- **配置界面内置扫码登录**。二维码原本只打印在启动日志里，systemd 部署时得靠 journalctl 翻日志，
  而终端里的字符画二维码在字体或宽度不合适时根本扫不出来。现在运行状态页直接展示登录状态与
  二维码图片（服务端渲染，界面不引入任何外部依赖），扫码成功后页面自动刷新；
  并提供「退出登录」按钮——`logout()` 此前全项目零调用点，等于做了却没接上。
  退出后会自动发起新一轮扫码，同时约束同一时刻只允许一个扫码流程：
  否则两个流程会各自申请二维码并互相覆盖，界面上就会出现扫了却不生效的二维码。
- **发送测试消息**。配置完成后若无法当场验证，群号写错、Token 不匹配、OneBot 未启动、
  机器人不在群里这四类错误的表现完全一样：什么都不发生，只能等真实事件发生时才发现配错了。
  运行状态页新增该功能，直接发一条消息并回显推送接口的原始响应与排查建议。
- **OneBot 连接状态改为持续记录**。此前连接检查的结果只写进日志，界面与告警都读不到。
- **OneBot 可用性检测默认开启**。`enable-http-detect` 与 `enable-websocket-detect` 原本默认关闭，
  连带使依赖它们的邮件告警也形同虚设：OneBot 中途挂掉不会被察觉，表现为「消息发不出去且无人告知」。

- **哔哩哔哩 Cookie 自动续期**。哔哩哔哩自 2023 年起会随敏感接口的调用逐步作废 Web 端凭据，
  不续期的结果是凭据某天突然失效、动态推送静默停摆，只能重新扫码。现实现官方续期链路
  （`cookie/info` → CorrespondPath → `refresh_csrf` → `cookie/refresh` → `confirm/refresh`），
  随登录态复检一并按需执行。整条链路按「失败即维持原状」设计：只在服务端提示需要时才续期、
  换到新凭据后先做一次真实调用验证、验证通过且新凭据落盘之后才作废旧凭据，
  因此中途任一步失败账号都仍持有一份可用的旧凭据。
  可用 `starbot.bilibili.account.auto-refresh-cookie` 关闭。

  **已在真实账号上验证的部分**：续期判断接口、`CorrespondPath` 的 RSA-OAEP 签名
  （公钥与填充参数均确认与服务端一致）、实时刷新口令的获取。

  **已知限制**：续期入口所需的持久化刷新口令只在登录成功那一刻下发一次，而实测扫码登录时
  服务端会把该字段返回为<b>空串</b>，同一账号在浏览器里登录则拿得到。已实测排除的猜想：
  轮询未带 `source=main-fe-header`、设备标识不全、User-Agent 过旧——补齐后均无改善。
  拿不到口令时续期直接跳过，无副作用，但凭据到期后仍需重新扫码。
  该状态由登录健康探针显式展示，不会静默。
- **登录凭据新增持久化刷新口令**。扫码登录时一并取走并持久化 `refresh_token`，它是续期链路的唯一入口。
- **登录健康探针展示「无法自动续期」**。缺少刷新口令时自动续期会一直静默跳过，
  凭据到期后表现为「某天突然掉登录」。现于健康自检中说明，但仍记为正常——它不影响当前推送。
- **健康探针声明所属范围**。`HealthProbe#scope` 决定探针出现在界面的哪个页签，
  由探针自己声明而非界面按名称硬编码分类——否则插件新增探针后界面要跟着改一版才认得它。
- **NovaBot 自有标识**。`docs/assets/` 下新增图标与组合标（SVG 与 PNG），纯几何构造，
  与上游标识无衍生关系；权利状态与商标保留声明见 NOTICE。

### 变更

- **版本号由 3.0.0 升至 4.0.0**。此前跟随上游版本号以示同源，但本仓库已积累大量自有改动，
  继续沿用会让「装的是哪一版」无从分辨。包名、配置键与构件坐标前缀仍为
  `com.starlwr` / `starbot`，第三方插件的兼容性不受影响。
  同时修掉一处硬编码：OneBot 的 `status` 指令回复里写死了版本字符串，
  升级版本不会带着它一起变，且没有任何测试会发现这种偏差，现改为从 `BuildProperties` 读取。
- **用户可见文案统一为 NovaBot**。涉及 `install.sh`、`starbot.service`、`start.sh`、
  `Dockerfile` 与聚合 pom 的项目名。文件名、安装路径与构件坐标
  （`StarBotCore.jar`、`/opt/starbot`、`com.starlwr`）保持不变——它们是兼容性契约，
  改了会破坏既有部署与第三方插件。
  真机验证动态推送时发现绘图水印是漏网之鱼：推送出去的图片底部仍是上游品牌与
  仓库链接，会把本项目魔改后的输出误归于上游，现一并改为本项目标识。
  上游项目的致谢保留在 README 与启动 banner 中。
- **配置界面信息架构重组**。原先的「常规配置 / 推送配置 / 配置文件 / 运行状态」是按**数据存在哪个文件**
  划分的，使用者得先知道「登录态属于运行状态、机器人端口属于配置文件」才找得到入口。
  改为按用户任务划分：总览 / 推送规则 / 机器人 / 哔哩哔哩 / 设置。原始 YAML 编辑不再是独立页签，
  收进「设置 → 高级」并配独立的保存按钮，避免与逐项改动共用一个按钮而误将整份文件覆盖。
  首次配置向导从独立页签移至总览页顶部——新用户打开界面第一眼就该看到它，
  四步完成后自动收起。
- **机器人连接参数新增回填**。新增 `GET /config/api/setup/bot` 与 `BotConnectionTester#current`，
  改一个字段不必再把整套地址端口重敲一遍。**有意不回填两个 token**：
  回填只省几次输入，却让凭据白白多经过一次浏览器；保存端对空白字段是「保持原值」语义。
- **工程结构整合**。上游将 3.0 拆分为 5 个独立仓库，本仓库整合为单一 Maven 多模块工程，
  新增聚合 pom 统一管理依赖版本；构建期 Maven 插件移至 `build-tools/`，
  第三方插件模板移至 `templates/`，两者均不参与主工程构建。
- **发行包与库产物分离**。上游 `package` profile 的 Spring Boot 重打包会覆盖 `target/StarBotCore.jar`，
  使增量构建时下游模块解析到 BOOT-INF 布局的 jar 而找不到核心类。重打包产物改为输出至 `target/dist/`。
- **插件 jar 不再包含测试类**。上游插件 pom 将整个 `target` 目录拷入 jar，
  一旦模块存在测试，`test-classes` 会被打入插件 jar，插件加载器扫描时抛出
  `NoClassDefFoundError` 导致整个应用无法启动。改为仅拷贝插件描述文件。
- **移除多余依赖**。`starbot-onebot-adapter-napcat-extension` 声明了对 `starbot-bilibili` 的依赖，
  但源码中零引用，已移除；同时补充了其对 `starbot-core` 的显式声明（原先依赖传递引入）。
- 核心自调用推送接口的地址由 `localhost` 改为 `127.0.0.1`，确保稳定命中默认 IP 白名单。
- **收敛线程池与线程栈**。上游三个线程池的最大线程数为 50 至 100 且队列容量为 0 或 5，
  队列容量为 0 时每个任务都可能催生新线程；已调整为更小的池加更大的队列。
  Tomcat 工作线程由框架默认的 200 降至 8，线程栈由 1 MB 降至 256 KB。
- **共享直播间长连接客户端**。上游为每个直播间新建一个 `StandardWebSocketClient`，
  而每个实例都会创建独立的 WebSocket 容器与线程池，监听 N 个直播间就有 N 套；现改为全局共享一个。
- **动态绘图画布改为按需扩展**。画布为每像素 4 字节的位图，按「足够高」一次性分配会占用数百兆，
  在小内存机器上必然耗尽内存。

### 修复

- **推送配置热重载不同步直播间长连接**。数据源 JSON 支持热重载，但直播间连接的同步
  只在启动时执行一次：为既有用户补配直播事件后，新房间要到下次重启才会建立连接，
  反向摘除后连接也不会断开。现监听数据源变更事件重新同步（短暂合并窗口内的连发事件），
  已在真机验证增删双向均于秒级生效。顺带修掉一个连带问题：同步逻辑每执行一次
  就会多注册一份风控检测周期任务，此前因「只执行一次」而未暴露，现加一次性守卫。
- **扫码登录在服务端改版后失效**（严重）。哔哩哔哩已把扫码登录的凭据下发方式从「拼在跳转地址的
  查询串里」改为「放在轮询响应的 Set-Cookie 响应头里」，跳转地址现在只是一个带 ticket 的
  crossDomain 地址。原实现只认前者，表现为**扫码明明成功、程序却报「未能解析出登录凭据」并继续等待**。
  现两种形态都支持，并补上了回归测试。该问题只有用真实账号扫一次才会暴露。
- **登录相关的失败日志给不出可排查的信息**。「未能解析出登录凭据」「没有持久化刷新口令」
  原先都只有结论没有依据。现附上响应的字段名、字段类型与长度、Set-Cookie 的项名——
  「字段不存在」与「字段存在但为空」的排查方向完全不同。<b>只输出结构，不输出任何取值</b>。
- **QQ 告警因目标类型编码写错而永不送达**。`starbot.core.alert.qq-type` 的说明与默认值写的是
  「1 群聊、2 私聊」，但该字段与推送目标的 `type` 用的是同一套编码
  （`PushTargetType`：`GROUP(1)`、`FRIEND(0)`），取值 2 会被解析为 `UNKNOWN` 并在发送阶段直接丢弃。
  即**按默认配置启用 QQ 告警时，告警一条也发不出去，且不留任何痕迹**。
  已改正默认值与说明；同时让该通道在类型非法时直接判定为不可用并写明原因——
  告警本就是「出问题时唯一的提示」，它自己静默失效是最糟的情况。
- **OneBot 长连接重连退化为满核空转**（严重）。超时后调用 `sessionFuture.cancel(true)`
  会中断当前线程本身，使随后的退避等待立刻抛出 `InterruptedException`；异常处理仅恢复中断标志
  却继续循环，导致下一次等待再次立即抛出，重连循环以最高速度空转。
  实测在 OneBot 服务不可达时 30 秒内重试逾 61 万次，占满一个 CPU 核心并疯狂写日志。
  改为以 `cancel(false)` 取消，并在收到中断时退出循环。修复后同样场景 40 秒内仅重试 4 次
  （退避 2、4、8、16 秒），CPU 占用 3.6%。
- **移除一处虚空配置**。`starbot.bilibili.dynamic.draw-logo` 此前只声明未接线，
  改动该配置不产生任何效果；已实现对应功能，该配置随后又因移除内置标识而改为
  `logo-path`（见「移除」一节）。该问题由新增的配置项一致性测试发现。
- **登录态失效后静默停摆**。此前登录态只在启动时判定一次，`loggedIn` 没有任何地方会将其置回
  （`logout()` 全项目零调用点），动态轮询失败又只以 `debug` 级别记录，默认日志级别下不可见。
  结果是凭据过期后动态推送悄无声息地停止，而运行状态仍显示正常。
  现增加定期复检（默认十分钟一次，可配置或关闭），并在动态接口返回「账号未登录」时立即复检。
  仅当服务端明确返回该错误代码时才判定为失效，网络故障一律维持原状态——
  否则一次抖动就会误报，反而稀释告警的价值。
- **推送目标类型的取值文档写反**。README 称「`type` 为 `1` 表示群聊、`2` 表示私聊」，
  但实际取值来自 `PushTargetType`：`GROUP(1)`、`FRIEND(0)`。按文档填 `2` 会被解析为「未知」，
  运行期直接丢弃该消息，且无任何提示——私聊推送因此从来就配不出来。
  已更正文档，配置界面在保存时也会拦下非法取值。
- **每次轮询都在重新建立 TLS 连接**。默认的 `SimpleClientHttpRequestFactory` 底层是
  `HttpURLConnection`，其连接缓存的空闲超时固定为 5 秒且不可配置，而本项目的轮询间隔是 10 秒
  ——每次轮询恰好踩空缓存窗口。实测间隔 10 秒、连续 8 次查询会产生 24 次 TLS 握手，
  而把间隔缩到 1 秒（落在窗口内）时只有 2 次。改用 JDK 内置的 `HttpClient` 后同样条件下降为 0。
  选它而非 Apache HttpClient 是因为无需引入新依赖。
- **消息队列无界导致的内存风险**。发送队列此前无容量上限，OneBot 掉线时消息会持续堆积，
  小内存机器上迟早耗尽内存。现设上限并在满时丢弃最旧的一条（通知类消息越旧价值越低），
  同时计数并告警；队列积压与丢弃情况已纳入健康探针。
- **投递失败不再直接丢弃**。此前一次网络抖动就会丢掉一条推送。现对「请求本身失败」做有限次
  退避重试；服务端已明确返回业务失败（如群号不存在）时不重试——那只会重复打扰。
- **响应缺少 code 字段时的空指针**。`result.getInteger("code") == 0` 在字段缺失时会因自动拆箱
  抛出 NPE，被外层捕获后表现为消息静默丢失，而日志指向的却是别处。
- **线程池未托管**。`JsonDataSource`、`DefaultLiveDataService`、`StarBotMessageSender` 中三个
  手工创建的线程池此前既非 Spring 托管也无人关闭，停机时队列内待发的消息直接随进程消失。
  现补齐关闭逻辑：发送器会先尽力发完队列（有时限），文件监听则需主动关闭 `WatchService`
  ——它的等待不响应中断，单靠 `shutdownNow()` 叫不醒。实测停机耗时仍为 0.8 秒。
- **依赖下载后的自动重启**。此前拼出完整命令行再以 `cmdLine.split(" ")` 切分，
  安装目录或 JVM 参数带空格就会切错；且父进程要一直驻留等待子进程结束，白占一份内存，
  systemd 下的进程树也不正确。改为以约定退出码退出，交由 systemd 的 `Restart` 策略
  或 `start.sh` 的重启循环拉起。
- **运行状态页的线程数少报**。`Thread.activeCount()` 只统计当前线程组及其子组，
  在 Web 请求线程上调用会漏掉大量线程，实测同一时刻它报 15 而实际存活的 Java 线程为 25。
  改用 `ThreadMXBean.getThreadCount()`。
- **停机耗时 31 秒**。扫码登录的轮询循环运行在 `bilibiliTaskScheduler` 上且不感知停机，
  Spring 停止生命周期 Bean 时会等待其自行结束，等满 30 秒超时后才中断，
  期间还会以 ERROR 级别打印「哔哩哔哩登录失败」及完整堆栈——正常停机却报错。
  首次部署尚未扫码时必然命中，`systemctl restart` 每次要等半分钟。
  改为借 `ContextClosedEvent`（发布时机早于生命周期停止）放行闭锁，使等待立即返回。
  实测 SIGTERM 到进程退出由 31.0 秒降至 0.7 秒，停机日志中的 ERROR 亦消除。
- **消除重复实现**。`IpMatcher` 与访问令牌相关逻辑此前只存在于 OneBot 适配器中，
  配置界面同样需要，已上移至核心的 `com.starlwr.bot.core.util`，两处共用同一实现。
- **WBI 签名参数编码不一致**。上游以 URL 编码后的参数计算签名，却返回未编码的查询串，
  参数值含空格或非 ASCII 字符时签名与服务端计算结果不符。两处统一为编码后的形式。
- **直播间消息解析的空指针**。上游多处直接对可能为 null 的 `Integer` 拆箱比较
  （如 `getInteger("is_spread") == 1`），字段缺失时抛出 `NullPointerException`。
  哔哩哔哩的消息字段随版本增删属常态，此类异常会中断整个直播间的消息处理。已全部改为空值安全。
- **弹幕消息数组越界**。上游直接读取 `info[16]` 获取荣耀等级，该下标不存在时抛出
  `IndexOutOfBoundsException`。改为越界返回空。

以下三项在真机端到端联调中发现。共同点是**离线测试全绿、单看代码也挑不出错**，
必须有真实的 OneBot 实现与真实直播间才会暴露：

- **OneBot HTTP 接口始终报「服务不可用」，而 WebSocket 同 Token 认证成功**（严重）。
  `RestTemplateConfig` 建 `HttpClient` 时未指定协议版本，JDK 默认 `HTTP_2`——
  对明文 `http://` 会先发一个带 `Connection: Upgrade, HTTP2-Settings` 与 `Upgrade: h2c`
  的升级请求。NapCat 若在 HTTP 端口上同时启用 WebSocket，其 `ws` 库会接管所有带
  `Upgrade` 头的请求，并对非 GET 一律回 `405 Invalid HTTP method`。
  表现极具迷惑性：`curl` 手测同一接口完全正常。给 curl 手工补上 h2c 升级头即可复现。
  现固定使用 HTTP/1.1——OneBot 实现均为 HTTP/1.1，HTTPS 走 ALPN 协商不受影响。
- **直播间长连接握手后立刻断开，且陷入重连死循环**（严重）。认证包中的 `uid` 硬编码为 0，
  而 `token` 由 `getDanmuInfo` 取得，已登录时该请求带着 SESSDATA，服务端签发的是
  **与账号绑定**的 token。身份不一致时服务端在握手完成后约 16 毫秒切断连接且不发关闭帧
  （表现为 1006），重连再取新 token 又被拒，如此往复。现改为发送登录 uid，未登录时仍为 0。
  实测：修复前每秒重连约 10 次且从未收到任何数据包，修复后连接持续存活、正常收包。
- **重连的指数退避形同虚设**。`afterConnectionEstablished` 中清零重试计数，
  但「握手成功」不等于「连接可用」——认证被拒的连接同样会走到这里，于是每次重连都从
  最短间隔重来。与上一条叠加后，退避完全失效，最终把 `getDanmuInfo` 打成 `-352` 限流，
  而 `-352` 又极易被误判为风控或签名错误（本次排查一度就走了这条弯路）。
  现改为在收到服务端数据包时才清零——服务端只在认证通过后才下发数据，那才是连接可用的证据。

以下两项在为数据源补测试时发现，此前该模块零测试覆盖：

- **逐个添加推送用户必然抛异常**。`AbstractDataSource.add(PushUser)` 把入参包成
  `Collections.singletonList` 交给 `add(List)`，而后者第一步就是 `removeIf`，
  于是必然抛 `UnsupportedOperationException`。仓库内所有 `load()` 传的都是
  可变列表，故一直没暴露；但基类 Javadoc 写的正是「读取完毕后需调用 add 方法将推送用户
  添加至数据源中」，第三方插件实现自定义数据源时逐个添加即会踩到。
  现改为复制一份再筛选，顺带消除了「调用方的列表被就地改掉」这一副作用。
- **更新推送配置后 `getAllUsers()` 仍返回旧内容**。`update()` 只替换了索引
  `userMap` 中的条目，未同步 `users` 列表，而两者分别服务于不同调用方：
  推送链路走 `getUser()` / `getUsers()`（已是新配置），配置界面
  `/api/status` 走 `getAllUsers()`（仍是旧配置）。表现为改完
  `datasource.json` 并自动重载后，总览页的「当前已加载的推送用户」一直显示改动前的
  昵称与推送目标数，使用者据此认为配置没生效，实际推送用的已经是新配置。

以下几项来自对部署路径的复核。`install.sh` 与 `Dockerfile` 此前从未被真正执行过，
问题都是静态审查发现的：

- **容器部署完全没有持久化**（严重）。`Dockerfile` 注释里给出的 `-v "$PWD/data:/app/data"`
  一个字节都没存下来：程序读写的文件——`application.yml`、`cookies.json`、`cookies.key`、
  `datasource.json`、配置备份、`plugins-lib`——全部使用相对工作目录的路径，都落在 `/app`。
  照注释部署的话，**每次重建容器都要重新扫码登录，推送规则也全部丢失**。
  改为程序文件放镜像内的 `/opt/starbot`、启动时由入口脚本同步到 `/app`，`/app` 整个挂卷：
  配置与登录态跟着卷走，换新镜像即完成升级。
- **升级会静默卸载第三方插件**。`install.sh` 升级时 `rm -rf plugins/` 再整个复制，
  使用者自己放进去的插件就此消失——而兼容上游生态的第三方插件是本仓库明确宣传的能力。
  改为只按构件名清理内置插件的旧版本。匹配式里版本位限定数字开头，
  否则 `starbot-onebot-adapter-*` 会连带匹配 `starbot-onebot-adapter-napcat-extension-*`，
  以内置插件名为前缀的第三方插件也会被误删。
- **`install.sh --dir ""` 会以 root 执行 `rm -rf /lib`**（严重）。安装目录直接取自命令行且未经校验，
  空值或 `/usr` 之类的路径足以毁掉系统。现校验其为绝对路径且非根目录，
  并且只在目标目录确实是既有安装（存在 `StarBotCore.jar`）时才执行清理，认不出来就中止并提示。
  `--port`、`--user` 同样补上了取值校验。
- **升级过程中登录凭据会短暂暴露**。`install.sh` 原先把 `cookies.json` / `cookies.key`
  复制到 `/tmp/starbot-keep-*` 中转：root 的默认 umask 会让它们对同机所有用户可读，
  可预测的路径还能被抢先创建的同名软链劫持，脚本中途失败时更会一直留在那里。
  实际上这两个文件根本不在构建产物中，复制回来的步骤不会覆盖它们——中转本身就是多余的。
  现只中转确实会被覆盖的 `application.yml` 与 `datasource.json`，且改用 `mktemp -d`。
- **`systemd` 的内存上限低于 JVM 自己的上限**。`MemoryMax=768M`，而 `start.sh` 允许
  `-Xmx512m` 加 `MaxMetaspaceSize=192m`，再计入线程栈与代码缓存已超出该值。
  堆真涨起来时进程会先被内核 SIGKILL——不给优雅停机的机会，可能停在写 `cookies.json` 的中途，
  再配合 `Restart=on-failure` 就成了崩溃循环。改为 `MemoryHigh=768M` + `MemoryMax=1G`，
  并写明想要更低的硬上限应当同步调低 `-Xmx`。
- **停机信号在容器里传不到 JVM**。原 `ENTRYPOINT` 直接 `exec java`，尚可收到信号；
  但一旦经由 `start.sh`（它才有插件依赖重启逻辑），bash 不转发 SIGTERM，
  `docker stop` 只能等超时后 SIGKILL。现由 `start.sh` 捕获 SIGTERM 转发给 java，
  并在 `wait` 被信号打断后继续等待其真正退出。实测：改前 0.03 秒即退出、留下一个孤儿 java 进程；
  改后完整等满子进程的停机流程，无残留。
- **`JAVA_OPTS` 环境变量形同虚设**。`start.sh` 把外部传入的 `JAVA_OPTS` 拼在默认值**之前**，
  而 JVM 对同类参数取后出现的那个，于是 `JAVA_OPTS="-Xmx1g" ./start.sh` 会被默认的
  `-Xmx512m` 悄悄盖掉。改为拼在末尾，覆盖真正生效。
- **镜像里的 JVM 参数已与 `start.sh` 漂移**。`Dockerfile` 抄了一份 `JAVA_OPTS`，
  其中 `-Xms128m` 是实测已被淘汰的取值（降到 64m 可使稳态常驻内存从 130~155 MB 降到 105~110 MB），
  另外还漏了 `-Dfile.encoding=UTF-8` 与 `-Duser.timezone`。
  现镜像改为经 `start.sh` 启动，JVM 参数全项目只有一份。
- **`install.sh` 声称安装 JDK 实则安装 JRE**。从源码构建需要 `javac`，
  而脚本装的是 `*-jre-headless`；机器上恰好没有 Maven 自带的 JDK 时构建会失败。
  现按是否需要构建分别安装 JDK 或 JRE，并显式检查 `javac`。
- **`install.sh --help` 会把代码当成用法打印**。用法说明按写死的行号 `2,16p` 截取，
  脚本增删几行后就连 `set -euo pipefail` 与 `INSTALL_DIR="/opt/starbot"` 一起输出了。
  改为读到第一行非注释为止。
- **`--port` 在升级时会静默失效**。该参数靠替换 `application.yml` 里的 `port: 7827` 实现，
  而升级时保留的是使用者原有的配置文件，其中端口未必是 7827，替换便落空，
  脚本结尾却仍按 `--port` 的取值打印访问地址——给出一个打不开的地址。
  现结尾提示以文件中的实际取值为准，不一致时明确告警。
- **`build.sh` 吞掉了模板拷贝的失败**。`cp -R dist/templates/. "$OUT/" 2>/dev/null || true`
  失败时构建仍报成功，产物里却没有 `application.yml`，要到运行时才暴露成一句莫名其妙的启动失败。
- **systemd 单元的 `Documentation` 指向了别的项目**——上游 StarBot 的 Python 版仓库。
- **文件监听线程在停机时空转刷 ERROR**。停机会关闭 `WatchService`，阻塞中的 `take()`
  随即抛出 `ClosedWatchServiceException`，而 catch 分支记完 ERROR 与完整堆栈后继续循环，
  下一次 `take()` 立刻再抛——正常停机变成边空转边刷错误日志，直到进程退出才停。
  本次实跑停机时被抓到（停机只用 0.5 秒，所以仅刷出一行，停得慢就会刷满日志）。
  现将其识别为正常终止并跳出循环。这是与「停机耗时 31 秒」同一类的问题：
  **正常路径不该报错**。

### 安全（续）

- **插件依赖下载增加完整性校验**。`auto-download-dependency` 默认开启，下载的 jar 会在下次启动时
  被加载进 JVM——下载环节等同于代码执行入口，而此前对下载内容不做任何校验。
  现比对 Maven 仓库提供的 SHA-1，不匹配即换源重试；仓库拿不到校验和时保守放行并告警
  （若因此拒绝安装，只会逼使用者关掉自动下载，反而更糟）。
- **拒绝越权写入的依赖坐标**。`groupId` / `artifactId` / `version` 取自插件自带的 `dependency.json`，
  属于第三方可控输入，却同时用于拼接下载地址与落盘文件名。形如 `../` 的取值即可写出依赖目录之外。
  现对坐标做字符白名单校验，并在解析落盘路径后二次确认其仍位于 `plugins-lib` 内。
- **依赖文件改为原子写入**。此前直接写目标路径，中途失败会留下长度不足的 jar，
  要到下次启动加载时才暴露为难以理解的类加载错误。

### 移除

- **移除 MySQL 数据源与全部 JPA 依赖**。

  该数据源存的只有推送配置本身（监听谁、推给谁、推什么），而非运行时数据。
  `LoadDataSourceListener` 在应用就绪时调用一次 `load()`，此后全部在内存中，
  `add` / `remove` / `update` 也只改内存——**运行期一条 SQL 不发，且没有任何写回路径**。

  它在本仓库中不可用，也不该继续留着：

  - **配置界面完全不认它**。`/api/datasource` 读写的固定是 `datasource.json`，
    界面导航不区分 profile。选用 MySQL 后「推送规则」页照常显示、照常保存，
    改的却是一个没人读的文件——比不支持更糟，是静默地改错东西。
  - **按现状根本启动不起来**。全仓库没有任何 `spring.datasource` 配置
    （`DataSourceConfig` 原为 `@Profile("!mysql")` 才排除数据源自动配置，切到 mysql
    反而使其生效却无 url 可读），且 `ddl-auto: none` 而仓库中零个建表 SQL。
  - **没有自动重载**。JSON 数据源有文件监听，改完即刻生效；MySQL 改了行必须重启。
  - **代价与收益不成比例**。仅为它引入的依赖使发行包 `lib/` 从 62 MB 降到 30 MB
    （整包 71 MB → 39 MB），而目标部署环境是 1 GB 内存的 VPS。

  数据源本身仍是可扩展点：实现按 `spring.profiles.active` 选取，
  第三方插件可自带实现并指定对应的 profile 名。

  **不兼容变更**：删除了 `MySQLDataSource`、三个 JPA Repository 接口，
  以及 `PushUser` / `PushTarget` / `PushMessage` 上的 JPA 注解与仅供数据库使用的
  `id` 字段（`getId()` / `setId()` 随之移除；该字段在 JSON 数据源下始终为 null，
  仓库内无任何调用点）。三个模型类的其余方法签名未变。

  `org.springframework.data.util.Pair` 出现在 `FontUtil` 与 `CommonPainter`
  的公开方法签名中（返回文本宽高），原先随 JPA 间接引入。为不破坏第三方绘图插件，
  改为显式声明 `spring-data-commons`（1.6 MB）而非替换该类型。

- **移除内置的 StarBot 标识图片**。上游在类路径中内置了一张带 "StarBot" 字样的标识
  （`starbot-core/src/main/resources/logo.png`），并默认绘制在每一张推送出去的动态图片底部。

  AGPL-3.0 覆盖的是代码；图形资产是独立的著作权客体，字标还额外涉及商标属性，
  以 AGPL 开源代码并不等同于授权他人把其品牌标识用于衍生产品——何况该标识会随每次推送
  持续对外分发，而非仅存放于仓库。已删除该图片。

  **不兼容变更**：配置项 `starbot.bilibili.dynamic.draw-logo`（布尔）改为
  `starbot.bilibili.dynamic.logo-path`（路径），留空则不绘制底部标识。
  两项合并为一项而非并存，是为了避免出现「开关打开却因无图可用而不生效」的虚空配置。
  需要打自有标识的使用者在新配置中填入本地图片路径即可。

  非代码资产的来源与授权已在 NOTICE 中单列章节说明。
