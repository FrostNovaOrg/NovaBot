# ---- 构建产物 ----
target/
dist/build/
*.jar
!.mvn/wrapper/maven-wrapper.jar

# ---- 运行期数据（含凭据，务必不要提交）----
cookies.json
cookies.enc
# 加密密钥。它与 cookies.json 合起来等同于账号密码，同样不能进仓库
cookies.key
# 明文凭据迁移为加密存储时留下的备份
cookies.json.plain.bak
# 仓库根的运行期配置。程序第一次保存设置时写在工作目录。
# 只锚仓库根这一份，源码树与发行模板里的同名示例不在此列。
/application.yml
# 保存配置时的备份。名形为 原文件名.yyyyMMdd-HHmmss.bak，
# 同一秒第二份起为 原文件名.yyyyMMdd-HHmmss-2.bak。
/*.????????-??????.bak
/*.????????-??????-[0-9]*.bak
datasource.json
template-defaults.json
application-local.yml
application-dev.yml
logs/
*.log

# ---- 本机工作备忘（含本机路径，不对外）----
docs/WORK-STATE.md

# 本地脚本跑过之后留下的 Python 字节码
__pycache__/

# ---- IDE ----
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.project
.classpath

# ---- 系统 ----
.DS_Store
Thumbs.db

# 本机运维备忘，含服务器与账号信息，不进仓库
docs/OPS-NOTES.md

# 产品需求文档，暂不公开
docs/PRD-5.0.md
dist/*.tar.gz
docs/PRD-6.0-运营分析.md

# 冻结待办（含本机路径与未公开需求）
docs/BACKLOG-冻结.md

*sensitive-patterns*
*.bundle

# 运行时数据文件。程序在安装目录生成，但从仓库根跑起来时会落在这里。
# 内容都含他人个人信息或本机身份：
#   state.json      推送目标的群号／QQ 号
#   data.json       本场计分表，含观众 uid 与昵称
#   sessions.jsonl  场次归档（F5 落地后含观众 uid 名单）
#   snapshots.jsonl 主播基础数据快照
#   timeline/       事件时间线，按日一个文件，含主播名与推送目标的群号／QQ 号
# 2026-08-12 核过：当时的四者从未被提交过，这里补的是守卫不是补救；
# timeline/ 是它落地那一笔一并加的，从未存在过未被忽略的窗口。
state.json
data.json
sessions.jsonl
snapshots.jsonl
# 保存时的临时文件，以及读坏后改名留下的原件。内容与上面那几份相同，不能进仓库。
state.json.tmp
data.json.tmp
state.json.bad-*
data.json.bad-*
# 🔴 这一条必须带前导斜杠。不带的话 timeline/ 会匹配**任意深度**上任何一个叫
# timeline 的目录——包括源码包 nova-core/src/main/java/.../core/timeline/，
# 于是整个包在 git 眼里凭空消失，而 git status 一声不响、构建照常通过。
# 上面那几项是文件名不是目录名，没有这个问题，故写法不同。
/timeline/
EventDebug/
NetworkDebug/

# 本地草稿目录，不入库（锚到仓根：别处的同名子目录不跟着被忽略）
/scratch/
