# NovaBot 事件流反向代理（Caddy）
#
# 用途：让**另一台机器**上的面板读到事件流，而不必开 SSH 隧道。
#
# 🔴 装这个之前先读这一段
#
# Caddy 与 NovaBot 在同一台机器上，**只要这份配置按下面那样剥掉 X-Forwarded-\***，
# 它转发过来的连接源地址就是回环。
# ⚠️ 反过来：**若把 X-Forwarded-* 送下去**，而 NovaBot 开着
# server.forward-headers-strategy，回环判据看到的就是真实客户端 IP，
# 于是**把反代自己挡在门外**（症状是一个空的 200）。这两种情形结论相反，别混用。
#
# 按本文件这样剥头之后，NovaBot 那道「只接受本机连接」的判据**就不再等于
# 「人在这台机器上」**——任何能连上反代的人在端点看来都是本机。
# **这正是必须开 require-token 的理由**：剥头换来了「反代能通」，
# 同时也换走了回环判据的全部含义，门只剩口令这一道。
#
# 所以必须同时打开：
#
#     starbot:
#       core:
#         event-stream:
#           require-token: true
#
# 不打开就等于没有鉴权，而且表面上一切正常。
#
# 口令在配置控制台里签发（只读，既不给 shell 也不给控制台），
# 面板用它连接；控制台令牌**不要**发给面板那台机器。
#
# 🔴 下面的 127.0.0.1:7827 与 /nova/events 必须与 application.yml 对得上
#
#     server.port                          ←→ 本文件里的 7827
#     starbot.core.event-stream.path       ←→ 本文件里的 /nova/events
#
# 两者都是可改的配置项，本文件写的是模板默认值。改了一边不改另一边，症状很难查：
# 端口对不上是连不上；**路径对不上则会被下面那条「其余一律 404」吃掉**——
# 而那正是配置正确时拒绝陌生路径的样子，两种情况长得一模一样。
#
# 安装：
#   sudo cp dist/templates/Caddyfile /etc/caddy/Caddyfile
#   # 把下面的 events.example.com 改成你自己的域名
#   # ⚠️ 日志目录必须先存在且 caddy 可写，否则 validate 直接失败（是错误，不是警告）
#   sudo mkdir -p /var/log/caddy && sudo chown caddy:caddy /var/log/caddy
#   sudo caddy validate --config /etc/caddy/Caddyfile
#   sudo systemctl reload caddy
#
# ⚠️ `caddy validate` 唯一会拦下这份配置的，是上面那个日志目录不存在。

events.example.com {
	# 🔴 跨域头，放在站点级而不是某一条 handle 里，这是有意的。
	#
	# 面板连不上事件流时，会把 wss:// 换成 https:// 对同一个地址发一次普通 GET，
	# 靠状态码分辨「数据源没在运行」与「谁都够不着」——**读不到状态码就只能报「不知道」**。
	# 而浏览器要把状态码交给页面脚本，就得有这个头。
	#
	# 为什么是站点级：**探测最需要它的时刻恰恰是出错的时刻**（502/503/504 与 404）。
	# 只挂在事件流那条 handle 上，其余情形就没有它，
	# 而那几种情形正是探测要分辨的对象。
	#
	# 为什么 `*` 是安全的：这条响应不带凭据也不含任何秘密，探测方不发 Cookie。
	# （浏览器本来也不允许 `*` 与凭据并用。）
	#
	# 写在站点级 → 502 与 404 上都带着这个头；
	# 删掉这一行 → 两者都不带（否则「有头」也可能只是别处加的）。
	header Access-Control-Allow-Origin "*"

	# 只放行事件流与代签发端点，其余一律 404。
	# 这是本文件最关键的一条：配置界面、静态资源、接口全都不该经由反代出去，
	# 它们背后是能改配置的控制台。
	handle /nova/events {
		reverse_proxy 127.0.0.1:7827 {
			# WebSocket 升级头透传。Caddy 2 默认就会转发 Upgrade/Connection，
			# 这里写出来是为了让人知道它是被依赖的，改动时别顺手删掉
			header_up Host {host}
			header_up X-Real-IP {remote_host}

			# 🔴 必须删掉 X-Forwarded-*，这一行不是可选的。
			#
			# Caddy 的 reverse_proxy **默认会加** X-Forwarded-For；而 NovaBot 若开着
			# server.forward-headers-strategy（控制台的登录锁定与 https 判定要它），
			# Spring 会拿这个头改写 request.getRemoteAddress()。
			# 于是事件流那道「只接受本机连接」的判据看到的是**真实客户端 IP**，
			# 把反代自己挡在门外。症状是：
			# 握手回一个**空的 200**（不是 401、不是 403），后端日志写着
			# 「拒绝了来自 <公网IP> 的事件输出连接」。
			# ⚠️ 空 200 这个症状极难猜，别在这上面耗时间。
			#
			# 删掉之后后端看见真实 TCP 对端 127.0.0.1，回环判据放行，
			# 由首帧口令充当真正的门——这正是 require-token 存在的理由。
			# 代价：NovaBot 侧鉴权失败日志的来源恒为 127.0.0.1，
			# 「谁在敲门」改由下面那份 access log 提供。
			header_up -X-Forwarded-For
			header_up -X-Forwarded-Proto
			header_up -X-Forwarded-Host
		}
	}

	# 代签发只读口令（跨项目契约 §五①）：主播在面板里输控制台密码 + 动态验证码，
	# 换一把只读口令回来。凭据走请求体，不进 URL、不进请求头，因此不进访问日志。
	handle /nova/readonly-token {
		reverse_proxy 127.0.0.1:7827 {
			header_up Host {host}

			# 🔴 这条 handle **要让 X-Forwarded-For 送下去**，与上面那条事件流正相反。
			#
			# 端点的失败计数与锁定是**按来源 IP 记**的。收不到真实来源的话，
			# 所有失败都落进「127.0.0.1」这一个桶——而登录限流是**刻意不做全局锁定**的
			# （理由写在 LoginThrottle 的类文档里：那等于让攻击者一键把主播关在门外）。
			#
			# 所以这里**什么都不用写**：Caddy 默认就会送 X-Forwarded-For。
			# **上面那条要显式删，这条只要别删。**
			#
			# 客户端伪造这个头也没用：
			# 请求里带 `X-Forwarded-For: 9.9.9.9`，上游收到的仍是真实对端 ——
			# Caddy 默认不信任客户端带来的转发头。
			# ⚠️ 但如果 Caddy 前面还有一层反代并配了 trusted_proxies，
			# 客户端的值才会被采信。**那是另一套前提，别照搬这段结论。**
			#
			# 送了不会重演事件流那个「把反代自己挡在门外」的问题：
			# 「只接受本机连接」是挂在**事件流的握手拦截器**上的，
			# **不是 /nova 前缀级的过滤器**，这个端点不受它影响。
			header_up X-Real-IP {remote_host}
		}
	}

	# 其余路径直接拒绝。⚠️ 用 404 而不是 403：
	# 403 等于告诉对方「这里有东西但你不能看」，404 什么也不告诉。
	#
	# ⚠️ 这条还挡住了另一件事：**配置界面的令牌是走地址栏 `?token=` 的**
	# （ConfigUiSecurityFilter 从 request.getParameter("token") 取）。
	# 事件流的口令刻意不走查询串，控制台的却走——所以一旦把控制台也放行到反代上，
	# 控制台令牌会被原样抄进下面那份访问日志。**别为了图方便在这里开第二条 handle。**
	handle {
		respond 404
	}

	# 访问日志：记下来是为了让「有人在敲门」看得见（我们不做自动封禁，用可见性代替对抗）。
	#
	# 🔴 下面那段 filter 不是可选的，删了就等于把口令写进日志。
	#
	#    Caddy 的 json 日志**默认记录全部请求头**，只对 Authorization、Cookie 等凭据头做
	#    REDACTED，而 `Sec-Websocket-Protocol` **不在那份遮蔽名单里，是明文落盘的**；
	#    查询串则原样落在 uri 字段。两条路进的是同一条日志的不同字段。
	#
	#    凭据已整个挪出 HTTP 工件：本端对握手里带凭据的连接一律拒收（400），
	#    子协议这条路不再是认证方式。这道 filter 仍然要留——
	#    旧客户端照旧会把它发过来，而**握手先进日志、再被拒**，拒收挡不住落盘。
	#
	# `query { delete token }` 是额外一道：我们的端点刻意不认 `?token=`，
	# 但客户端可能自作主张加上，那样口令照样会进 uri 字段。
	log {
		output file /var/log/caddy/nova-events.log
		format filter {
			wrap json
			fields {
				request>headers>Sec-Websocket-Protocol delete
				request>uri query {
					delete token
				}
			}
		}
	}

	# TLS 由 Caddy 自动申请与续期，这也是选它而不选 nginx 的主要理由
}
