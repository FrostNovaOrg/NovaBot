# 重立基线：已交付案卷的字节对照（2026-08-21）

`render.py` 里有一条对照项：**已交付的老案卷重渲染后必须逐字节相同**。
它是「改渲染脚本没改动历史读数」的哨兵。

本次改动（缺键拒出覆盖率格 ＋ 旧 JSON 补键）**会让其中一份的字节变化**，
所以按裁决 #182 ⑦ 显式重立一次基线，并把口径改写为：
**逐字节相同，除非有显式记录的重立基线。**

> 对照项的价值是「变了会被发现」，不是「永远不变」。
> 悄悄变才是坏的；**记录着变、并证明变的不是读数**，对照项仍然成立。

## 一、改前 / 改后 md5

| 案卷 | 改前 | 改后 | 变了吗 |
|---|---|---|---|
| `84e3358.md` | `8a3b43213d8019a595a1cbdfe72cf948` | `a5db6b12b98610d55512abe7e5d51958` | **变了** |
| `d862a36.md` | `b6059a5fa7e0e2acb3660159297c973b` | 同左 | 没变 |
| `3e9dffd.md` | `dccf31418c0f791437c05e9284e9a1b2` | 同左 | 没变 |

（`d862a36.附录.md` 不经渲染脚本，未动。`733ff4e.json` 与 `e15d193.json` 没有对应的 `.md`。）

## 二、唯一那处变化的全文 diff

```diff
342c342
< - 这三个类里被红过的判据：**25** 条
---
> - 这 3 个类里被红过的判据：**25** 条
```

**全篇只有这一行不同，且不同的只是数字的写法**：中文数字「三」→ 阿拉伯数字「3」。
**读数（`**25**` 条）一个字没变。**

成因：老 JSON 缺 `本批类` 键时，渲染走的是写死的 `"三"`；补键之后走 `f" {len(...)} "`。
**这一格变化正是「不再用编出来的默认值」的直接结果。**

## 三、补的键是从哪儿来的（不是凭印象填的）

各案卷的键，取自**跑那一次时所用的那一版脚本**，用 `git show <rev>:docs/break-run/breakrun.py` 读出：

| 案卷 | 脚本版本 | 本批类 | 本批新增判据数 | 变体总数 | 本跑变体数 |
|---|---|---|---|---|---|
| `84e3358.json` | `f62f80a`（脚本与该案卷同一提交进树） | 3 | 23 | 18 | 18（整跑） |
| `733ff4e.json` | `733ff4e` | 5 | 26 | 21 | 3（挑跑） |

补键前有一道断言：**JSON 里 `变体` 的实际条数必须等于要补的 `本跑变体数`**，对不上就不补。

## 四、fail-closed 的阳性对照

拿 `d862a36.json` 做探针，逐个抽掉键：

| 抽掉 | 拒出覆盖率格 | 其余段落完好 | 没有偷偷用默认值 |
|---|---|---|---|
| `本批新增判据数` | ✅ | ✅ | ✅ |
| `本批类` | ✅ | ✅ | ✅ |
| （键齐，阴性对照） | 不喊缺键 ✅ | — | 照常给出覆盖率 ✅ |

🔴 **「其余段落完好」这一列是有来由的**：拒出这一格的第一版我写成了 `return`，
而它在 `main()` 里——那会**整份案卷都渲不出来**，等于把「这一格不可信」办成了「什么都没有」。
**fail-closed 要拒的是那一格，不是整件事。**

---

# 第二次重立基线（同日，晚于上面那次）

改动：案卷正文加一行「本跑口径：整跑/挑跑（n/N）」＋时点义提示（提交 `c297da9`）。
**这一行加在三份案卷上，所以三份的字节全变了。**

| 案卷 | 上一次基线 | 本次 | 变了吗 |
|---|---|---|---|
| `84e3358.md` | `a5db6b12b98610d55512abe7e5d51958` | `1efe89cd17be3b0aacad5d40f6278ae6` | 变了 |
| `d862a36.md` | `b6059a5fa7e0e2acb3660159297c973b` | `c8194868443c3c3690608603f3ded2f7` | 变了 |
| `3e9dffd.md` | `dccf31418c0f791437c05e9284e9a1b2` | `a17218ecfdbd4423bcde06ffc3aca9fa` | 变了 |

**变化是新增一行，没有任何一行被修改或删除**，因此**没有任何读数被改动**——
三份的「被红过的判据」条数、变体红了几条、基线条数，逐项与上一次相同。

## 🔴 这一条要照记：规矩是我立的，下一个提交就是我破的

上面那次我刚把口径改写成「逐字节相同，**除非有显式记录的重立基线**」，
`c297da9` 就改了三份案卷的字节**而没有记录**——本节是事后补的。

**这不是「忘了」，是「没有任何东西会拦住我忘」。**
按「『记住』不是修法，把它变成一个会红的东西才是修法」——**已经做了，不是挂账**：

## 五、哨兵：`breakrun.py --check-baselines`

基线数据在 `案卷基线.json`（文件名 → md5），本文件是它的叙述面。哨兵**两头都查**，
因为它们坏的方式不一样：

- **树上那份的 md5**：有人手改了案卷、或重渲了却没更新基线记录；
- **从 JSON 重渲一遍的 md5**：渲染脚本改了而**没人重渲**——
  这一格树上的字节还是老的，**只查树上那份看不出来**。

阳性对照四例＋阴性一例，机读留痕在 `案卷基线哨兵-阳性对照.json`：

| 破法 | 报出 |
|---|---|
| 手改案卷字节 | 「树上这份 … ≠ 基线 …」 |
| 改渲染脚本但不重渲 | 「重渲出来是 … ≠ 基线 …」 |
| 基线记录里有、树上没有 | 「基线记录里有，树上没有」 |
| 把 `案卷基线.json` 删掉 | 「没有基线就没有对照 —— 这不是『都对』，是『压根没在比』」 |
| （复原，阴性对照） | 退 0 |

🔴 **最后那一例是特意加的**：把基线文件删掉之后若还退 0，这道哨兵就等于随手可关，
而关掉它的样子和「全都对」一模一样。**共同纪律 34 在工具自己身上同样适用。**

**改案卷字节不是不行** —— 但要在本文件里写明「变了什么、为什么、读数动没动」，
并把新 md5 补进 `案卷基线.json`。**悄悄变才是坏的。**
