# 资源占用与调优

## 实测数据

下列数字为本仓库在 **macOS / JDK 17.0.20 / 默认启动参数** 下的实测值，测量方法与条件一并列出，
以便在自己的环境中复现或对照。

### 测量条件

- 启动参数：`start.sh` 的默认值（`-Xms64m -Xmx512m -XX:+UseSerialGC -Xss256k`）
- 已加载插件：OneBot 适配器、NapCat 扩展、哔哩哔哩插件
- **未配置任何主播**（`datasource.json` 为空数组），未登录哔哩哔哩账号
- 采样方式：启动完成并静置约 40 秒后，每 4 秒读一次 `ps -o rss=`，取多次读数

### 结果

| 指标 | 数值 |
|---|---|
| 稳态常驻内存（RSS） | 105 – 110 MB |
| 启动瞬时峰值 | 150 MB 左右（类加载期间，随后回落） |
| 堆实际使用 | 约 29 MB |
| Java 线程数 | 25 |
| 空闲 CPU | 低于 1% |

启动期的峰值来自类加载与 Spring 容器初始化，属于一次性开销，不代表长期占用。

### 带负载的实测值

上面是空载基线，用于对照 JVM 参数的影响。**实际部署后的占用要高得多**，
因此另在真实环境测了一组：

测量条件：RHEL 9.8 / x86_64 / 4 核 / JDK 17.0.20，systemd 托管，**NovaBot 4.1.0**，
监听 2 位主播（两条直播间长连接均已建立且持续收包）、OneBot HTTP 与 Websocket 均已连接、
动态轮询与备用直播轮询均在运行、哔哩哔哩账号已登录、Redis 累计数据存储已启用。
运行 1 小时 41 分后连续采样 5 次。

| 指标 | 数值 |
|---|---|
| 稳态常驻内存（RSS） | **约 476 MB**（5 次采样波动小于 0.1 MB） |
| 堆实际使用 | 144 MB（上限 494 MB） |
| Java 线程数 | 55 |
| 1 小时 41 分内累计 CPU 时间 | 45 秒（约 0.7%） |

RSS 远高于堆使用量是正常的：其余来自 Metaspace、代码缓存、55 条线程的栈
（`-Xss256k` 约 14 MB）、TLS 与 WebSocket 的直接内存缓冲，以及 glibc 的 malloc arena。
**不要按空载的 105 MB 去规划容量**，1 GB 的机器要留意 —— 见下方的容量建议。

> **本节此前记录为「约 302 MB」，那是 4.0.0 上监听 1 位主播时的实测值。**
> 4.1.0 增加了第二条直播间长连接、Redis 客户端、按用户计分的多张表与报告绘制所需的缓存，
> 稳态随之抬到 476 MB。旧数字不是测错，是条件变了——留在这里是为了说明
> **这个量级会随主播数与功能开启情况明显变化，照抄任何一个数字去规划容量都不可靠**，
> 上线后自己测一次才算数。

### 该准备多少内存

按实测的 476 MB（2 位主播、功能全开）推算：

| 机器内存 | 结论 |
|---|---|
| 512 MB | **不够**。绘制报告图时需要同时持有若干张解码后的位图，必然 OOM |
| 1 GB | 能跑，但**装不下默认的那组上限**，要一起调低几处，见下 |
| 2 GB 及以上 | 宽裕，默认参数即可 |

影响占用的主要是**主播数**（每位一条直播间长连接及其收包缓冲）和**是否启用 Redis**。
只推直播不推动态、不出报告图的话会明显更低。

默认的 systemd 单元设了 `MemoryHigh=1.2G` / `MemoryMax=1.5G`，`start.sh` 设的是 `-Xmx512m`。

**两条上限都必须高于「堆上限 + 元空间上限 + 约 150M」**——这 150M 是线程栈、代码缓存、
GC 元数据与直接内存。按默认参数即 `512 + 192 + 150 ≈ 854M`，
而**实测稳态峰值 896 MB**（`memory.peak`，2 位主播、功能全开、连续运行 63 小时）。

> 🔴 **896 MB 是那个场景下的稳态峰值，不是上界。** 它的条件写死在括号里：
> **2 位主播、功能全开、连续运行 63 小时**。主播更多、瞬时高峰更猛时它还会往上走，
> 而那种场景**没有测过**——这里不拿一个没测过的数去冒充上界。
>
> 默认的 `1.2G` / `1.5G` 因此是「**实测峰值 + 说得出理由的余量**」，不是算尽的容量：
> 这组值在生产上自 2026-08-10 起连续运行，`memory.events` 的 `high` / `max` / `oom`
> 三个计数一直是 0。主播数明显更多时请自己往上调，启动自检也会在装不下时明说。

两条上限被突破后的后果完全不同，别按同一个直觉配：

| | 突破之后 |
|---|---|
| `MemoryMax`（硬） | 进程被 **SIGKILL**，不给优雅停机的机会（可能停在写 `cookies.json` 中途），配合 `Restart=on-failure` 就是崩溃循环 |
| `MemoryHigh`（软） | 不杀进程，而是**对每一次内存分配施加睡眠惩罚并强制同步回收** |

> ⚠️ **此前这里写着「`MemoryHigh` 被突破不会静默劣化，顶到上限是看得见的」——那是错的，已删。**
> 它恰恰是完全静默的。2026-08-10 生产上 `MemoryHigh` 被设成 768M（低于 896M 的实测峰值），
> 服务被持续节流约 10 小时：本机回环的 HTTP 调用要 2~11 秒（`curl` 打同一地址只要 1.3 毫秒），
> 而 **CPU 几乎不动、堆完全健康、六项健康探针全绿、应用日志里一个字都没有**。
> 唯一看得见它的地方是 cgroup 自己的计数器：
>
> ```bash
> cat /sys/fs/cgroup/system.slice/starbot.service/memory.events   # 看 high 那一行
> ```
>
> 当时该计数以约 44 次/秒持续增长。**把 `MemoryHigh` 设在稳态峰值之下，等于让服务在预算之内
> 自我惩罚**——它的角色是 OOM 前的缓冲垫，不是稳态警察。

**1 GB 的机器上不能只压 `MemoryMax`**（等于把 OOM 提前），**也不能只压 `MemoryHigh`**
（换来上面那种静默变慢）。要按同一个公式把几处一起往下调，例如：

| 项 | 值 | 算式 |
|---|---|---|
| `-Xmx` | 384m | — |
| `-XX:MaxMetaspaceSize` | 128m | — |
| 需求 | ≈ 662M | 384 + 128 + 150 |
| `MemoryHigh` | 700M | 要高于需求 |
| `MemoryMax` | 850M | 要高于 `MemoryHigh` |

这组是**按公式算的，未在 1 GB 机器上实测**；堆压到 384m 之后绘制报告图还够不够，
要自己验一遍。并且别再跑其他吃内存的服务。

**配错了不用自己算**：启动时程序会读 cgroup 的 `memory.high` / `memory.max` 与 JVM 上限比对，
装不下会在日志里明说是哪一条、差多少、该往哪改。

### 初始堆大小的影响

初始堆会被提前提交，页面一旦触碰便长期常驻，因此 `-Xms` 直接决定了常驻内存的下限：

| 启动参数 | 稳态 RSS |
|---|---|
| `-Xms128m -Xmx512m` | 130 – 155 MB，且波动明显 |
| `-Xms64m -Xmx512m` | **105 – 110 MB** |
| `-Xms32m -Xmx320m` | 113 – 115 MB |

64m 优于 32m 是因为后者在启动期会触发更多次 GC 扩堆。默认值已据此定为 64m。

### 测量方法上的两个坑

- **不要用「强制 GC 后的读数」当稳态指标**。一次 Full GC 会扫过整个堆，把此前未触碰的页面全部变为常驻，
  实测同一进程 `jcmd GC.run` 后 RSS 反而从 110 MB 涨到 180 MB。
- **不要只取单次采样**。启动后头一两次读数仍带着类加载期的峰值，需静置后连续采样。

### 关于此前版本的数据

本文更早的版本记录为「稳态 53 – 66 MB」。该数字对当前构建**已不成立**：
其后陆续加入了健康探针、告警、推送历史、配置界面的多项能力，基线本就会上移；
且当时的采样未充分静置，读数偏低。此处的数字为重新测量所得。

### 未覆盖的部分

**每个直播间长连接会在上述基线之上带来额外开销**，包括一个 WebSocket 会话、消息缓冲区
以及事件对象的分配。这部分需要真实登录哔哩哔哩账号才能测量，本文未做实测，因此不给出具体数字。
弹幕活跃的大型直播间开销明显高于冷清的直播间，请以自己的实际部署为准。

动态图片绘制是唯一的 CPU 突发点：绘制期间单核占用会短时升高，持续约一到数秒，之后回落。

## 已做的优化

相比上游默认配置，本仓库做了以下调整，均已体现在默认值中：

| 项目 | 上游 | 本仓库 | 原因 |
|---|---|---|---|
| 核心网络线程池 | 10 / 50，队列 5 | 4 / 24，队列 64 | 队列过小会让任务直接催生新线程 |
| OneBot 线程池 | 10 / 100，队列 0 | 2 / 16，队列 128 | 队列容量为 0 时每个任务都可能新建线程 |
| 哔哩哔哩线程池 | 10 / 100，队列 0 | 4 / 32，队列 256 | 同上 |
| Tomcat 工作线程 | 200（框架默认） | 8 | NovaBot 只服务推送接口与配置界面 |
| 线程栈 | 1 MB（平台默认） | 256 KB | 调用栈很浅，几十个线程下可省下数十兆 |
| 直播间 WebSocket 客户端 | 每个直播间一个 | 全部共享一个 | 每个客户端各带一套容器与线程池 |
| 动态绘图画布 | — | 按需扩展 | 初始按「足够高」分配会一次性吃掉数百兆 |

### HTTP 客户端：从每请求重握手到完全复用

默认的 `SimpleClientHttpRequestFactory` 底层是 `HttpURLConnection`。它并非「每次请求都重建连接」
——JDK 有内置的 KeepAliveCache——但**空闲超时固定为 5 秒且不可配置**，
而本项目的轮询间隔是 10 秒，每次轮询恰好踩空缓存窗口。

实测（间隔 10 秒、连续 8 次主播信息查询，每次查询内含约 3 个请求，
以 `-Djavax.net.debug=ssl:handshake` 统计 ClientHello）：

| 客户端 | 调用间隔 | 新增 TLS 握手 |
|---|---|---|
| SimpleClientHttpRequestFactory | 1 秒（窗口内） | 2 |
| SimpleClientHttpRequestFactory | 10 秒（实际轮询间隔） | **24** |
| JDK HttpClient | 10 秒 | **0** |

1 秒间隔下只有 2 次握手，说明连接复用本身是生效的，问题确实出在「轮询间隔超出缓存窗口」。
换用 JDK 内置的 `HttpClient` 后握手完全消除。选它而非 Apache HttpClient 是因为无需引入新依赖，
对以小内存 VPS 为目标的项目更合适。

此外还修复了一处会直接占满 CPU 的缺陷：**OneBot 长连接重连原本会退化为满核空转**
（详见 CHANGELOG）。OneBot 服务未启动时，上游实测 30 秒内重试逾 61 万次；修复后 40 秒内仅重试 4 次。

其中后两项对内存影响最大：

- **共享 WebSocket 客户端**：`StandardWebSocketClient` 每个实例都会创建独立的 WebSocket 容器与
  线程池。上游为每个直播间新建一个，监听 N 个直播间就有 N 套线程池。
- **绘图画布按需扩展**：画布是 `TYPE_INT_ARGB` 位图，每像素 4 字节。若按 900×100000 直接分配，
  单次绘制就要 360 MB，在小内存机器上必然 OOM。

## 进一步压低占用

### 缩小堆

监听的直播间不多时可以压得更低：

```bash
JAVA_OPTS="-Xms96m -Xmx320m" ./start.sh
```

不建议低于 256 MB：绘制多图动态时需要同时持有若干张解码后的位图。

### 关闭不需要的功能

| 配置项 | 效果 |
|---|---|
| `novabot.bilibili.live.only-connect-necessary-rooms: true` | 只连接订阅了直播事件的直播间。⚠️ 会跳过[纯监听房间](user-guide.md#只采集不推送的房间)，开之前先确认没在用 |
| `novabot.bilibili.live.enable-connect-live-room: false` | 完全不连长连接，仅靠轮询判断开播下播；代价是收不到弹幕、礼物等事件 |
| `novabot.bilibili.live.complete-event: false`（默认） | 不为事件补全昵称头像，显著减少接口请求 |
| `novabot.core.config-ui.enabled: false` | 配置完成后可关闭配置界面 |

### 应用类数据共享

把类元数据预先归档，可缩短启动时间：

```bash
java -XX:ArchiveClassesAtExit=starbot.jsa -Dloader.path=lib,plugins,plugins-lib -jar NovaBot.jar
```

之后启动时带上归档：

```bash
java -XX:SharedArchiveFile=starbot.jsa -Dloader.path=lib,plugins,plugins-lib -jar NovaBot.jar
```

## 关于用其他语言重写

结论：不建议。

常驻内存的绝大部分来自 JVM 与 Spring 容器本身——GC 后堆实际使用不足 1 MB，说明业务对象
占比极小。真正吃 CPU 的只有动态图片绘制，而那是短促的突发负载。把绘制部分改用 Rust 之类的
语言实现，需要引入 JNI 或外部进程通信，给一个插件化的 Spring 应用增加跨语言边界，
复杂度显著上升，却省不下那几十兆的基座开销。

如果确实需要把内存压到更低，正确的方向是 GraalVM 原生镜像而不是换语言——Spring Boot 4 原生支持，
可将常驻内存进一步降低并把启动时间压进毫秒级。

代价是：反射与动态类加载需要额外的配置提示文件，而本项目的插件机制正是基于运行期类加载，
适配工作量不小，且构建产物与平台绑定。目前未提供原生镜像构建，需要的话可自行尝试：

```bash
mvn -Pnative native:compile
```
